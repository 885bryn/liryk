---
phase: 04-cache-freshness-and-repeat-load-performance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/lyrics/cache-policy.ts
  - src/core/lyrics/cache-policy.test.ts
  - src/infra/cache/file-lyrics-cache.ts
  - src/infra/cache/file-lyrics-cache.test.ts
autonomous: true
requirements:
  - CACH-01
  - CACH-02
must_haves:
  truths:
    - User gets an immediate local-cache result on repeat plays of the same Spotify track when the entry is still fresh.
    - Cached lyric entries have deterministic freshness states instead of living forever.
    - Invalid or schema-mismatched cache entries are detectable and can be discarded before they leak into runtime state.
  artifacts:
    - path: src/core/lyrics/cache-policy.ts
      provides: Pure cache entry contract and freshness evaluation rules.
    - path: src/infra/cache/file-lyrics-cache.ts
      provides: Local track-id-keyed persistence adapter for cached lyric results.
  key_links:
    - from: src/infra/cache/file-lyrics-cache.ts
      to: src/core/lyrics/cache-policy.ts
      via: persisted entry schema and validation
      pattern: schemaVersion|evaluateLyricsCacheEntry|trackId
    - from: src/infra/cache/file-lyrics-cache.ts
      to: src/core/lyrics/types.ts
      via: cached canonical lyrics payload
      pattern: ResolvedLyrics
---

<objective>
Create the cache foundation for fast repeat lyric loads and deterministic freshness checks.

Purpose: Give Phase 4 one shared cache contract and one local persistence adapter so later runtime wiring can reuse cached lyrics safely instead of re-fetching every repeat play.
Output: Cache policy module, file-backed cache adapter, and tests covering fresh/stale/expired/invalid behavior.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-cache-freshness-and-repeat-load-performance/04-RESEARCH.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-03-SUMMARY.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-04-SUMMARY.md

<interfaces>
From `src/core/lyrics/types.ts` today:
```typescript
export type LyricTrackMetadata = {
  trackId: string;
  title: string;
  artist: string;
  album?: string;
  durationMs?: number;
};

export type ResolvedLyrics = {
  sourceState: LyricsSourceState;
  renderMode: LyricRenderMode;
  lines: ResolvedLyricLine[];
  warning?: string;
  provider?: "lrclib";
  confidenceScore?: number;
  candidateId?: string;
};
```

Plan 01 should cache this canonical `ResolvedLyrics` payload plus freshness metadata, not invent a second lyrics result shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define cache entry contract and freshness policy</name>
  <files>src/core/lyrics/cache-policy.ts, src/core/lyrics/cache-policy.test.ts</files>
  <read_first>src/core/lyrics/types.ts, src/core/lyrics/lyrics-resolver.ts, .planning/phases/04-cache-freshness-and-repeat-load-performance/04-RESEARCH.md</read_first>
  <behavior>
    - Test 1: a positive cached lyric entry evaluates to `fresh` before its stale threshold.
    - Test 2: a positive cached lyric entry evaluates to `stale` after `staleAtMs` but before `expiresAtMs`.
    - Test 3: an entry evaluates to `expired` after `expiresAtMs` and `invalid` when schema version, track key, or payload shape is unusable.
    - Test 4: `not-found` cache entries use a shorter freshness window than synced/plain entries.
  </behavior>
  <action>Create `src/core/lyrics/cache-policy.ts` with the exported cache contracts Phase 4 needs: a versioned `LyricsCacheEntry` shape keyed by Spotify `trackId`, helpers to build positive and negative entries from canonical `ResolvedLyrics`, and a pure `evaluateLyricsCacheEntry(entry, nowMs)` function returning `fresh`, `stale`, `expired`, or `invalid`. Use one shared schema version constant, keep the payload as `ResolvedLyrics`, and encode shorter TTL defaults for `sourceState: "not-found"` than for `synced`, `plain`, or `low-confidence`. Do not perform file IO or provider fetches in this module.</action>
  <acceptance_criteria>
    - `src/core/lyrics/cache-policy.ts` exports `LyricsCacheEntry`
    - `src/core/lyrics/cache-policy.ts` exports `evaluateLyricsCacheEntry`
    - `src/core/lyrics/cache-policy.ts` contains explicit handling for `not-found`
    - `src/core/lyrics/cache-policy.test.ts` covers fresh, stale, expired, invalid, and negative-cache cases
    - `npm test -- src/core/lyrics/cache-policy.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/core/lyrics/cache-policy.test.ts</automated>
  </verify>
  <done>Phase 4 has one cache contract and one deterministic freshness classifier that downstream runtime code can trust.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement a local file-backed lyrics cache adapter</name>
  <files>src/infra/cache/file-lyrics-cache.ts, src/infra/cache/file-lyrics-cache.test.ts</files>
  <read_first>src/core/lyrics/cache-policy.ts, src/core/lyrics/types.ts, .planning/research/ARCHITECTURE.md</read_first>
  <behavior>
    - Test 1: writing a cache entry persists it under the track-id key and a later read returns the same entry.
    - Test 2: deleting an entry removes it without disturbing other track keys.
    - Test 3: malformed on-disk JSON or wrong schema version is treated as a cache miss, not a runtime crash.
  </behavior>
  <action>Create `src/infra/cache/file-lyrics-cache.ts` as the local persistence adapter for Phase 4. Export a factory such as `createFileLyricsCache({ filePath, fsImpl? })` with async methods to `read(trackId)`, `write(entry)`, `delete(trackId)`, and optionally `clear()` for tests. Persist entries in one versioned JSON object keyed by Spotify `trackId`; validate loaded entries against the `LyricsCacheEntry` contract before returning them. Keep this adapter storage-only: it should not decide fresh vs stale and should not call the provider resolver.</action>
  <acceptance_criteria>
    - `src/infra/cache/file-lyrics-cache.ts` exports `createFileLyricsCache`
    - `src/infra/cache/file-lyrics-cache.ts` contains `read` and `write` methods keyed by `trackId`
    - `src/infra/cache/file-lyrics-cache.test.ts` covers read/write/delete plus malformed-file recovery
    - `npm test -- src/infra/cache/file-lyrics-cache.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/infra/cache/file-lyrics-cache.test.ts</automated>
  </verify>
  <done>Phase 4 has a real local cache adapter that persists canonical lyric results by Spotify track ID and fails closed on bad data.</done>
</task>

</tasks>

<verification>
Run cache foundation tests to validate the shared freshness rules and local persistence behavior:
- `npm test -- src/core/lyrics/cache-policy.test.ts src/infra/cache/file-lyrics-cache.test.ts`
</verification>

<success_criteria>
- CACH-01 has a local storage path keyed by Spotify track ID.
- CACH-02 has a deterministic freshness policy with schema-aware invalidation instead of permanent reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cache-freshness-and-repeat-load-performance/04-01-SUMMARY.md`
</output>
