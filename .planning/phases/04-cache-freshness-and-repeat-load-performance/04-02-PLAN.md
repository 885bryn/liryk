---
phase: 04-cache-freshness-and-repeat-load-performance
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/core/lyrics/lyrics-resolver.ts
  - src/core/lyrics/lyrics-resolver.test.ts
  - src/app/lyrics-resolution-runtime.ts
  - src/app/lyrics-resolution-runtime.test.ts
autonomous: true
requirements:
  - CACH-01
  - CACH-02
must_haves:
  truths:
    - User gets repeat lyric loads from local cache without waiting for a provider round-trip when the entry is still fresh.
    - User still receives updated lyrics when a cached entry is stale, expired, or invalid.
    - Background refresh and retry do not let older track work overwrite the active track's lyrics.
  artifacts:
    - path: src/app/lyrics-resolution-runtime.ts
      provides: Cache-aware track-session orchestration for fresh hits, stale revalidation, and forced refresh.
    - path: src/core/lyrics/lyrics-resolver.ts
      provides: Fresh provider resolve path still used as the source of truth for cache writes.
  key_links:
    - from: src/app/lyrics-resolution-runtime.ts
      to: src/infra/cache/file-lyrics-cache.ts
      via: cache read/write/delete before and after provider resolution
      pattern: read|write|delete
    - from: src/app/lyrics-resolution-runtime.ts
      to: src/core/lyrics/cache-policy.ts
      via: freshness evaluation before deciding cache hit vs refresh
      pattern: fresh|stale|expired|invalid
    - from: src/app/lyrics-resolution-runtime.ts
      to: src/core/lyrics/lyrics-resolver.ts
      via: fresh provider fetch used for misses, expired entries, and revalidation
      pattern: resolveLyricsForTrack
---

<objective>
Wire the cache into lyrics resolution so repeat plays are fast and stale results self-correct.

Purpose: Turn the Phase 4 cache foundation into user-visible behavior by serving fresh cache hits immediately, revalidating stale entries safely, and evicting invalid or expired data.
Output: Cache-aware resolver/runtime logic with tests for fresh hits, stale revalidation, expired bypass, and retry-forced refresh.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-cache-freshness-and-repeat-load-performance/04-RESEARCH.md
@.planning/phases/04-cache-freshness-and-repeat-load-performance/04-01-PLAN.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-03-SUMMARY.md

<interfaces>
From `src/app/lyrics-resolution-runtime.ts` today:
```typescript
export type LyricsResolutionRuntimeDependencies = {
  subscribePlayback: (listener: (event: PlaybackRuntimeEvent) => void) => () => void;
  resolveLyricsForTrack: (metadata: LyricTrackMetadata) => Promise<ResolvedLyrics>;
  liveSyncStore: LiveSyncStore;
  mapTrackMetadata?: (event: PlaybackRuntimeEvent) => LyricTrackMetadata | null;
};
```

From Plan 01, this plan should consume:
```typescript
type LyricsCacheEntry = {
  trackId: string;
  schemaVersion: number;
  resolvedLyrics: ResolvedLyrics;
  fetchedAtMs: number;
  staleAtMs: number;
  expiresAtMs: number;
};

type CacheState = "fresh" | "stale" | "expired" | "invalid";
```

Plan 02 should extend runtime dependencies to include cache access and freshness evaluation, not duplicate those rules inline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make lyrics resolution cache-aware for fresh, stale, and expired entries</name>
  <files>src/core/lyrics/lyrics-resolver.ts, src/core/lyrics/lyrics-resolver.test.ts, src/app/lyrics-resolution-runtime.ts, src/app/lyrics-resolution-runtime.test.ts</files>
  <read_first>src/app/lyrics-resolution-runtime.ts, src/app/lyrics-resolution-runtime.test.ts, src/core/lyrics/lyrics-resolver.ts, src/core/lyrics/cache-policy.ts, src/infra/cache/file-lyrics-cache.ts</read_first>
  <behavior>
    - Test 1: a fresh cache hit returns cached `ResolvedLyrics` immediately and skips provider resolution.
    - Test 2: a stale cache hit returns cached lyrics immediately, then revalidates in the background and updates the same track session if the refreshed result changes.
    - Test 3: an expired cache entry does not render as a hit; runtime fetches fresh lyrics before completing.
  </behavior>
  <action>Update the lyrics-resolution path so runtime becomes cache-aware without changing the canonical `ResolvedLyrics` contract. Extend `src/app/lyrics-resolution-runtime.ts` dependencies to include the Phase 4 cache adapter and freshness evaluator. On track change: read cache by `trackId`, classify the entry, and branch as follows - `fresh`: project cached lyrics immediately and do not call the provider; `stale`: project cached lyrics immediately, keep the current panel readable, and launch a session-guarded background refresh; `expired` or miss: continue through provider resolution before projecting final lyrics. Keep `src/core/lyrics/lyrics-resolver.ts` as the fresh-provider source of truth used after cache misses or refreshes, and write successful refreshed results back to cache.</action>
  <acceptance_criteria>
    - `src/app/lyrics-resolution-runtime.ts` consults cache before provider resolution
    - `src/app/lyrics-resolution-runtime.ts` contains distinct handling for `fresh`, `stale`, and `expired`/miss states
    - `src/app/lyrics-resolution-runtime.test.ts` proves fresh-hit short-circuit and stale-hit background refresh behavior
    - `npm test -- src/core/lyrics/lyrics-resolver.test.ts src/app/lyrics-resolution-runtime.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/core/lyrics/lyrics-resolver.test.ts src/app/lyrics-resolution-runtime.test.ts</automated>
  </verify>
  <done>Repeat plays of a cached track are fast, while stale entries still trigger a safe refresh path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evict invalid cache entries and force refresh on retry</name>
  <files>src/app/lyrics-resolution-runtime.ts, src/app/lyrics-resolution-runtime.test.ts</files>
  <read_first>src/app/lyrics-resolution-runtime.ts, src/app/lyrics-resolution-runtime.test.ts, src/state/playback/live-sync-store.ts, .planning/research/PITFALLS.md</read_first>
  <behavior>
    - Test 1: invalid cache data is deleted and replaced by a fresh provider resolve instead of crashing or rendering poisoned lyrics.
    - Test 2: manual retry bypasses any cached entry and rewrites the cache from fresh provider output.
    - Test 3: stale refresh and retry still respect the current session token so old-track results cannot overwrite newer tracks.
  </behavior>
  <action>Finish the runtime wiring in `src/app/lyrics-resolution-runtime.ts` so invalid or schema-mismatched entries are evicted with `cache.delete(trackId)` before a fresh resolve runs. Reuse the Phase 3 session guard for both background refresh and explicit retry. Retry must bypass cached hits entirely, show the existing inline retry status, fetch provider truth, and rewrite the cache with the new result (including short-lived `not-found` entries). Do not introduce new UI controls, cache badges, or global settings in this phase.</action>
  <acceptance_criteria>
    - `src/app/lyrics-resolution-runtime.ts` deletes invalid cache entries before resolving fresh lyrics
    - `src/app/lyrics-resolution-runtime.ts` bypasses cache on retry
    - `src/app/lyrics-resolution-runtime.test.ts` covers invalid-entry eviction and retry-forced refresh
    - `npm test -- src/app/lyrics-resolution-runtime.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/app/lyrics-resolution-runtime.test.ts</automated>
  </verify>
  <done>Cache freshness is recoverable: invalid entries are purged, retries fetch fresh truth, and no stale background work leaks across track sessions.</done>
</task>

</tasks>

<verification>
Run cache runtime tests to verify fast cache hits, stale revalidation, invalid-entry eviction, and retry-forced refresh behavior:
- `npm test -- src/core/lyrics/lyrics-resolver.test.ts src/app/lyrics-resolution-runtime.test.ts`
</verification>

<success_criteria>
- CACH-01 repeat loads return cached lyrics quickly through the existing runtime path.
- CACH-02 stale, expired, and invalid entries are corrected through revalidation or forced refresh instead of lingering forever.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cache-freshness-and-repeat-load-performance/04-02-SUMMARY.md`
</output>
