# Continue Here — Phase 21

**Branch:** `gsd/phase-21-panel-container-toggle-and-ux-shell`
**Date paused:** 2026-04-17
**Phase:** 21 — Panel Container, Toggle, and UX Shell

---

## Where We Are

Wave 1 (Plan 21-01) is **complete**. Commit `2f85fbd` delivered:
- `src/web/dev-activity-panel/use-dev-activity-log.ts` — ring buffer hook (150 entries, oldest-eviction)
- `src/web/dev-activity-panel/dev-activity-panel.tsx` — panel component with auto-scroll, pause toggle, scroll isolation
- `src/web/dev-activity-panel/dev-activity-panel.test.tsx` — 14 passing tests

Wave 2 (Plan 21-02) is **not started**.

---

## Next Action

Execute **Plan 21-02**: Wire toggle, panel JSX, and auth events into `FullscreenLyricsPage`.

```
/gsd:execute-phase 21
```

The executor will detect 21-01 is done (files exist, tests green) and proceed directly to Wave 2.

---

## What 21-02 Does

**Task 21-02-01** — `src/web/fullscreen-lyrics-page.tsx`:
1. Add `showDevPanel` boolean state (alongside `showDiagnostics`)
2. Call `useDevActivityLog()` hook at component top, get `{ entries, append }`
3. Add `useEffect` watching `webAuth.sessionAccessToken` — log `[AUTH] Token refreshed`
4. Add `useEffect` watching auth connection state — log `[AUTH] Connected` / `[AUTH] Disconnected`
5. Add toggle button below "Show Diagnostics" button (`top-16 sm:top-20`, lucide `ScrollText` icon, `aria-expanded`)
6. Render `<DevActivityPanel>` as sibling of `<main>`, **outside** `viewportSurfaceRef` subtree

**Task 21-02-02** — `src/web/fullscreen-lyrics-page.test.tsx`:
- Tests: panel renders outside viewport surface, toggle opens/closes panel, auth events log to panel

---

## Key Constraints (Do Not Break)

- Panel must be **sibling of `<main>`**, NOT inside `viewportSurfaceRef` subtree (lines 982–1039)
- Panel must use `fixed bottom-4 left-4 z-20` — never flow into lyric column DOM
- Toggle button `z-20`, same subdued opacity as diagnostics toggle (`text-white/45`)
- `useDevActivityLog` result must be scoped in component state, not in rAF loop ancestors
- Auth `useEffect` must use `prevRef` sentinel (initialized to `undefined`) to suppress first-render spurious entries
- Panel is visually: `bg-black/60 backdrop-blur-sm border border-white/15 text-[10px] text-white/72`

---

## Planning Artifacts

- Context: `.planning/phases/21-panel-container-toggle-and-ux-shell/21-CONTEXT.md`
- Plan 02: `.planning/phases/21-panel-container-toggle-and-ux-shell/21-02-PLAN.md`
- Validation: `.planning/phases/21-panel-container-toggle-and-ux-shell/21-VALIDATION.md`
