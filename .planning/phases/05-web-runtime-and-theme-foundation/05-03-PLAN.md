---
phase: 05-web-runtime-and-theme-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/web/theme/theme-store.ts
  - src/web/theme/theme-store.test.ts
  - src/web/theme/theme-toggle.tsx
  - src/web/app-shell.tsx
  - src/web/app-shell.test.tsx
  - src/ui/connection/account-menu.tsx
autonomous: true
requirements:
  - THEM-01
  - THEM-02
  - WEB-01
must_haves:
  truths:
    - User can toggle theme from an always-visible pre-connect control and from connected account menu placement.
    - User-selected theme persists across reload and is restored on startup.
    - Shell keeps split-first behavior on large screens and stacked layout on small screens with explicit placeholders.
  artifacts:
    - path: src/web/theme/theme-store.ts
      provides: Theme mode state, persistence, and root class synchronization.
    - path: src/web/theme/theme-toggle.tsx
      provides: Shared icon-based toggle control used by header/account surfaces.
    - path: src/web/app-shell.tsx
      provides: Final Phase 5 shell composition using shadcn primitives and theme controls.
  key_links:
    - from: src/web/theme/theme-store.ts
      to: localStorage
      via: persisted theme key read/write
      pattern: localStorage|getItem|setItem
    - from: src/web/app-shell.tsx
      to: src/ui/connection/account-menu.tsx
      via: connected-state menu composition with theme control
      pattern: buildAccountMenu|Dropdown
---

<objective>
Implement persistent light/dark theme behavior and wire theme controls into the final Phase 5 web shell.

Purpose: Complete THEM-01 and THEM-02 while preserving WEB-01 shell clarity and locked placement decisions.
Output: Theme persistence store, reusable toggle control, and integrated shell/header/account menu behavior with tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-CONTEXT.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-RESEARCH.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-02-SUMMARY.md

<interfaces>
From `src/ui/connection/account-menu.tsx`:
```typescript
export function buildAccountMenu(
  state: UiAuthState,
  onDisconnect: () => Promise<void>,
): AccountMenuModel | null;
```

From shadcn primitives generated in Plan 02:
```typescript
export { Button } from "@/components/ui/button";
export { Card } from "@/components/ui/card";
export { Switch } from "@/components/ui/switch";
```

Plan 03 must add theme controls without changing auth/disconnect contract shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement theme persistence store and root class synchronization</name>
  <files>src/web/theme/theme-store.ts, src/web/theme/theme-store.test.ts</files>
  <read_first>src/styles/globals.css, src/web/app-shell.tsx, .planning/phases/05-web-runtime-and-theme-foundation/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: default mode resolves deterministically when no persisted value exists.
    - Test 2: toggling mode updates in-memory state, `document.documentElement` class, and localStorage key.
    - Test 3: reload hydration restores persisted value before shell uses theme mode.
  </behavior>
  <action>Create `src/web/theme/theme-store.ts` with a small theme store API (for example `createThemeStore`) that supports `light` and `dark` modes only. Persist mode under a single localStorage key (`liryk-theme`) and synchronize root class (`dark`) on every mode change. Export startup hydration helper used by shell initialization to apply persisted theme immediately. Add `src/web/theme/theme-store.test.ts` covering default mode, toggle behavior, localStorage writes, and hydration restore.</action>
  <acceptance_criteria>
    - `src/web/theme/theme-store.ts` defines `light`/`dark` mode union
    - `src/web/theme/theme-store.ts` uses `localStorage` with key `liryk-theme`
    - `src/web/theme/theme-store.ts` updates `document.documentElement.classList`
    - `src/web/theme/theme-store.test.ts` covers hydration and persistence behavior
    - `npm test -- src/web/theme/theme-store.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/theme/theme-store.test.ts</automated>
  </verify>
  <done>Theme mode is deterministic, persisted, and synchronized to root class for all shell consumers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire icon theme toggles into header and account menu, finalize split-first shell composition</name>
  <files>src/web/theme/theme-toggle.tsx, src/web/app-shell.tsx, src/web/app-shell.test.tsx, src/ui/connection/account-menu.tsx</files>
  <read_first>src/web/app-shell.tsx, src/ui/connection/account-menu.tsx, src/components/ui/dropdown-menu.tsx, src/components/ui/switch.tsx, src/web/theme/theme-store.ts</read_first>
  <behavior>
    - Test 1: pre-connect header always shows a theme toggle control.
    - Test 2: connected account menu exposes theme toggle placement while preserving disconnect action.
    - Test 3: shell keeps two-pane desktop layout and stacked mobile layout with explicit placeholder copy in inactive panes.
  </behavior>
  <action>Create `src/web/theme/theme-toggle.tsx` as a reusable icon toggle component backed by shadcn `Switch` and/or `Button` semantics. Update `src/web/app-shell.tsx` to use shadcn `Card`-based panes, keep both connection and lyrics panes visible with explicit placeholder text, and include the standalone header theme toggle regardless of auth state. Update `src/ui/connection/account-menu.tsx` so connected-state menu models include a slot/field for theme control placement in dropdown context without changing existing disconnect behavior. Extend `src/web/app-shell.test.tsx` to verify header toggle presence, connected-menu theme control rendering, and responsive structure markers/classes for split vs stacked layout.</action>
  <acceptance_criteria>
    - `src/web/theme/theme-toggle.tsx` exists and exports a theme toggle component
    - `src/web/app-shell.tsx` contains a header-level theme toggle rendered in disconnected state
    - `src/ui/connection/account-menu.tsx` exposes connected-menu theme control support and still contains `disconnectLabel`
    - `src/web/app-shell.test.tsx` covers pre-connect toggle visibility and connected-menu toggle behavior
    - `npm test -- src/web/app-shell.test.tsx src/web/theme/theme-store.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/app-shell.test.tsx src/web/theme/theme-store.test.ts</automated>
  </verify>
  <done>Users can switch themes before and after connection, persisted mode survives reload, and final Phase 5 shell behavior is in place.</done>
</task>

</tasks>

<verification>
Run final Phase 5 checks:
- `npm test -- src/web/app-shell.test.tsx src/web/shadcn-checkpoint.test.tsx src/web/theme/theme-store.test.ts`
- `npm run build`
</verification>

<success_criteria>
- THEM-01 and THEM-02 are met with visible controls and persisted state.
- WEB-01 shell remains browser-first, split-first, and explicit-state driven.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-runtime-and-theme-foundation/05-03-SUMMARY.md`
</output>
