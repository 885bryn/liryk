---
phase: 05-web-runtime-and-theme-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - components.json
  - package.json
  - tailwind.config.ts
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/switch.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/web/shadcn-checkpoint.tsx
  - src/web/shadcn-checkpoint.test.tsx
  - .planning/phases/05-web-runtime-and-theme-foundation/05-SHADCN-CHECKPOINT.md
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - User-visible shell surfaces are built from shadcn/ui primitives, not ad hoc controls.
    - shadcn initialization is captured as an explicit checkpoint artifact before further UI composition.
  artifacts:
    - path: components.json
      provides: shadcn configuration baseline.
    - path: src/components/ui/button.tsx
      provides: Required Button primitive for shell actions.
    - path: src/components/ui/dropdown-menu.tsx
      provides: Required Dropdown primitive for account theme placement.
    - path: src/web/shadcn-checkpoint.tsx
      provides: Rendered primitive verification surface.
  key_links:
    - from: src/web/shadcn-checkpoint.tsx
      to: src/components/ui/button.tsx
      via: primitive showcase rendering
      pattern: Button
    - from: src/web/shadcn-checkpoint.tsx
      to: src/components/ui/card.tsx
      via: shell card surface rendering
      pattern: Card
---

<objective>
Initialize shadcn/ui and verify required Phase 5 primitives before shell feature composition proceeds.

Purpose: Lock in UI-02 early so Phase 5 and later phases avoid mixed component systems and refactor churn.
Output: shadcn config files, required primitive components, and a checkpoint artifact proving primitives render.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-CONTEXT.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-RESEARCH.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-01-SUMMARY.md

<interfaces>
Phase 5 required primitives by locked decision: `Button`, `Card`, `Switch`, `Dropdown`.

From `src/ui/connection/account-menu.tsx`:
```typescript
export type AccountMenuModel = {
  accountLabel: string;
  secondaryLabel?: string;
  disconnectLabel: string;
  onDisconnect: () => Promise<void>;
};
```
Account menu composition in Plan 03 will consume Dropdown + Switch primitives from this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn baseline and generate required primitives</name>
  <files>components.json, package.json, tailwind.config.ts, src/lib/utils.ts, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/switch.tsx, src/components/ui/dropdown-menu.tsx</files>
  <read_first>package.json, tailwind.config.ts, src/styles/globals.css, .planning/phases/05-web-runtime-and-theme-foundation/05-CONTEXT.md</read_first>
  <action>Run shadcn initialization compatible with the Vite React scaffold (`npx shadcn@latest init`) and create baseline `components.json`. Add required shadcn dependencies in `package.json` and utility helper `src/lib/utils.ts`. Generate required primitives in `src/components/ui`: `button.tsx`, `card.tsx`, `switch.tsx`, and `dropdown-menu.tsx`. Ensure generated components compile with current TypeScript/Vite setup and Tailwind content config still covers `src/**/*.{ts,tsx}`.</action>
  <acceptance_criteria>
    - `components.json` exists with aliases referencing `src/components` and `src/lib`
    - `src/components/ui/button.tsx` exists and exports `Button`
    - `src/components/ui/card.tsx` exists and exports `Card`
    - `src/components/ui/switch.tsx` exists and exports `Switch`
    - `src/components/ui/dropdown-menu.tsx` exists and exports dropdown components
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>shadcn baseline setup is complete and required primitives are available for shell composition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create explicit shadcn install checkpoint artifact and rendered primitive verification</name>
  <files>src/web/shadcn-checkpoint.tsx, src/web/shadcn-checkpoint.test.tsx, .planning/phases/05-web-runtime-and-theme-foundation/05-SHADCN-CHECKPOINT.md</files>
  <read_first>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/switch.tsx, src/components/ui/dropdown-menu.tsx</read_first>
  <behavior>
    - Test 1: checkpoint surface renders Button, Card, Switch, and Dropdown primitives.
    - Test 2: checkpoint doc records setup command and verification command outputs for reproducibility.
  </behavior>
  <action>Create `src/web/shadcn-checkpoint.tsx` that renders a lightweight shell checkpoint section demonstrating all required primitives in one surface. Add `src/web/shadcn-checkpoint.test.tsx` asserting each required primitive label/role appears. Create `.planning/phases/05-web-runtime-and-theme-foundation/05-SHADCN-CHECKPOINT.md` documenting exact install command(s), generated files, and the verification command used to prove primitives render before continuing with Phase 5 shell composition.</action>
  <acceptance_criteria>
    - `src/web/shadcn-checkpoint.tsx` imports Button, Card, Switch, and Dropdown primitives
    - `src/web/shadcn-checkpoint.test.tsx` asserts all four primitive types are rendered
    - `.planning/phases/05-web-runtime-and-theme-foundation/05-SHADCN-CHECKPOINT.md` contains `npx shadcn@latest init`
    - `.planning/phases/05-web-runtime-and-theme-foundation/05-SHADCN-CHECKPOINT.md` contains `npm test -- src/web/shadcn-checkpoint.test.tsx`
    - `npm test -- src/web/shadcn-checkpoint.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/shadcn-checkpoint.test.tsx</automated>
  </verify>
  <done>Phase 5 has an explicit shadcn install checkpoint artifact and rendered proof of required primitives.</done>
</task>

</tasks>

<verification>
Run shadcn baseline checks:
- `npm run build`
- `npm test -- src/web/shadcn-checkpoint.test.tsx`
</verification>

<success_criteria>
- UI-02 is satisfied by explicit shadcn setup and verified primitive rendering artifacts.
- Required primitive set (Button, Card, Switch, Dropdown) exists before milestone shell composition.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-runtime-and-theme-foundation/05-02-SUMMARY.md`
</output>
