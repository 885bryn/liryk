---
phase: 05-web-runtime-and-theme-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - index.html
  - tsconfig.json
  - tsconfig.node.json
  - vite.config.ts
  - postcss.config.cjs
  - tailwind.config.ts
  - src/main.tsx
  - src/styles/globals.css
  - src/web/app-shell.tsx
  - src/web/app-shell.test.tsx
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - User can open a browser-rendered shell without desktop-only runtime assumptions.
    - User sees header and two primary shell regions immediately, including explicit placeholder content when disconnected.
  artifacts:
    - path: src/main.tsx
      provides: Browser entrypoint that mounts the web shell.
    - path: src/web/app-shell.tsx
      provides: Base header + split/stack shell composition with placeholder states.
    - path: tailwind.config.ts
      provides: Tailwind setup required for shadcn and shell styling.
  key_links:
    - from: src/main.tsx
      to: src/web/app-shell.tsx
      via: React root render
      pattern: createRoot|AppShell
    - from: src/web/app-shell.tsx
      to: src/ui/connection/connect-spotify-card.tsx
      via: connection pane composition
      pattern: buildConnectSpotifyCard|Connect
---

<objective>
Create the browser runtime scaffold and baseline shell layout for Phase 5.

Purpose: Establish WEB-01 early so all later Phase 5 work builds on a real browser entrypoint instead of desktop assumptions.
Output: Vite React scaffold files, Tailwind baseline wiring, and an executable shell model with tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-CONTEXT.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-RESEARCH.md
@.planning/phases/01-spotify-connection-foundation/01-04-SUMMARY.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-04-SUMMARY.md

<interfaces>
From `src/ui/connection/connect-spotify-card.tsx`:
```typescript
export type ConnectSpotifyCardModel = {
  title: string;
  onboardingExplainer: string;
  trustCopy: string;
  ctaLabel: string;
  isBusy: boolean;
  showPermissionSummary: boolean;
  onConnect: () => void;
};
```

From `src/ui/lyrics/live-lyrics-panel.tsx`:
```typescript
export type LiveLyricsPanelModel = LiveLyricsViewModel & {
  title: string;
  trackLabel: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold browser runtime entry and app shell contract</name>
  <files>package.json, index.html, tsconfig.json, tsconfig.node.json, vite.config.ts, src/main.tsx, src/web/app-shell.tsx, src/web/app-shell.test.tsx</files>
  <read_first>package.json, src/ui/connection/connect-spotify-card.tsx, src/ui/lyrics/live-lyrics-panel.tsx, .planning/phases/05-web-runtime-and-theme-foundation/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: App shell renders a header plus both connection and lyrics pane placeholders in a browser test environment.
    - Test 2: Shell does not import Node-only modules (`node:fs`, `electron`) from web entry files.
    - Test 3: Existing connection/lyrics model boundaries remain consumable by the new shell without changing their contracts.
  </behavior>
  <action>Update `package.json` scripts for web runtime (`dev`, `build`, `preview`, `test`) and add dependencies required for React + Vite + Testing Library execution. Add `index.html`, `tsconfig.json`, `tsconfig.node.json`, and `vite.config.ts` for a standard Vite TypeScript React app. Create `src/main.tsx` mounting `<AppShell />`. Create `src/web/app-shell.tsx` with a header and two primary regions: connection/status pane and lyrics pane. On large screens use a two-column layout; on smaller screens stack sections vertically. Render explicit placeholder/state text in both panes when no live data is connected. Add `src/web/app-shell.test.tsx` to assert the shell header and both pane placeholders render.</action>
  <acceptance_criteria>
    - `package.json` contains scripts `dev`, `build`, and `preview`
    - `src/main.tsx` contains `createRoot` and renders `AppShell`
    - `src/web/app-shell.tsx` contains both connection and lyrics section headings/placeholders
    - `src/web/app-shell.test.tsx` asserts both pane placeholders render
    - `npm test -- src/web/app-shell.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/app-shell.test.tsx</automated>
  </verify>
  <done>Browser shell is executable and verifiably renders core Phase 5 structure with no desktop runtime dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Add Tailwind baseline and global token stylesheet</name>
  <files>postcss.config.cjs, tailwind.config.ts, src/styles/globals.css, src/main.tsx</files>
  <read_first>src/main.tsx, .planning/phases/05-web-runtime-and-theme-foundation/05-RESEARCH.md, .planning/research/STACK.md</read_first>
  <action>Create `postcss.config.cjs` and `tailwind.config.ts` with content globs covering `index.html`, `src/**/*.{ts,tsx}` and dark mode set to class strategy. Add `src/styles/globals.css` with Tailwind base/components/utilities directives plus baseline light/dark CSS variables used by shell surfaces. Import `src/styles/globals.css` from `src/main.tsx` so styles load globally before shell render.</action>
  <acceptance_criteria>
    - `tailwind.config.ts` contains `darkMode: ["class"]`
    - `tailwind.config.ts` contains `content` globs for `index.html` and `src/**/*.{ts,tsx}`
    - `src/styles/globals.css` contains `@tailwind base;`, `@tailwind components;`, and `@tailwind utilities;`
    - `src/main.tsx` imports `./styles/globals.css`
    - `npm test -- src/web/app-shell.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/app-shell.test.tsx</automated>
  </verify>
  <done>Web scaffold includes Tailwind/token baseline required for shadcn initialization and theme work.</done>
</task>

</tasks>

<verification>
Run shell baseline checks:
- `npm test -- src/web/app-shell.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- WEB-01 runtime foundation exists and can render shell in browser test/build paths.
- Tailwind class-based dark-mode baseline is present before shadcn installation work.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-runtime-and-theme-foundation/05-01-SUMMARY.md`
</output>
