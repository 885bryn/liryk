---
phase: 14-timing-diagnostics-and-early-cueing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/playback/live-sync-store.ts
  - src/state/playback/live-sync-store.test.ts
  - src/app/live-sync-runtime.ts
  - src/app/live-sync-runtime.test.ts
  - src/web/fullscreen-lyrics-page.tsx
  - src/web/fullscreen-lyrics-page.test.tsx
autonomous: true
requirements:
  - DBG-01
must_haves:
  truths:
    - User can toggle a diagnostics overlay on fullscreen lyrics without leaving playback.
    - Overlay shows estimated progress, polled progress, drift delta, and correction/confidence state for the current sample.
    - Diagnostics values update during playback and reset safely on idle/track clear.
  artifacts:
    - path: src/state/playback/live-sync-store.ts
      provides: Runtime diagnostics fields persisted in UI sync state.
    - path: src/app/live-sync-runtime.ts
      provides: Population of diagnostics values from frame estimate and poll snapshot inputs.
    - path: src/web/fullscreen-lyrics-page.tsx
      provides: Toggleable fullscreen diagnostics overlay with structured timing fields.
  key_links:
    - from: src/app/live-sync-runtime.ts
      to: src/state/playback/live-sync-store.ts
      via: store setters for estimated/polled/drift/correction diagnostics
      pattern: "setEstimatedProgressMs|setPolledProgressMs|setDriftDeltaMs|setCorrectionState"
    - from: src/web/fullscreen-lyrics-page.tsx
      to: src/state/playback/live-sync-store.ts
      via: fullscreen diagnostics overlay rendering from live sync diagnostics state
      pattern: "diagnostics|estimatedProgressMs|polledProgressMs|driftDeltaMs"
---

<objective>
Add a minimal toggleable diagnostics overlay that exposes timing internals needed to debug drift behavior in fullscreen playback.

Purpose: Deliver DBG-01 with deterministic, inspectable timing fields before any cueing behavior is introduced.
Output: Runtime diagnostics state wiring plus fullscreen overlay and regression coverage.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/state/playback/live-sync-store.ts
@src/app/live-sync-runtime.ts
@src/web/fullscreen-lyrics-page.tsx

<interfaces>
From src/state/playback/live-sync-store.ts:
```typescript
export type LiveSyncUiState = {
  playbackState: PlaybackUiState;
  activeLineIndex: number | null;
  nextLineIndex: number | null;
  confidence: SyncConfidence | "static";
  trackId: string | null;
  statusLine: string;
  estimatedProgressMs: number;
};
```

From src/core/sync/lyric-sync-engine.ts:
```typescript
export type SyncFrame = {
  activeLineIndex: number | null;
  nextLineIndex: number | null;
  confidence: SyncConfidence;
  progressMs: number;
  isPlaying: boolean;
  trackId: string | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add diagnostics state contract and runtime population</name>
  <files>src/state/playback/live-sync-store.ts, src/state/playback/live-sync-store.test.ts, src/app/live-sync-runtime.ts, src/app/live-sync-runtime.test.ts</files>
  <behavior>
    - Test 1: Runtime stores both estimated progress and latest polled progress on each trusted playback sample.
    - Test 2: Runtime stores drift delta (`estimated - polled`) plus correction/confidence state for diagnostics.
    - Test 3: Diagnostics fields reset to safe defaults on idle snapshot and track clear.
  </behavior>
  <action>Extend `LiveSyncUiState` with explicit diagnostics fields (`polledProgressMs`, `driftDeltaMs`, `correctionState`) and add store setters/reset behavior. In runtime snapshot handling, compute diagnostics from engine frame output and trusted snapshot progress (no ad hoc UI math) and write through store methods only. Keep stale-snapshot guards and existing playback state semantics unchanged.</action>
  <verify>
    <automated>npm run test -- src/state/playback/live-sync-store.test.ts src/app/live-sync-runtime.test.ts</automated>
  </verify>
  <done>Diagnostics fields are available in store state, updated by runtime, and covered by deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render fullscreen diagnostics overlay with explicit toggle and field labels</name>
  <files>src/web/fullscreen-lyrics-page.tsx, src/web/fullscreen-lyrics-page.test.tsx</files>
  <behavior>
    - Toggle control shows and hides diagnostics overlay without affecting lyric rendering.
    - Overlay shows estimated ms, polled ms, drift delta ms, and correction state labels.
    - Overlay values update from live diagnostics state and do not crash when playback is idle.
  </behavior>
  <action>Add a minimal diagnostics toggle (keyboard-accessible button) to fullscreen and render a compact overlay panel bound to diagnostics fields. Keep existing lyric-first hierarchy (overlay stays secondary and subdued), and avoid introducing additional shell chrome or route changes. Add regression tests for toggle behavior, required labels/values, and non-playing fallback display.</action>
  <verify>
    <automated>npm run test -- src/web/fullscreen-lyrics-page.test.tsx && npm run build</automated>
  </verify>
  <done>Fullscreen exposes a toggleable diagnostics panel with required timing fields and passing regression/build gates.</done>
</task>

</tasks>

<verification>
Run `npm run test -- src/state/playback/live-sync-store.test.ts src/app/live-sync-runtime.test.ts src/web/fullscreen-lyrics-page.test.tsx && npm run build` and confirm diagnostics fields and overlay toggle contracts pass.
</verification>

<success_criteria>
DBG-01 is met: users can toggle a diagnostics overlay during fullscreen playback and inspect estimated vs polled timing plus drift/correction state from the same runtime sample.
</success_criteria>

<output>
After completion, create `.planning/phases/14-timing-diagnostics-and-early-cueing/14-01-SUMMARY.md`
</output>
