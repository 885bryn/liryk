---
phase: 14-timing-diagnostics-and-early-cueing
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/core/sync/early-cue.ts
  - src/core/sync/early-cue.test.ts
  - src/web/fullscreen-lyrics-page.tsx
  - src/web/fullscreen-lyrics-page.test.tsx
autonomous: true
requirements:
  - CUE-01
must_haves:
  truths:
    - Active lyric highlighting starts slightly early with a conservative lead value during synced playback.
    - Early cueing never reorders lines or creates multiple active lines.
    - Cueing behavior is configurable and can be tuned without rewriting fullscreen rendering logic.
  artifacts:
    - path: src/core/sync/early-cue.ts
      provides: Pure configurable early-cue progress utility.
    - path: src/core/sync/early-cue.test.ts
      provides: Deterministic edge-case coverage for cue lead and ordering safety.
    - path: src/web/fullscreen-lyrics-page.tsx
      provides: Fullscreen synced-line selection wired to early-cue utility.
  key_links:
    - from: src/web/fullscreen-lyrics-page.tsx
      to: src/core/sync/early-cue.ts
      via: active-line progress calculation uses cue-adjusted progress
      pattern: "applyEarlyCue|cueLeadMs|activeSyncedIndex"
    - from: src/core/sync/early-cue.test.ts
      to: src/web/fullscreen-lyrics-page.test.tsx
      via: shared ordering and boundary expectations
      pattern: "before-first|ordering|single active"
---

<objective>
Introduce conservative early cueing after diagnostics baseline is verified, without destabilizing line order or readability.

Purpose: Deliver CUE-01 by adding a small lead offset that improves perceived lyric readiness while preserving deterministic progression.
Output: Pure cueing utility, fullscreen wiring, and regression coverage for ordering safety.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-timing-diagnostics-and-early-cueing/14-02-SUMMARY.md
@src/web/fullscreen-lyrics-page.tsx
@src/web/fullscreen-lyrics-page.test.tsx

<interfaces>
From src/core/sync/lyric-timeline.ts:
```typescript
export type LyricLine = {
  startMs: number;
  text: string;
};
```

From src/web/fullscreen-lyrics-page.tsx:
```typescript
const syncedDisplayLines = syncedLines.map((line) => ({
  text: line.displayText ?? normalizeChineseForDisplay(line.text),
  startMs: line.startMs ?? 0,
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure early-cue utility with deterministic boundary tests</name>
  <files>src/core/sync/early-cue.ts, src/core/sync/early-cue.test.ts</files>
  <behavior>
    - Test 1: Cue utility applies configured lead ms and clamps at zero for low progress.
    - Test 2: Zero/disabled lead keeps progress unchanged.
    - Test 3: Utility preserves monotonic progression expectations for sequential samples.
  </behavior>
  <action>Implement a pure helper that transforms playback progress into cue-adjusted progress using a conservative lead constant (small default, explicitly exported for tuning). Keep it dependency-free and deterministic so fullscreen and future runtime consumers can share behavior. Add unit tests first, then implementation.</action>
  <verify>
    <automated>npm run test -- src/core/sync/early-cue.test.ts</automated>
  </verify>
  <done>Reusable cue utility exists with deterministic tests covering clamp and lead semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire conservative early cueing into fullscreen synced-line activation</name>
  <files>src/web/fullscreen-lyrics-page.tsx, src/web/fullscreen-lyrics-page.test.tsx</files>
  <behavior>
    - Active lyric can lead slightly ahead of raw progress when synced lyrics are present.
    - Pre-first-line behavior stays deterministic (no active line before cue-adjusted threshold).
    - Rendered hierarchy still has exactly one active tier and stable near/distant ordering.
  </behavior>
  <action>Use the new cue utility in fullscreen active-index derivation only for synced mode. Keep all existing tier rendering rules and accessibility/motion contracts intact. Add regression tests that prove early lead behavior on boundary timestamps while preserving single-active-line and ordering invariants from prior phases.</action>
  <verify>
    <automated>npm run test -- src/core/sync/early-cue.test.ts src/web/fullscreen-lyrics-page.test.tsx && npm run build</automated>
  </verify>
  <done>Fullscreen applies conservative early cueing with deterministic ordering and all regression/build gates passing.</done>
</task>

</tasks>

<verification>
Run `npm run test -- src/core/sync/early-cue.test.ts src/web/fullscreen-lyrics-page.test.tsx && npm run build` and verify cue lead behavior appears in boundary tests without ordering regressions.
</verification>

<success_criteria>
CUE-01 is met: synced lyric highlighting leads slightly and consistently, while line ordering, readability tiers, and deterministic boundary behavior remain stable.
</success_criteria>

<output>
After completion, create `.planning/phases/14-timing-diagnostics-and-early-cueing/14-03-SUMMARY.md`
</output>
