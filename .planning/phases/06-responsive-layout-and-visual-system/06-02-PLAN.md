---
phase: 06-responsive-layout-and-visual-system
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/app-shell.tsx
  - src/web/app-shell.test.tsx
autonomous: true
requirements:
  - WEB-02
  - UI-03
must_haves:
  truths:
    - User gets a stable stacked layout on mobile/tablet and an emphasized lyrics-first split on desktop.
    - User sees lyric content visually prioritized over connection/status content.
    - User sees consistent spacing rhythm with denser desktop spacing and stepped-down mobile spacing.
  artifacts:
    - path: src/web/app-shell.tsx
      provides: Responsive shell composition and lyrics-first pane ordering.
    - path: src/web/app-shell.test.tsx
      provides: Automated contract checks for responsive classes, pane order, and hierarchy markers.
  key_links:
    - from: src/web/app-shell.tsx
      to: src/web/app-shell.test.tsx
      via: class/markup assertions for responsive contract
      pattern: grid-cols-1|lg:grid-cols-5|aria-label
---

<objective>
Implement the responsive shell rhythm and typography hierarchy decisions in the primary web composition.

Purpose: Deliver WEB-02 and UI-03 behavior directly in the shell users interact with first, while preserving existing theme toggle and account menu behavior.
Output: Updated app shell markup/classes plus tests that lock responsive ordering and hierarchy expectations.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-responsive-layout-and-visual-system/06-CONTEXT.md
@.planning/phases/06-responsive-layout-and-visual-system/06-RESEARCH.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-03-SUMMARY.md

<interfaces>
From `src/web/app-shell.tsx`:
```typescript
type AppShellProps = {
  isConnected?: boolean;
  accountName?: string;
  onDisconnect?: () => Promise<void>;
};

export function AppShell(input?: AppShellProps): JSX.Element;
```

From `src/web/theme/theme-toggle.tsx`:
```typescript
export function ThemeToggle(input: {
  mode: ThemeMode;
  onToggle: () => void;
  label?: string;
  className?: string;
}): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce responsive pane ordering and 40/60 desktop emphasis</name>
  <files>src/web/app-shell.tsx, src/web/app-shell.test.tsx</files>
  <read_first>src/web/app-shell.tsx, src/web/app-shell.test.tsx, .planning/phases/06-responsive-layout-and-visual-system/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Shell layout keeps `grid-cols-1` baseline and uses `lg:grid-cols-5` with connection `lg:col-span-2` and lyrics `lg:col-span-3`.
    - Test 2: In stacked/mobile DOM order, lyrics pane is rendered before connection pane.
    - Test 3: Existing theme toggle and connected account menu behavior remain available.
  </behavior>
  <action>Update `src/web/app-shell.tsx` main layout classes to `grid-cols-1` through tablet and switch at desktop to `lg:grid-cols-5`. Set lyrics pane wrapper classes to `lg:col-span-3` and connection pane wrapper classes to `lg:col-span-2` to realize the approximately 40/60 emphasis. Render lyrics pane markup before connection pane markup so stacked mobile flow places lyrics first. Keep header `ThemeToggle` and connected dropdown `ThemeToggle` placements unchanged in behavior.</action>
  <acceptance_criteria>
    - `src/web/app-shell.tsx` contains `lg:grid-cols-5`
    - `src/web/app-shell.tsx` contains both `lg:col-span-3` (lyrics) and `lg:col-span-2` (connection)
    - `src/web/app-shell.tsx` renders the lyrics pane block before the connection pane block
    - `npm test -- src/web/app-shell.test.tsx -t "split"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/app-shell.test.tsx</automated>
  </verify>
  <done>Shell responsive structure matches locked layout decisions for desktop emphasis and mobile ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply moderate typography and spacing hierarchy to shell surfaces</name>
  <files>src/web/app-shell.tsx, src/web/app-shell.test.tsx</files>
  <read_first>src/web/app-shell.tsx, src/web/app-shell.test.tsx, src/styles/globals.css</read_first>
  <behavior>
    - Test 1: App title and pane headings use explicit hierarchy classes stronger than body/supporting text.
    - Test 2: Status/placeholder body text keeps readable but quiet styling via muted classes.
    - Test 3: Main/pane spacing uses stepped rhythm (`gap-4` mobile with larger desktop expansion).
  </behavior>
  <action>In `src/web/app-shell.tsx`, set header title class to `text-xl font-semibold tracking-tight sm:text-2xl`. Set pane titles to moderate contrast classes such as `text-lg font-medium`. Keep status body text as `text-sm text-muted-foreground leading-relaxed`. Apply spacing rhythm with mobile baseline `gap-4 px-4 py-5` and desktop expansion `sm:gap-5 sm:px-6 sm:py-6 lg:gap-6 lg:py-8`. Update `src/web/app-shell.test.tsx` to assert these concrete class markers are present on shell layout and typography nodes.</action>
  <acceptance_criteria>
    - `src/web/app-shell.tsx` header title contains `tracking-tight` and `sm:text-2xl`
    - `src/web/app-shell.tsx` pane title classes include `text-lg` and `font-medium`
    - `src/web/app-shell.tsx` placeholder/status text contains `text-muted-foreground` and `leading-relaxed`
    - `src/web/app-shell.tsx` main layout classes include `gap-4` and `lg:gap-6`
    - `npm test -- src/web/app-shell.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/app-shell.test.tsx</automated>
  </verify>
  <done>Shell typography and spacing show intentional hierarchy aligned with Phase 6 decisions.</done>
</task>

</tasks>

<verification>
Run responsive shell checks:
- `npm test -- src/web/app-shell.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- WEB-02 is enforced by test-covered responsive class markers and mobile/desktop pane behavior.
- UI-03 hierarchy is encoded through explicit shell typography and spacing classes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-responsive-layout-and-visual-system/06-02-SUMMARY.md`
</output>
