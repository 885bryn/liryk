---
phase: 06-responsive-layout-and-visual-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/globals.css
  - tailwind.config.ts
autonomous: true
requirements:
  - THEM-03
  - UI-03
must_haves:
  truths:
    - User sees readable contrast and cohesive visual tone in both light and dark themes for shell backgrounds and card surfaces.
    - User sees clear text hierarchy (title/body/supporting text) without introducing a second font family.
  artifacts:
    - path: src/styles/globals.css
      provides: Canonical light/dark theme tokens and typography baseline.
    - path: tailwind.config.ts
      provides: Tailwind token mapping used by shell and shadcn surfaces.
  key_links:
    - from: src/styles/globals.css
      to: tailwind.config.ts
      via: CSS variable names consumed by Tailwind color keys
      pattern: --background|--foreground|colors:
---

<objective>
Refine the theme token and typography foundation used by all Phase 6 surfaces.

Purpose: Deliver THEM-03 contrast consistency and UI-03 hierarchy baseline before layout-level composition changes.
Output: Updated global token set and Tailwind mappings that preserve current utility usage and improve readability in both themes.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-responsive-layout-and-visual-system/06-CONTEXT.md
@.planning/phases/06-responsive-layout-and-visual-system/06-RESEARCH.md
@.planning/phases/06-responsive-layout-and-visual-system/06-VALIDATION.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-03-SUMMARY.md

<interfaces>
From `tailwind.config.ts`:
```typescript
const config: Config = {
  darkMode: ["class"],
  theme: {
    extend: {
      colors: {
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        card: "hsl(var(--card))",
        muted: "hsl(var(--muted))",
        accent: "hsl(var(--accent))",
      },
    },
  },
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tune light/dark token pairs for readable shell and card contrast</name>
  <files>src/styles/globals.css</files>
  <read_first>src/styles/globals.css, .planning/phases/06-responsive-layout-and-visual-system/06-CONTEXT.md, .planning/phases/06-responsive-layout-and-visual-system/06-RESEARCH.md</read_first>
  <action>Update light and dark variable values in `src/styles/globals.css` for `--background`, `--foreground`, `--card`, `--card-foreground`, `--muted`, `--muted-foreground`, `--accent`, `--accent-foreground`, `--border`, and `--ring` so the shell and card surfaces have stronger readable contrast in both themes. Keep the single-family typography decision by preserving `font-family: "IBM Plex Sans", "Segoe UI", sans-serif;` in `body`. Add explicit body typography rhythm values with concrete defaults: `font-size: 16px`, `line-height: 1.5`, and `letter-spacing: 0.01em`.</action>
  <acceptance_criteria>
    - `src/styles/globals.css` contains both `:root` and `.dark` token blocks with all keys `--background`, `--foreground`, `--card`, `--card-foreground`, `--muted`, `--muted-foreground`, `--accent`, `--accent-foreground`, `--border`, and `--ring`
    - `src/styles/globals.css` `body` block contains `font-family: "IBM Plex Sans"`
    - `src/styles/globals.css` `body` block contains `font-size: 16px`, `line-height: 1.5`, and `letter-spacing: 0.01em`
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Theme token values and body typography baseline satisfy THEM-03/UI-03 constraints without changing runtime logic.</done>
</task>

<task type="auto">
  <name>Task 2: Keep Tailwind token mapping aligned with refined global tokens</name>
  <files>tailwind.config.ts</files>
  <read_first>tailwind.config.ts, src/styles/globals.css</read_first>
  <action>Ensure `tailwind.config.ts` still maps shell-relevant semantic color keys (`background`, `foreground`, `card`, `card-foreground`, `muted`, `muted-foreground`, `accent`, `accent-foreground`, `border`, `ring`) to `hsl(var(--...))` values and does not introduce hardcoded hex values. Keep `darkMode: ["class"]` and existing content globs intact.</action>
  <acceptance_criteria>
    - `tailwind.config.ts` contains `darkMode: ["class"]`
    - `tailwind.config.ts` contains color mappings for `background`, `foreground`, `card`, `card-foreground`, `muted`, `muted-foreground`, `accent`, `accent-foreground`, `border`, and `ring`
    - `tailwind.config.ts` contains no `#` hex literals in `extend.colors`
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Token utility mappings remain coherent with the updated global theme contract.</done>
</task>

</tasks>

<verification>
Run foundational style checks:
- `npm run build`
</verification>

<success_criteria>
- THEM-03 token consistency baseline is implemented in global CSS + Tailwind mapping.
- UI-03 typography baseline (single-family, readable body rhythm) is explicitly encoded.
</success_criteria>

<output>
After completion, create `.planning/phases/06-responsive-layout-and-visual-system/06-01-SUMMARY.md`
</output>
