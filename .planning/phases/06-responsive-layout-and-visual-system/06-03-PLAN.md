---
phase: 06-responsive-layout-and-visual-system
plan: "03"
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/components/ui/card.tsx
  - src/web/visual-system.test.tsx
  - .planning/phases/06-responsive-layout-and-visual-system/06-VISUAL-CHECKPOINT.md
autonomous: true
requirements:
  - THEM-03
  - WEB-02
  - UI-03
must_haves:
  truths:
    - User sees consistent card and navigation surface treatment across themes.
    - User gets automated proof that responsive and visual-system contracts remain intact after refinement.
    - User can verify the phase quickly using one reproducible checklist.
  artifacts:
    - path: src/components/ui/card.tsx
      provides: Consistent card surface classes aligned to updated token strategy.
    - path: src/web/visual-system.test.tsx
      provides: Focused visual contract tests for theme and responsive markers.
    - path: .planning/phases/06-responsive-layout-and-visual-system/06-VISUAL-CHECKPOINT.md
      provides: Reproducible validation checklist for this phase.
  key_links:
    - from: src/components/ui/card.tsx
      to: src/web/app-shell.tsx
      via: card primitives rendering shell panes
      pattern: data-slot="card"|CardTitle
    - from: src/web/visual-system.test.tsx
      to: src/web/app-shell.tsx
      via: rendered shell class assertions
      pattern: render\(<AppShell
---

<objective>
Finalize visual-system consistency and add explicit verification artifacts for the responsive shell refinements.

Purpose: Prevent regressions after token/layout changes by locking card-surface styling and shipping a dedicated visual checkpoint.
Output: Card primitive polish, targeted visual contract tests, and a phase-local checkpoint markdown.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-responsive-layout-and-visual-system/06-CONTEXT.md
@.planning/phases/06-responsive-layout-and-visual-system/06-RESEARCH.md
@.planning/phases/06-responsive-layout-and-visual-system/06-VALIDATION.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-02-SUMMARY.md
@.planning/phases/05-web-runtime-and-theme-foundation/05-03-SUMMARY.md

<interfaces>
From `src/components/ui/card.tsx`:
```typescript
function Card(props: React.ComponentProps<"div"> & { size?: "default" | "sm" }): JSX.Element;
function CardTitle(props: React.ComponentProps<"h3">): JSX.Element;
export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent };
```

From `src/web/app-shell.tsx`:
```typescript
export function AppShell(input?: AppShellProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden card surface styling for readable theme-consistent panes</name>
  <files>src/components/ui/card.tsx, src/web/visual-system.test.tsx</files>
  <read_first>src/components/ui/card.tsx, src/web/app-shell.tsx, src/styles/globals.css</read_first>
  <behavior>
    - Test 1: Card root includes token-driven background/text/ring classes used by shell panes.
    - Test 2: Card title keeps heading semantics and hierarchy-compatible typography class markers.
  </behavior>
  <action>Update `src/components/ui/card.tsx` base class strings to keep token-driven readability with explicit classes `bg-card`, `text-card-foreground`, `ring-1`, and `ring-border/60` on the root card element. Keep `CardTitle` rendered as `h3` and ensure title class includes `font-medium` and `leading-snug`. Add `src/web/visual-system.test.tsx` with tests that render `<AppShell />` and assert pane wrappers still contain card heading roles and token-driven card classes.</action>
  <acceptance_criteria>
    - `src/components/ui/card.tsx` root class string contains `bg-card`, `text-card-foreground`, and `ring-border/60`
    - `src/components/ui/card.tsx` keeps `CardTitle` as `h3`
    - `src/web/visual-system.test.tsx` imports and renders `AppShell`
    - `npm test -- src/web/visual-system.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/visual-system.test.tsx</automated>
  </verify>
  <done>Shell card primitives enforce consistent readable surface treatment and are locked by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create reproducible visual-system checkpoint for phase verification</name>
  <files>.planning/phases/06-responsive-layout-and-visual-system/06-VISUAL-CHECKPOINT.md</files>
  <read_first>.planning/phases/06-responsive-layout-and-visual-system/06-VALIDATION.md, .planning/phases/06-responsive-layout-and-visual-system/06-RESEARCH.md</read_first>
  <action>Create `.planning/phases/06-responsive-layout-and-visual-system/06-VISUAL-CHECKPOINT.md` with three sections: `## Automated Checks`, `## Manual Viewport Checks`, and `## Requirement Traceability`. Under automated checks include exact commands `npm test -- src/web/app-shell.test.tsx src/web/visual-system.test.tsx` and `npm run build`. Under manual checks include viewport sizes `390x844` and `1280x800`, with required observations: mobile stacked lyrics-first flow, desktop 40/60 emphasis, and readable muted/supporting text in both themes. Under traceability map WEB-02, THEM-03, UI-03 to the exact files and commands that verify each requirement.</action>
  <acceptance_criteria>
    - `.planning/phases/06-responsive-layout-and-visual-system/06-VISUAL-CHECKPOINT.md` contains heading `## Automated Checks`
    - `.planning/phases/06-responsive-layout-and-visual-system/06-VISUAL-CHECKPOINT.md` contains command `npm test -- src/web/app-shell.test.tsx src/web/visual-system.test.tsx`
    - `.planning/phases/06-responsive-layout-and-visual-system/06-VISUAL-CHECKPOINT.md` contains `390x844` and `1280x800`
    - `.planning/phases/06-responsive-layout-and-visual-system/06-VISUAL-CHECKPOINT.md` contains requirement IDs `WEB-02`, `THEM-03`, and `UI-03`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/app-shell.test.tsx src/web/visual-system.test.tsx && npm run build</automated>
  </verify>
  <done>Phase verification can be repeated quickly with explicit automated and manual checks tied to requirements.</done>
</task>

</tasks>

<verification>
Run final visual-system checks:
- `npm test -- src/web/app-shell.test.tsx src/web/visual-system.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- WEB-02, THEM-03, and UI-03 all have explicit verification hooks and reproducible checkpoint guidance.
- Card and shell visual contracts are protected by automated tests after layout/token refinements.
</success_criteria>

<output>
After completion, create `.planning/phases/06-responsive-layout-and-visual-system/06-03-SUMMARY.md`
</output>
