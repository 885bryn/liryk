---
phase: 15-hold-and-transition-motion-windowing
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/web/fullscreen-lyrics-page.tsx
  - src/web/fullscreen-lyrics-page.test.tsx
autonomous: true
requirements:
  - MOT-04
  - MOT-05
must_haves:
  truths:
    - Active lyric remains visually anchored during most of each line interval.
    - Vertical lyric motion starts only inside a pre-change window near the next line start.
    - Motion phase calculation does not break existing single-active-line hierarchy behavior.
  artifacts:
    - path: src/web/fullscreen-lyrics-page.tsx
      provides: Fullscreen hold-then-transition offset behavior using motion-window helpers.
    - path: src/web/fullscreen-lyrics-page.test.tsx
      provides: Regression coverage for hold and transition-only motion timing.
  key_links:
    - from: src/web/fullscreen-lyrics-page.tsx
      to: src/core/sync/lyric-motion-window.ts
      via: track translateY derived from `getTransitionPhase` phase and progress
      pattern: "getTransitionPhase|transitionStartMs|phaseProgress"
    - from: src/web/fullscreen-lyrics-page.test.tsx
      to: src/web/fullscreen-lyrics-page.tsx
      via: tests assert hold offset before transition window and movement inside window
      pattern: "hold|transition window|translateY"
---

<objective>
Refactor fullscreen lyric track motion so lines hold steady during reading and animate only near line-change boundaries.

Purpose: Deliver MOT-04 and MOT-05 readability behavior by replacing index-snap offset with windowed interpolation.
Output: Fullscreen offset interpolation wiring and deterministic tests for hold-before-window and transition-in-window behavior.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hold-and-transition-motion-windowing/15-01-SUMMARY.md
@src/web/fullscreen-lyrics-page.tsx
@src/web/fullscreen-lyrics-page.test.tsx

<interfaces>
From src/core/sync/lyric-motion-window.ts (from Plan 15-01):
```typescript
export type TransitionPhase = "hold" | "transition" | "complete";
export function getAdaptiveTransitionMs(...): number;
export function getTransitionPhase(input: {
  progressMs: number;
  currentStartMs: number;
  nextStartMs: number;
  minTransitionMs?: number;
  maxTransitionMs?: number;
  transitionFraction?: number;
}): { phase: TransitionPhase; phaseProgress: number; transitionStartMs: number; transitionDurationMs: number };
```

From src/web/fullscreen-lyrics-page.tsx:
```typescript
const SYNC_LINE_STEP_PX = 88;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fullscreen motion-window behavior tests before refactor</name>
  <files>src/web/fullscreen-lyrics-page.test.tsx</files>
  <read_first>src/web/fullscreen-lyrics-page.test.tsx, src/web/fullscreen-lyrics-page.tsx, src/core/sync/lyric-motion-window.ts</read_first>
  <behavior>
    - Test 1: For progress far from next line boundary, track translateY stays locked to current active line offset.
    - Test 2: For progress inside transition window, track translateY is between current and next line offsets.
    - Test 3: At/after next line start, track translateY lands exactly on next line offset with no overshoot.
  </behavior>
  <action>Add deterministic test fixtures using synced lines with known starts (for example 0ms, 4000ms, 9000ms) and progress snapshots that sample hold, transition, and boundary-complete moments. Assert `data-testid="fullscreen-lyrics-track"` transform values for each case, keeping existing hierarchy and cue tests unchanged.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.test.tsx` contains at least one test mentioning "hold" and one mentioning "transition window".
    - Added tests assert `track.style.transform` for three states: hold, transition, and complete.
    - `npm run test -- src/web/fullscreen-lyrics-page.test.tsx` fails before Task 2 implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/web/fullscreen-lyrics-page.test.tsx</automated>
  </verify>
  <done>Regression tests lock expected motion-window behavior in fullscreen before implementation changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hold-then-transition offset interpolation in fullscreen page</name>
  <files>src/web/fullscreen-lyrics-page.tsx, src/web/fullscreen-lyrics-page.test.tsx</files>
  <read_first>src/web/fullscreen-lyrics-page.tsx, src/core/sync/lyric-motion-window.ts, src/web/fullscreen-lyrics-page.test.tsx</read_first>
  <behavior>
    - Before transition window start, offset equals `-activeIndex * SYNC_LINE_STEP_PX`.
    - Inside transition window, offset interpolates from active index to active+1 using phase progress 0..1.
    - Motion remains monotonic toward next line and preserves one active lyric line contract.
  </behavior>
  <action>Import `getTransitionPhase` into fullscreen page and replace direct `activeSyncedRenderIndex` transform with a computed floating index. For synced mode with a valid next line, compute phase from current line start, next line start, and current progress; map hold -> current index, transition -> `currentIndex + phaseProgress`, complete -> `nextIndex`. Convert floating index to `translateY(-floatingIndex * SYNC_LINE_STEP_PX)`; keep non-synced and pre-first behavior unchanged.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.tsx` imports `getTransitionPhase` from `@/core/sync/lyric-motion-window`.
    - `src/web/fullscreen-lyrics-page.tsx` contains interpolation logic using `phaseProgress` and `SYNC_LINE_STEP_PX`.
    - `npm run test -- src/web/fullscreen-lyrics-page.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/web/fullscreen-lyrics-page.test.tsx</automated>
  </verify>
  <done>Fullscreen track offset now holds then transitions near line boundaries with deterministic regression coverage.</done>
</task>

</tasks>

<verification>
Run `npm run test -- src/web/fullscreen-lyrics-page.test.tsx` and confirm new hold/transition assertions pass with existing fullscreen regressions.
</verification>

<success_criteria>
MOT-04 and MOT-05 are met in fullscreen: no continuous drift during line reading and motion starts only in a pre-change transition window.
</success_criteria>

<output>
After completion, create `.planning/phases/15-hold-and-transition-motion-windowing/15-02-SUMMARY.md`
</output>
