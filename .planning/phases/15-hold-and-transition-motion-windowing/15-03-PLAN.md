---
phase: 15-hold-and-transition-motion-windowing
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - src/core/sync/lyric-motion-window.ts
  - src/core/sync/lyric-motion-window.test.ts
  - src/web/fullscreen-lyrics-page.tsx
  - src/web/fullscreen-lyrics-page.test.tsx
autonomous: true
requirements:
  - TRN-01
must_haves:
  truths:
    - Transition timing uses explicit exported defaults with readable min/max clamping.
    - Short and long line gaps produce stable, non-jarring motion behavior.
    - Fullscreen motion wiring uses centralized constants rather than inline magic numbers.
  artifacts:
    - path: src/core/sync/lyric-motion-window.ts
      provides: Exported motion-window constants and tunable clamp defaults.
    - path: src/core/sync/lyric-motion-window.test.ts
      provides: Clamp verification for short and long gap edge cases.
    - path: src/web/fullscreen-lyrics-page.tsx
      provides: Fullscreen consumption of centralized motion constants.
    - path: src/web/fullscreen-lyrics-page.test.tsx
      provides: UI-level regression checks for readable clamp behavior.
  key_links:
    - from: src/web/fullscreen-lyrics-page.tsx
      to: src/core/sync/lyric-motion-window.ts
      via: imports exported defaults for transition window tuning
      pattern: "DEFAULT_.*TRANSITION|getTransitionPhase"
    - from: src/core/sync/lyric-motion-window.test.ts
      to: src/web/fullscreen-lyrics-page.test.tsx
      via: shared expectations for min/max clamped motion timing
      pattern: "min|max|clamp|gap"
---

<objective>
Finalize transition-window tuning by exposing motion constants and locking readable clamp behavior through deterministic tests.

Purpose: Complete TRN-01 with transparent, reusable defaults so future phases can adjust motion timing without rewriting logic.
Output: Exported tuning constants, updated fullscreen wiring, and clamp-focused verification coverage including build gate.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hold-and-transition-motion-windowing/15-RESEARCH.md
@.planning/phases/15-hold-and-transition-motion-windowing/15-02-SUMMARY.md
@src/core/sync/lyric-motion-window.ts
@src/web/fullscreen-lyrics-page.tsx

<interfaces>
From src/core/sync/lyric-motion-window.ts:
```typescript
export function getAdaptiveTransitionMs(...): number;
export function getTransitionPhase(...): { phase: "hold" | "transition" | "complete"; phaseProgress: number; transitionStartMs: number; transitionDurationMs: number };
```

Constants to expose in this plan:
```typescript
export const DEFAULT_TRANSITION_WINDOW_FRACTION = 0.42;
export const DEFAULT_MIN_TRANSITION_MS = 220;
export const DEFAULT_MAX_TRANSITION_MS = 520;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export motion tuning constants and lock clamp edge-case tests</name>
  <files>src/core/sync/lyric-motion-window.ts, src/core/sync/lyric-motion-window.test.ts</files>
  <read_first>src/core/sync/lyric-motion-window.ts, src/core/sync/lyric-motion-window.test.ts</read_first>
  <behavior>
    - Very short gaps clamp to `DEFAULT_MIN_TRANSITION_MS`.
    - Very long gaps clamp to `DEFAULT_MAX_TRANSITION_MS`.
    - Mid-range gaps use fraction-based adaptive duration without exceeding bounds.
  </behavior>
  <action>Promote transition tuning defaults to named exported constants: `DEFAULT_TRANSITION_WINDOW_FRACTION`, `DEFAULT_MIN_TRANSITION_MS`, and `DEFAULT_MAX_TRANSITION_MS`. Update helper signatures to use these exports as defaults and add/adjust tests that assert exact values for short-gap clamp, long-gap clamp, and representative mid-gap adaptation.</action>
  <acceptance_criteria>
    - `src/core/sync/lyric-motion-window.ts` exports all three `DEFAULT_*` constants.
    - `src/core/sync/lyric-motion-window.test.ts` includes clamp tests for both short and long gaps.
    - `npm run test -- src/core/sync/lyric-motion-window.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/core/sync/lyric-motion-window.test.ts</automated>
  </verify>
  <done>Motion timing constants are centralized and protected by deterministic clamp tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire exported defaults in fullscreen and prove clamp readability behavior</name>
  <files>src/web/fullscreen-lyrics-page.tsx, src/web/fullscreen-lyrics-page.test.tsx</files>
  <read_first>src/web/fullscreen-lyrics-page.tsx, src/web/fullscreen-lyrics-page.test.tsx, src/core/sync/lyric-motion-window.ts</read_first>
  <behavior>
    - Fullscreen uses exported motion constants instead of inline timing literals.
    - Short-gap and long-gap fixtures still produce readable transition behavior (no immediate jump or overly long drift).
    - Existing diagnostics/cue/hierarchy tests remain green.
  </behavior>
  <action>Update fullscreen motion-window call sites to pass or rely on exported defaults from `lyric-motion-window.ts` and remove any remaining inline window-clamp literals. Extend fullscreen test fixtures with one short-gap and one long-gap scenario that assert translateY remains hold-first then transitions within clamped timing expectations. Keep all existing fullscreen test cases intact.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.tsx` imports at least one `DEFAULT_*TRANSITION*` constant from `@/core/sync/lyric-motion-window`.
    - `src/web/fullscreen-lyrics-page.test.tsx` contains short-gap and long-gap transition-window assertions.
    - `npm run test -- src/core/sync/lyric-motion-window.test.ts src/web/fullscreen-lyrics-page.test.tsx && npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/core/sync/lyric-motion-window.test.ts src/web/fullscreen-lyrics-page.test.tsx && npm run build</automated>
  </verify>
  <done>Clamp-tuned transition behavior is centralized, wired into fullscreen motion, and verified with targeted plus regression tests.</done>
</task>

</tasks>

<verification>
Run `npm run test -- src/core/sync/lyric-motion-window.test.ts src/web/fullscreen-lyrics-page.test.tsx && npm run build` to confirm clamp tuning and fullscreen regressions are green.
</verification>

<success_criteria>
TRN-01 is operational and tunable: transition timing adapts per gap, clamps to readable bounds, and remains stable in fullscreen behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/15-hold-and-transition-motion-windowing/15-03-SUMMARY.md`
</output>
