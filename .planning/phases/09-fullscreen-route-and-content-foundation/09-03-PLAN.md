---
phase: 09-fullscreen-route-and-content-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - src/web/fullscreen-lyrics-page.tsx
  - src/web/fullscreen-lyrics-page.test.tsx
  - .planning/phases/09-fullscreen-route-and-content-foundation/09-FULLSCREEN-VERIFICATION.md
autonomous: true
requirements:
  - CHN-03
  - CHN-04
must_haves:
  truths:
    - User sees Chinese lyrics in Simplified Chinese on fullscreen page.
    - User sees non-Chinese segments (Latin, numbers, punctuation) preserved in fullscreen lyric lines.
    - Phase includes repeatable verification evidence for fullscreen requirement coverage.
  artifacts:
    - path: src/web/fullscreen-lyrics-page.tsx
      provides: Final fullscreen line rendering path with Simplified-display fallback.
    - path: src/web/fullscreen-lyrics-page.test.tsx
      provides: Regression tests for Chinese normalization and mixed-content preservation in fullscreen route.
    - path: .planning/phases/09-fullscreen-route-and-content-foundation/09-FULLSCREEN-VERIFICATION.md
      provides: Requirement-traceable verification checklist for FULL and CHN goals.
  key_links:
    - from: src/web/fullscreen-lyrics-page.tsx
      to: src/core/lyrics/unicode-normalization.ts
      via: line display fallback
      pattern: normalizeChineseForDisplay
    - from: src/web/fullscreen-lyrics-page.test.tsx
      to: src/web/fullscreen-lyrics-page.tsx
      via: rendered line text assertions
      pattern: 爱在台北|欢迎光临 ABC 2026!
---

<objective>
Lock Simplified Chinese output behavior in fullscreen mode and publish the phase verification runbook.

Purpose: CHN-03 and CHN-04 must be proven at the final fullscreen rendering surface, not only in shared helpers.
Output: Fullscreen CHN regression tests, fallback normalization wiring, and a reproducible phase verification artifact.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fullscreen-route-and-content-foundation/09-RESEARCH.md
@.planning/phases/09-fullscreen-route-and-content-foundation/09-VALIDATION.md
@.planning/phases/09-fullscreen-route-and-content-foundation/09-02-SUMMARY.md
@src/core/lyrics/unicode-normalization.ts
@src/web/fullscreen-lyrics-page.tsx

<interfaces>
From src/core/lyrics/unicode-normalization.ts:
```typescript
export function normalizeChineseForDisplay(input: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fullscreen rendering tests for Simplified Chinese and mixed-content preservation</name>
  <files>src/web/fullscreen-lyrics-page.test.tsx</files>
  <read_first>src/web/fullscreen-lyrics-page.test.tsx, src/web/fullscreen-lyrics-page.tsx, src/core/lyrics/unicode-normalization.ts</read_first>
  <behavior>
    - Test 1: Traditional lyric source `愛在臺北` is rendered as `爱在台北` in fullscreen active/visible lines.
    - Test 2: Mixed-content source `歡迎光臨 ABC 2026!` is rendered as `欢迎光临 ABC 2026!`.
    - Test 3: Traditional originals are not rendered in visible lyric text nodes.
  </behavior>
  <action>Extend `src/web/fullscreen-lyrics-page.test.tsx` with deterministic Chinese fixtures and assertions against rendered fullscreen lyric lines. Use route/page rendering path (not direct helper invocation) so behavior is proven end-to-end for fullscreen UI. Add positive assertions for `爱在台北` and `欢迎光临 ABC 2026!`, and negative assertions for `愛在臺北` and `歡迎光臨 ABC 2026!` in visible lyric content.</action>
  <acceptance_criteria>
    - test file includes explicit assertions for `爱在台北` and `欢迎光临 ABC 2026!`
    - test file includes negative assertions for Traditional originals
    - tests run through fullscreen page rendering path
    - `npm test -- src/web/fullscreen-lyrics-page.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/fullscreen-lyrics-page.test.tsx</automated>
  </verify>
  <done>Fullscreen CHN behavior is regression-locked with route-surface tests.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure fullscreen fallback normalization and publish phase verification checklist</name>
  <files>src/web/fullscreen-lyrics-page.tsx, .planning/phases/09-fullscreen-route-and-content-foundation/09-FULLSCREEN-VERIFICATION.md</files>
  <read_first>src/web/fullscreen-lyrics-page.tsx, src/core/lyrics/unicode-normalization.ts, .planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-SIMPLIFIED-CHINESE-VERIFICATION.md</read_first>
  <action>Update fullscreen lyric line mapping in `src/web/fullscreen-lyrics-page.tsx` to use `line.displayText ?? normalizeChineseForDisplay(line.text)` for every rendered line, including active and adjacent lines. Create `.planning/phases/09-fullscreen-route-and-content-foundation/09-FULLSCREEN-VERIFICATION.md` with sections: prerequisites, automated commands, manual browser checks, and requirement traceability table. Include these exact automated commands: `npm test -- src/main.test.tsx src/web/fullscreen-lyrics-page.test.tsx`, `npm test -- src/web/app-shell.test.tsx`, and `npm run build`. Traceability table must include rows for FULL-01, FULL-02, CHN-03, and CHN-04.</action>
  <acceptance_criteria>
    - fullscreen page contains `line.displayText ?? normalizeChineseForDisplay(line.text)` in rendered lyric mapping
    - verification file exists at exact path and includes all three required commands exactly
    - verification file includes requirement rows for FULL-01, FULL-02, CHN-03, and CHN-04
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Fullscreen route guarantees Simplified Chinese display fallback and ships reproducible verification evidence.</done>
</task>

</tasks>

<verification>
- `npm test -- src/web/fullscreen-lyrics-page.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- CHN-03 is verified on fullscreen visible lyric output.
- CHN-04 is verified by mixed-content preservation assertions and runbook traceability.
</success_criteria>

<output>
After completion, create `.planning/phases/09-fullscreen-route-and-content-foundation/09-03-SUMMARY.md`
</output>
