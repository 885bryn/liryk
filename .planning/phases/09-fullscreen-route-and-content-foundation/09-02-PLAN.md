---
phase: 09-fullscreen-route-and-content-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/web/fullscreen-lyrics-page.tsx
  - src/web/fullscreen-lyrics-page.test.tsx
  - src/web/web-app-router.tsx
autonomous: true
requirements:
  - FULL-01
  - FULL-02
must_haves:
  truths:
    - User sees immersive fullscreen page content without shell header or connection pane chrome.
    - User sees lyrics in a centered column with left-aligned text.
    - User sees stable vertical breathing room above and below lyric content.
  artifacts:
    - path: src/web/fullscreen-lyrics-page.tsx
      provides: Fullscreen page component with centered-column lyrics layout and immersive-only content.
    - path: src/web/fullscreen-lyrics-page.test.tsx
      provides: Contract tests for layout markers, alignment classes, spacing classes, and no-shell-chrome assertions.
    - path: src/web/web-app-router.tsx
      provides: Fullscreen route branch wired to the new page component.
  key_links:
    - from: src/web/web-app-router.tsx
      to: src/web/fullscreen-lyrics-page.tsx
      via: `/fullscreen` branch component render
      pattern: FullscreenLyricsPage
    - from: src/web/fullscreen-lyrics-page.tsx
      to: src/ui/lyrics/live-lyrics-panel.tsx
      via: panel builder line/metadata model
      pattern: createLiveLyricsPanelBuilder
---

<objective>
Implement the dedicated fullscreen page surface with centered lyric-column composition and route wiring.

Purpose: FULL-01 and FULL-02 require an immersive page that removes shell clutter and establishes fullscreen layout geometry.
Output: `FullscreenLyricsPage` component, route hookup, and automated layout/no-chrome tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fullscreen-route-and-content-foundation/09-RESEARCH.md
@.planning/phases/09-fullscreen-route-and-content-foundation/09-01-SUMMARY.md
@src/web/app-shell.tsx
@src/ui/lyrics/live-lyrics-panel.tsx

<interfaces>
From src/ui/lyrics/live-lyrics-panel.tsx:
```typescript
export type LiveLyricsPanelModel = LiveLyricsViewModel & {
  nowPlayingTitle: string;
  nowPlayingArtist: string;
  stateRailMessage: string;
  stateRailVariant: "idle" | "info" | "warning";
};

export function createLiveLyricsPanelBuilder(): {
  build(input: {
    syncState: LiveSyncUiState;
    lines: string[];
    trackTitle?: string;
    trackArtist?: string;
    showReturnToLive: boolean;
  }): LiveLyricsPanelModel;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fullscreen layout contract tests for immersive/no-chrome composition</name>
  <files>src/web/fullscreen-lyrics-page.test.tsx</files>
  <read_first>src/web/app-shell.test.tsx, src/ui/lyrics/live-lyrics-panel.tsx, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: fullscreen page renders `fullscreen-lyrics-layout` and `fullscreen-lyrics-column` markers.
    - Test 2: fullscreen page omits shell markers `shell-layout`, `Connection pane`, and header title `Liryk`.
    - Test 3: fullscreen column classes include centered width, left-aligned lyric text, and vertical spacing classes.
  </behavior>
  <action>Create `src/web/fullscreen-lyrics-page.test.tsx` with deterministic assertions for marker and class contracts. Require these class tokens on the page wrapper: `min-h-screen`, `w-full`, `bg-background`, `text-foreground`. Require these class tokens on lyric column wrapper: `mx-auto`, `max-w-3xl`, `text-left`, `py-20`, `sm:py-24`, `lg:py-28`. Add negative checks that no element with `data-testid="shell-layout"` exists and that headings/text for `Connection` and `Liryk` are absent.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.test.tsx` asserts both required test IDs
    - tests assert absence of shell chrome markers and labels
    - tests assert required wrapper and column class tokens exactly
    - `npm test -- src/web/fullscreen-lyrics-page.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/fullscreen-lyrics-page.test.tsx</automated>
  </verify>
  <done>Fullscreen composition requirements are test-locked before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fullscreen page component and route branch wiring</name>
  <files>src/web/fullscreen-lyrics-page.tsx, src/web/web-app-router.tsx</files>
  <read_first>src/web/web-app-router.tsx, src/web/app-shell.tsx, src/web/fullscreen-lyrics-page.test.tsx, src/ui/lyrics/live-lyrics-panel.tsx</read_first>
  <action>Create `src/web/fullscreen-lyrics-page.tsx` exporting `FullscreenLyricsPage` with immersive-only markup: root `<div data-testid="fullscreen-lyrics-layout" className="min-h-screen w-full bg-background text-foreground">`; column `<main data-testid="fullscreen-lyrics-column" className="mx-auto flex min-h-screen w-full max-w-3xl flex-col justify-center gap-8 px-6 py-20 text-left sm:px-8 sm:py-24 lg:py-28">`. Render only lyric-focused content (track title, artist, state rail, active line, next line) and do not render connection controls, shell cards, or theme toggle/header chrome. Reuse existing data flow boundaries from `AppShell` (`useWebAuthRuntime`, `fetchWebNowPlaying`, `resolveLyricsForTrack`, `createLiveLyricsPanelBuilder`) so fullscreen content stays bound to live lyrics state. Update `src/web/web-app-router.tsx` `/fullscreen` branch to render `<FullscreenLyricsPage />` from the new file.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.tsx` exports `FullscreenLyricsPage`
    - root and column wrappers include exact test IDs and class contracts from Task 1
    - file does not contain imports for `Card`, `DropdownMenu`, or `ThemeToggle`
    - `/fullscreen` branch in `src/web/web-app-router.tsx` renders `FullscreenLyricsPage`
    - `npm test -- src/main.test.tsx src/web/fullscreen-lyrics-page.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/main.test.tsx src/web/fullscreen-lyrics-page.test.tsx</automated>
  </verify>
  <done>Dedicated fullscreen route renders immersive centered-column lyrics content with no utility-shell chrome.</done>
</task>

</tasks>

<verification>
- `npm test -- src/main.test.tsx src/web/fullscreen-lyrics-page.test.tsx`
</verification>

<success_criteria>
- FULL-01 is satisfied by a dedicated fullscreen route that renders immersive-only content.
- FULL-02 is satisfied by centered column geometry, left-aligned lyric text, and stable vertical breathing room.
</success_criteria>

<output>
After completion, create `.planning/phases/09-fullscreen-route-and-content-foundation/09-02-SUMMARY.md`
</output>
