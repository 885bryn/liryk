---
phase: 09-fullscreen-route-and-content-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.tsx
  - src/main.test.tsx
  - src/web/web-app-router.tsx
autonomous: true
requirements:
  - FULL-01
must_haves:
  truths:
    - User can open `/fullscreen` and land on a dedicated fullscreen experience entry point.
    - User can still open `/` and get the existing utility shell flow.
    - Unknown paths do not break app boot and resolve to the shell entry.
  artifacts:
    - path: src/web/web-app-router.tsx
      provides: Path-based app entry switch between shell and fullscreen page.
    - path: src/main.tsx
      provides: Root render uses the route switch instead of hard-wired AppShell.
    - path: src/main.test.tsx
      provides: Route-level contract tests for `/`, `/fullscreen`, and fallback path.
  key_links:
    - from: src/main.tsx
      to: src/web/web-app-router.tsx
      via: root render target component
      pattern: WebAppRouter
    - from: src/web/web-app-router.tsx
      to: src/web/app-shell.tsx
      via: default route branch
      pattern: AppShell
---

<objective>
Establish explicit web entry routing so fullscreen mode has a dedicated URL without breaking the existing shell page.

Purpose: FULL-01 requires a direct fullscreen entry path before layout and lyric rendering can be implemented.
Output: Route switch component and locked route contracts for `/`, `/fullscreen`, and unknown-path fallback.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fullscreen-route-and-content-foundation/09-RESEARCH.md
@.planning/phases/09-fullscreen-route-and-content-foundation/09-VALIDATION.md
@src/main.tsx
@src/web/app-shell.tsx

<interfaces>
From src/main.tsx:
```typescript
createRoot(rootElement).render(<AppShell />);
```

From src/web/app-shell.tsx:
```typescript
export function AppShell(input?: AppShellProps)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add route-entry tests for shell, fullscreen, and fallback paths</name>
  <files>src/main.test.tsx</files>
  <read_first>src/main.tsx, src/web/app-shell.tsx, .planning/ROADMAP.md</read_first>
  <behavior>
    - Test 1: pathname `/` renders shell marker `shell-layout` and does not render fullscreen marker.
    - Test 2: pathname `/fullscreen` renders fullscreen marker `fullscreen-lyrics-layout` and does not render `shell-layout`.
    - Test 3: unknown pathname (for example `/missing`) resolves to shell marker `shell-layout`.
  </behavior>
  <action>Create `src/main.test.tsx` and mock `window.location.pathname` per test case using `history.pushState`. Render the root app entry component exactly as `src/main.tsx` does. Assert by `data-testid`: shell route requires `shell-layout`; fullscreen route requires `fullscreen-lyrics-layout`; fallback route requires `shell-layout`. Use negative assertions to prove shell and fullscreen markers never render together in the same route case.</action>
  <acceptance_criteria>
    - `src/main.test.tsx` contains test cases for `/`, `/fullscreen`, and `/missing`
    - tests assert `fullscreen-lyrics-layout` exists only on `/fullscreen`
    - tests assert `shell-layout` exists on `/` and `/missing`
    - `npm test -- src/main.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/main.test.tsx</automated>
  </verify>
  <done>Route-level behavior is locked before route wiring changes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement entry router and wire main bootstrap to it</name>
  <files>src/web/web-app-router.tsx, src/main.tsx</files>
  <read_first>src/main.tsx, src/web/app-shell.tsx, src/main.test.tsx</read_first>
  <action>Create `src/web/web-app-router.tsx` exporting `WebAppRouter()` that reads `window.location.pathname` and returns `AppShell` for `/`, `FullscreenLyricsPage` for `/fullscreen`, and `AppShell` for all other paths. Update `src/main.tsx` import from `AppShell` to `WebAppRouter` and render `<WebAppRouter />`. Keep existing root-element guard (`Root container not found.`) unchanged.</action>
  <acceptance_criteria>
    - `src/web/web-app-router.tsx` exports `WebAppRouter`
    - route switch includes explicit `/fullscreen` branch and fallback-to-shell branch
    - `src/main.tsx` renders `<WebAppRouter />`
    - `npm test -- src/main.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/main.test.tsx</automated>
  </verify>
  <done>App entry now supports dedicated fullscreen routing while preserving existing shell behavior.</done>
</task>

</tasks>

<verification>
- `npm test -- src/main.test.tsx`
</verification>

<success_criteria>
- FULL-01 route entry exists and is test-locked for `/fullscreen`.
- Existing shell route remains reachable from `/` and fallback paths.
</success_criteria>

<output>
After completion, create `.planning/phases/09-fullscreen-route-and-content-foundation/09-01-SUMMARY.md`
</output>
