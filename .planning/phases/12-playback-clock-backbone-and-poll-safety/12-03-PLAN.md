---
phase: 12-playback-clock-backbone-and-poll-safety
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/app/playback-runtime.ts
  - src/app/playback-runtime.test.ts
  - .planning/phases/12-playback-clock-backbone-and-poll-safety/12-03-VERIFICATION.md
autonomous: true
requirements:
  - CLK-02
must_haves:
  truths:
    - Overlapping playback poll completions cannot overwrite newer track/progress samples.
    - Runtime only promotes trusted newer snapshots to listeners.
    - Verification path includes explicit race reproduction steps and expected outcomes.
  artifacts:
    - path: src/app/playback-runtime.ts
      provides: Strong stale-response suppression during overlapping polls.
    - path: src/app/playback-runtime.test.ts
      provides: Deterministic race-order regression coverage.
    - path: .planning/phases/12-playback-clock-backbone-and-poll-safety/12-03-VERIFICATION.md
      provides: Reproducible overlap-race verification runbook.
  key_links:
    - from: src/app/playback-runtime.ts
      to: src/core/playback/types.ts
      via: isNewerSnapshot gate before emit
      pattern: "isNewerSnapshot"
    - from: src/app/playback-runtime.test.ts
      to: src/app/playback-runtime.ts
      via: deferred Promise overlap tests
      pattern: "deferred|drops stale"
---

<objective>
Harden and verify stale poll protection so out-of-order async completions cannot regress live playback state.

Purpose: Guarantee race-safe playback state updates under real overlapping poll behavior.
Output: Runtime guard refinements, overlap-focused tests, and phase-local verification runbook.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-playback-clock-backbone-and-poll-safety/12-RESEARCH.md
@src/app/playback-runtime.ts
@src/app/playback-runtime.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strengthen playback runtime trusted-snapshot gating and race tests</name>
  <files>src/app/playback-runtime.ts, src/app/playback-runtime.test.ts</files>
  <read_first>src/app/playback-runtime.ts, src/app/playback-runtime.test.ts, src/core/playback/types.ts</read_first>
  <behavior>
    - Test 1: Two concurrent polls resolving out of order emit only newest snapshot.
    - Test 2: Older completion with lower `capturedAtMs` never updates `latestSnapshot`.
    - Test 3: Same `capturedAtMs` but lower `progressMs` is ignored to prevent regressions.
  </behavior>
  <action>Keep latest-action-wins request ordering, and make trusted-update checks explicit before both emit and `latestSnapshot` assignment. Preserve behavior: if request is stale by request id or by `isNewerSnapshot(latestSnapshot, incoming)` freshness check, return early without emit or scheduling mutation. Extend `src/app/playback-runtime.test.ts` with deferred overlap fixtures asserting `seen` length and final `getLatestSnapshot()` values under inverted completion order and equal-timestamp/lower-progress edge case.</action>
  <acceptance_criteria>`playback-runtime.test.ts` includes overlap and equal-timestamp regression tests; `playback-runtime.ts` contains early-return guards before `latestSnapshot = incoming`; `npm run test -- src/app/playback-runtime.test.ts` exits 0.</acceptance_criteria>
  <verify>
    <automated>npm run test -- src/app/playback-runtime.test.ts</automated>
  </verify>
  <done>Runtime cannot regress state from stale overlapping poll completions and tests prove race safety.</done>
</task>

<task type="auto">
  <name>Task 2: Publish overlap race verification runbook for CLK-02</name>
  <files>.planning/phases/12-playback-clock-backbone-and-poll-safety/12-03-VERIFICATION.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, src/app/playback-runtime.test.ts</read_first>
  <action>Create `.planning/phases/12-playback-clock-backbone-and-poll-safety/12-03-VERIFICATION.md` with sections: objective, automated commands, expected pass outputs, and manual race sanity steps. Include exact commands `npm run test -- src/app/playback-runtime.test.ts` and `npm run build`, plus requirement traceability table mapping CLK-02 to runtime/test files.</action>
  <acceptance_criteria>`12-03-VERIFICATION.md` contains headers `## Automated Checks`, `## Manual Sanity`, and `## Requirement Traceability`; file includes exact command strings and references `CLK-02`.</acceptance_criteria>
  <verify>
    <automated>npm run test -- src/app/playback-runtime.test.ts && npm run build</automated>
  </verify>
  <done>Phase has a reproducible verification artifact proving stale poll safety behavior.</done>
</task>

</tasks>

<verification>
Run `npm run test -- src/app/playback-runtime.test.ts && npm run build` and verify overlap-race tests plus compile gate both pass.
</verification>

<success_criteria>
Playback runtime ignores stale overlap completions in deterministic tests, and verification documentation captures repeatable proof for CLK-02.
</success_criteria>

<output>
After completion, create `.planning/phases/12-playback-clock-backbone-and-poll-safety/12-03-SUMMARY.md`
</output>
