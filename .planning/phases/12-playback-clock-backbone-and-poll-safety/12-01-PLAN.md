---
phase: 12-playback-clock-backbone-and-poll-safety
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/core/playback/playback-clock.ts
  - src/core/playback/playback-clock.test.ts
  - src/core/playback/types.ts
autonomous: true
requirements:
  - CLK-01
must_haves:
  truths:
    - User-visible timing can be computed locally from the last trusted playback sample.
    - Estimated playback progress advances while playing and freezes while paused.
    - Estimation remains deterministic under injected monotonic time in tests.
  artifacts:
    - path: src/core/playback/playback-clock.ts
      provides: Playback anchor and estimation contract for runtime consumers.
    - path: src/core/playback/playback-clock.test.ts
      provides: Deterministic coverage for playing/paused/negative-time edge behavior.
  key_links:
    - from: src/core/playback/playback-clock.ts
      to: src/core/playback/types.ts
      via: PlaybackSnapshot-derived anchor type usage
      pattern: "PlaybackSnapshot|PlaybackClockAnchor"
---

<objective>
Define a reusable playback clock anchor contract and deterministic estimator utility so phase runtime code has one timing backbone.

Purpose: Establish a pure, testable source of truth for estimated progress between Spotify polls.
Output: Playback clock module plus unit tests that lock estimation behavior.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-playback-clock-backbone-and-poll-safety/12-RESEARCH.md
@src/core/playback/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add playback clock contracts and failing estimator tests</name>
  <files>src/core/playback/playback-clock.test.ts, src/core/playback/types.ts</files>
  <read_first>src/core/playback/types.ts, src/core/sync/lyric-sync-engine.ts</read_first>
  <behavior>
    - Test 1: Anchor at 10_000ms and now+750ms while playing returns 10_750ms.
    - Test 2: Paused anchor always returns fixed progress regardless of elapsed local time.
    - Test 3: Negative elapsed local delta clamps to anchor progress (no regression).
  </behavior>
  <action>Create `src/core/playback/playback-clock.test.ts` with deterministic tests using injected `nowPerfMs` values and expected exact `progressMs` outputs. Extend `src/core/playback/types.ts` with exported clock-specific types: `PlaybackClockAnchor` and `PlaybackClockSample` (snapshot plus perf timestamp capture metadata) so downstream plans can import stable contracts.</action>
  <acceptance_criteria>`src/core/playback/playback-clock.test.ts` contains `describe("playback clock")`; `src/core/playback/types.ts` exports `PlaybackClockAnchor`; running `npm run test -- src/core/playback/playback-clock.test.ts` fails before implementation.</acceptance_criteria>
  <verify>
    <automated>npm run test -- src/core/playback/playback-clock.test.ts</automated>
  </verify>
  <done>Clock contract types and failing RED tests exist with exact estimator expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement playback clock estimator utility</name>
  <files>src/core/playback/playback-clock.ts, src/core/playback/playback-clock.test.ts</files>
  <read_first>src/core/playback/playback-clock.test.ts, src/core/playback/types.ts, src/core/sync/lyric-sync-engine.ts</read_first>
  <behavior>
    - Test 1 passes with monotonic elapsed addition while playing.
    - Test 2 passes with paused anchor freezing progress.
    - Test 3 passes with non-negative progress clamp.
  </behavior>
  <action>Create `src/core/playback/playback-clock.ts` exporting `createPlaybackClockAnchor(sample)` and `estimatePlaybackProgressMs(anchor, nowPerfMs)`; compute elapsed as `Math.max(0, nowPerfMs - anchor.capturedAtPerfMs)`, add elapsed only when `anchor.isPlaying`, and clamp result to `>= 0`. Keep module pure (no timers, no store writes). Update tests to GREEN with exact expected values.</action>
  <acceptance_criteria>`src/core/playback/playback-clock.ts` exports `createPlaybackClockAnchor` and `estimatePlaybackProgressMs`; estimator uses `Math.max(0` clamp; `npm run test -- src/core/playback/playback-clock.test.ts` exits 0.</acceptance_criteria>
  <verify>
    <automated>npm run test -- src/core/playback/playback-clock.test.ts</automated>
  </verify>
  <done>Playback clock module is implemented and deterministic tests pass.</done>
</task>

</tasks>

<verification>
Run `npm run test -- src/core/playback/playback-clock.test.ts` and confirm all new clock contract tests pass.
</verification>

<success_criteria>
`PlaybackClockAnchor` exists, estimator behavior is deterministic, and phase runtime consumers can import a stable clock utility instead of duplicating progress math.
</success_criteria>

<output>
After completion, create `.planning/phases/12-playback-clock-backbone-and-poll-safety/12-01-SUMMARY.md`
</output>
