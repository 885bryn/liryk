---
phase: 12-playback-clock-backbone-and-poll-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/state/playback/live-sync-store.ts
  - src/state/playback/live-sync-store.test.ts
  - src/app/live-sync-runtime.ts
  - src/app/live-sync-runtime.test.ts
autonomous: true
requirements:
  - CLK-01
must_haves:
  truths:
    - Lyric timing consumers can read estimated playback progress between Spotify polls.
    - Estimated progress advances smoothly while playing and stays stable when paused/idle.
    - Track resets clear stale estimated progress state.
  artifacts:
    - path: src/state/playback/live-sync-store.ts
      provides: Public estimated progress field exposed to consumers.
    - path: src/app/live-sync-runtime.ts
      provides: Runtime wiring that updates estimated progress on ticker frames.
  key_links:
    - from: src/app/live-sync-runtime.ts
      to: src/core/playback/playback-clock.ts
      via: estimatePlaybackProgressMs import call
      pattern: "estimatePlaybackProgressMs"
    - from: src/app/live-sync-runtime.ts
      to: src/state/playback/live-sync-store.ts
      via: setEstimatedProgressMs updates
      pattern: "setEstimatedProgressMs"
---

<objective>
Wire playback-clock estimation into live sync runtime/state so lyric timing logic can read fresh progress continuously between polls.

Purpose: Remove poll-tick dependency for timing consumers and stabilize UI timing perception.
Output: Store/runtime updates that surface anchor-driven estimated progress.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-playback-clock-backbone-and-poll-safety/12-RESEARCH.md
@src/app/live-sync-runtime.ts
@src/state/playback/live-sync-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend live sync store contract with estimated progress field</name>
  <files>src/state/playback/live-sync-store.ts, src/state/playback/live-sync-store.test.ts</files>
  <read_first>src/state/playback/live-sync-store.ts, src/state/playback/live-sync-store.test.ts, src/app/live-sync-runtime.test.ts</read_first>
  <behavior>
    - Test 1: Initial state exposes `estimatedProgressMs: 0`.
    - Test 2: `setEstimatedProgressMs(1450)` updates state to exactly 1450.
    - Test 3: Track reset clears state and returns `estimatedProgressMs` to 0.
  </behavior>
  <action>Add `estimatedProgressMs: number` to `LiveSyncUiState` with initial value `0`. Implement `setEstimatedProgressMs(progressMs: number)` method in `LiveSyncStore`. Ensure `setTrack(null)` and track-change reset path zero out `estimatedProgressMs` so stale progress does not bleed across tracks.</action>
  <acceptance_criteria>`LiveSyncUiState` includes `estimatedProgressMs`; `LiveSyncStore` exports `setEstimatedProgressMs`; tests assert initial/update/reset behavior using exact numeric values.</acceptance_criteria>
  <verify>
    <automated>npm run test -- src/state/playback/live-sync-store.test.ts</automated>
  </verify>
  <done>Store contract exposes estimated progress with deterministic reset semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update live sync runtime to publish anchor-driven progress each frame</name>
  <files>src/app/live-sync-runtime.ts, src/app/live-sync-runtime.test.ts</files>
  <read_first>src/app/live-sync-runtime.ts, src/app/live-sync-runtime.test.ts, src/core/playback/playback-clock.ts, src/state/playback/live-sync-store.ts</read_first>
  <behavior>
    - Test 1: Playing snapshot writes increasing `estimatedProgressMs` across frame ticks.
    - Test 2: Paused snapshot keeps `estimatedProgressMs` fixed.
    - Test 3: Idle snapshot sets `estimatedProgressMs` to 0.
  </behavior>
  <action>Import `createPlaybackClockAnchor` and `estimatePlaybackProgressMs` from `src/core/playback/playback-clock.ts`. In playback snapshot handling, store/update a runtime-local anchor from trusted snapshots and call `setEstimatedProgressMs` on every `applyFrame`. Keep existing `syncEngine.estimateFrame()` behavior for active/next/confidence, but source store progress field from playback-clock estimator so timing consumers can read continuous progress independent of poll cadence.</action>
  <acceptance_criteria>`live-sync-runtime.ts` imports playback clock utilities; `setEstimatedProgressMs(` appears in frame application path; `npm run test -- src/app/live-sync-runtime.test.ts src/state/playback/live-sync-store.test.ts` exits 0.</acceptance_criteria>
  <verify>
    <automated>npm run test -- src/app/live-sync-runtime.test.ts src/state/playback/live-sync-store.test.ts</automated>
  </verify>
  <done>Runtime continuously publishes anchor-based estimated progress while preserving existing lyric index/confidence behavior.</done>
</task>

</tasks>

<verification>
Run `npm run test -- src/app/live-sync-runtime.test.ts src/state/playback/live-sync-store.test.ts` and confirm estimated-progress assertions pass for playing, paused, and idle transitions.
</verification>

<success_criteria>
Consumers can read `estimatedProgressMs` from live sync state, value updates between polls while playing, and resets safely on idle/track-change transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-playback-clock-backbone-and-poll-safety/12-02-SUMMARY.md`
</output>
