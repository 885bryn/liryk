---
phase: 16-smooth-transition-execution-and-settling
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/core/sync/lyric-motion-window.ts
  - src/core/sync/lyric-motion-window.test.ts
  - src/web/fullscreen-lyrics-page.tsx
  - src/web/fullscreen-lyrics-page.test.tsx
autonomous: true
requirements:
  - MOT-06
  - TRN-02
must_haves:
  truths:
    - Short lyric gaps move smoothly instead of abrupt snaps.
    - Long lyric gaps remain stable until late transition start near line change.
    - Each line change lands into a stable resting offset with no residual drift.
  artifacts:
    - path: src/core/sync/lyric-motion-window.ts
      provides: Settling-aware target offset interpolation contract.
    - path: src/core/sync/lyric-motion-window.test.ts
      provides: Deterministic short/long gap and settle boundary coverage.
    - path: src/web/fullscreen-lyrics-page.tsx
      provides: Fullscreen wiring to target-offset helper for settled translateY output.
    - path: src/web/fullscreen-lyrics-page.test.tsx
      provides: UI-level regression tests for short-gap smoothness and long-gap hold/settle behavior.
  key_links:
    - from: src/web/fullscreen-lyrics-page.tsx
      to: src/core/sync/lyric-motion-window.ts
      via: `getTargetScrollOffset` drives translateY target per frame
      pattern: "getTargetScrollOffset|translateY"
    - from: src/core/sync/lyric-motion-window.test.ts
      to: src/web/fullscreen-lyrics-page.test.tsx
      via: shared edge cases prove settle semantics for short and long gaps
      pattern: "short gap|long gap|complete"
---

<objective>
Finalize settled target-offset interpolation so fullscreen motion handles short and long gaps cleanly and lands without post-change drift.

Purpose: Deliver MOT-06 and TRN-02 by codifying settle semantics in reusable core helpers and wiring them to fullscreen transform output.
Output: New target-offset helper contract, deterministic edge-case tests, and fullscreen integration updates.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-smooth-transition-execution-and-settling/16-01-SUMMARY.md
@src/core/sync/lyric-motion-window.ts
@src/web/fullscreen-lyrics-page.tsx
@src/web/fullscreen-lyrics-page.test.tsx

<interfaces>
From src/core/sync/lyric-motion-window.ts:
```typescript
export function getTransitionPhase(input: {
  progressMs: number;
  currentStartMs: number;
  nextStartMs: number;
  minTransitionMs?: number;
  maxTransitionMs?: number;
  transitionFraction?: number;
}): {
  phase: "hold" | "transition" | "complete";
  phaseProgress: number;
  transitionStartMs: number;
  transitionDurationMs: number;
};
```

Contract to add in this plan:
```typescript
export function getTargetScrollOffset(input: {
  currentIndex: number;
  nextIndex: number | null;
  phaseProgress: number;
  phase: "hold" | "transition" | "complete";
  stepPx: number;
}): number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add target-offset settle semantics tests and helper</name>
  <files>src/core/sync/lyric-motion-window.ts, src/core/sync/lyric-motion-window.test.ts</files>
  <behavior>
    - Test 1: hold phase returns current line offset exactly.
    - Test 2: transition phase returns bounded interpolated offset between current and next.
    - Test 3: complete phase returns exact next-line offset with no residual fraction.
    - Test 4: null next index keeps stable current offset across all phases.
  </behavior>
  <action>Write failing tests for `getTargetScrollOffset` in `src/core/sync/lyric-motion-window.test.ts`, then implement the helper in `src/core/sync/lyric-motion-window.ts` as a pure deterministic function. Preserve existing `getTransitionPhase` API and keep outputs bounded so complete phase snaps cleanly to integer line offsets.</action>
  <verify>
    <automated>npm run test -- src/core/sync/lyric-motion-window.test.ts</automated>
  </verify>
  <done>Core helper now defines explicit hold/transition/complete target-offset behavior with deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire fullscreen transform to target-offset helper and lock edge cases</name>
  <files>src/web/fullscreen-lyrics-page.tsx, src/web/fullscreen-lyrics-page.test.tsx</files>
  <behavior>
    - Short-gap fixtures show compact smooth movement rather than abrupt jump.
    - Long-gap fixtures remain in hold until near next line start.
    - Complete boundary lands exactly on next-line translateY with no carryover drift.
  </behavior>
  <action>Update `src/web/fullscreen-lyrics-page.tsx` to compute synced track offset via `getTargetScrollOffset` rather than ad-hoc index mapping. Keep active-tier selection keyed to integer active index. Expand `src/web/fullscreen-lyrics-page.test.tsx` with targeted short-gap, long-gap, and completion-boundary assertions verifying final translateY values and bounded in-window movement.</action>
  <verify>
    <automated>npm run test -- src/core/sync/lyric-motion-window.test.ts src/web/fullscreen-lyrics-page.test.tsx</automated>
  </verify>
  <done>Fullscreen motion uses centralized settle semantics and edge-case tests confirm smooth short gaps, late-start long gaps, and stable landing.</done>
</task>

</tasks>

<verification>
Run `npm run test -- src/core/sync/lyric-motion-window.test.ts src/web/fullscreen-lyrics-page.test.tsx` and confirm all settle and gap-behavior regressions pass.
</verification>

<success_criteria>
MOT-06 and TRN-02 are met in fullscreen behavior: transitions settle cleanly, short gaps stay smooth, and long gaps stay stable until near line handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/16-smooth-transition-execution-and-settling/16-02-SUMMARY.md`
</output>
