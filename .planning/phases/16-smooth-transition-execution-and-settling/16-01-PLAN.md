---
phase: 16-smooth-transition-execution-and-settling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/sync/lyric-motion-window.ts
  - src/core/sync/lyric-motion-window.test.ts
autonomous: true
requirements:
  - VIS-04
must_haves:
  truths:
    - Transition interpolation follows a calm easing curve rather than linear movement.
    - Eased interpolation remains monotonic and bounded so motion never overshoots or bounces.
    - Easing behavior is deterministic and reusable by fullscreen motion logic.
  artifacts:
    - path: src/core/sync/lyric-motion-window.ts
      provides: Reusable easing helper and eased phase-progress output contract.
    - path: src/core/sync/lyric-motion-window.test.ts
      provides: Deterministic easing boundary and monotonicity coverage.
  key_links:
    - from: src/core/sync/lyric-motion-window.ts
      to: src/web/fullscreen-lyrics-page.tsx
      via: fullscreen motion consumes eased progress output for translateY interpolation
      pattern: "getTransitionPhase|phaseProgress"
    - from: src/core/sync/lyric-motion-window.test.ts
      to: src/core/sync/lyric-motion-window.ts
      via: tests lock no-overshoot and no-bounce easing semantics
      pattern: "easeInOutCubic|smoothstep|phaseProgress"
---

<objective>
Add deterministic easing to transition-phase interpolation so fullscreen lyric movement feels smooth without spring-like artifacts.

Purpose: Satisfy VIS-04 by ensuring interpolation is calm, bounded, and reusable from core timing helpers.
Output: Updated motion-window helper contracts with easing and focused unit coverage.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hold-and-transition-motion-windowing/15-03-SUMMARY.md
@src/core/sync/lyric-motion-window.ts
@src/core/sync/lyric-motion-window.test.ts

<interfaces>
From src/core/sync/lyric-motion-window.ts:
```typescript
export type TransitionPhase = "hold" | "transition" | "complete";
export function getTransitionPhase(input: {
  progressMs: number;
  currentStartMs: number;
  nextStartMs: number;
  minTransitionMs?: number;
  maxTransitionMs?: number;
  transitionFraction?: number;
}): {
  phase: TransitionPhase;
  phaseProgress: number;
  transitionStartMs: number;
  transitionDurationMs: number;
};
```

Contract addition in this plan:
```typescript
export function easeInOutCubic(progress: number): number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add easing contract tests for transition progress behavior</name>
  <files>src/core/sync/lyric-motion-window.test.ts</files>
  <behavior>
    - Test 1: `easeInOutCubic(0)` is `0` and `easeInOutCubic(1)` is `1`.
    - Test 2: easing output stays within 0..1 for representative in-range values.
    - Test 3: easing output is monotonic increasing and does not overshoot.
    - Test 4: `getTransitionPhase(...).phaseProgress` in transition phase reflects eased progress instead of raw linear ratio.
  </behavior>
  <action>Write failing-first tests in `src/core/sync/lyric-motion-window.test.ts` that pin easing endpoints, monotonic bounded behavior, and eased mid-window `phaseProgress` output. Keep existing clamp and boundary assertions unchanged.</action>
  <verify>
    <automated>npm run test -- src/core/sync/lyric-motion-window.test.ts</automated>
  </verify>
  <done>Easing semantics are explicitly encoded in deterministic failing-first tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement eased transition progress in motion-window helper</name>
  <files>src/core/sync/lyric-motion-window.ts, src/core/sync/lyric-motion-window.test.ts</files>
  <action>Add `easeInOutCubic(progress)` to `src/core/sync/lyric-motion-window.ts` as a pure bounded helper. Update `getTransitionPhase` so transition-phase `phaseProgress` applies this easing to the normalized linear ratio while preserving hold=0 and complete=1 semantics. Do not change clamp defaults, cue behavior, or phase boundary thresholds.</action>
  <verify>
    <automated>npm run test -- src/core/sync/lyric-motion-window.test.ts</automated>
  </verify>
  <done>Core transition progress is eased, monotonic, bounded, and validated by deterministic tests.</done>
</task>

</tasks>

<verification>
Run `npm run test -- src/core/sync/lyric-motion-window.test.ts` and confirm all easing and existing transition-window assertions pass.
</verification>

<success_criteria>
VIS-04 is established in core contracts: interpolation is eased, non-oscillatory, and safe for downstream fullscreen rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/16-smooth-transition-execution-and-settling/16-01-SUMMARY.md`
</output>
