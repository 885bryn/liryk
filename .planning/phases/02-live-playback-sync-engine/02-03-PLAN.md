---
phase: 02-live-playback-sync-engine
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/ui/lyrics/live-lyrics-panel.tsx
  - src/ui/lyrics/live-lyrics-panel.test.tsx
  - src/ui/lyrics/lyrics-viewport.tsx
  - src/ui/lyrics/lyrics-viewport.test.tsx
  - src/ui/lyrics/use-auto-scroll-controller.ts
  - src/ui/lyrics/use-auto-scroll-controller.test.ts
  - src/app/live-lyrics-presenter.ts
  - src/app/live-lyrics-presenter.test.ts
autonomous: true
requirements:
  - PLAY-01
  - PLAY-03
  - SYNC-02
must_haves:
  truths:
    - User sees live lyrics view track the currently playing song and reset immediately on track change.
    - User sees active and next lines with clear dual emphasis and subtle sync-confidence indicator.
    - User sees active line stay in center-biased viewport during playback, with smooth stepped scrolling.
    - User manual scroll temporarily pauses auto-scroll and exposes an explicit back-to-live control.
    - User sees explicit idle/unavailable/transient status messaging instead of blank lyric UI.
  artifacts:
    - path: src/ui/lyrics/live-lyrics-panel.tsx
      provides: User-facing live lyrics surface with status line and confidence badge.
    - path: src/ui/lyrics/use-auto-scroll-controller.ts
      provides: Auto-scroll pause/resume and return-to-live control logic.
    - path: src/app/live-lyrics-presenter.ts
      provides: Presenter that maps live-sync store state to render model copy/status.
  key_links:
    - from: src/ui/lyrics/live-lyrics-panel.tsx
      to: src/app/live-lyrics-presenter.ts
      via: presentational model mapping for status/copy/confidence
      pattern: buildLiveLyricsViewModel
    - from: src/ui/lyrics/lyrics-viewport.tsx
      to: src/ui/lyrics/use-auto-scroll-controller.ts
      via: scroll target + manual override hooks
      pattern: useAutoScrollController
    - from: src/ui/lyrics/live-lyrics-panel.tsx
      to: src/state/playback/live-sync-store.ts
      via: store selector subscription for active line and playback state
      pattern: selectLiveSync|selectPlaybackState
---

<objective>
Deliver the user-visible live lyrics viewport behavior and playback-state messaging.

Purpose: Turn sync runtime state into a readable, resilient lyrics experience that matches the phase UX decisions.
Output: Live lyrics panel, viewport auto-scroll controller, and presenter/view-model tests for all playback and manual-scroll states.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-live-playback-sync-engine/02-CONTEXT.md
@.planning/phases/02-live-playback-sync-engine/02-RESEARCH.md
@.planning/phases/02-live-playback-sync-engine/02-01-SUMMARY.md
@.planning/phases/02-live-playback-sync-engine/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build live lyrics presenter and status mapping for playback states</name>
  <files>src/app/live-lyrics-presenter.ts, src/app/live-lyrics-presenter.test.ts, src/ui/lyrics/live-lyrics-panel.tsx, src/ui/lyrics/live-lyrics-panel.test.tsx</files>
  <action>Create a presenter/view-model layer that translates live-sync store states into user-facing copy and flags for idle, syncing, reconnecting, no-track, and unsupported-content states. Render dual-line emphasis (active + next) and subtle confidence indicator in `live-lyrics-panel` while keeping wording aligned with context decisions. Include tests for each status variant and track-reset behavior on skip/track change.</action>
  <verify>
    <automated>npm test -- src/app/live-lyrics-presenter.test.ts src/ui/lyrics/live-lyrics-panel.test.tsx</automated>
  </verify>
  <done>Live lyrics panel always shows deterministic state messaging and dual-line emphasis behavior for playback lifecycle changes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement center-biased auto-scroll controller with manual override and return-to-live</name>
  <files>src/ui/lyrics/use-auto-scroll-controller.ts, src/ui/lyrics/use-auto-scroll-controller.test.ts, src/ui/lyrics/lyrics-viewport.tsx, src/ui/lyrics/lyrics-viewport.test.tsx</files>
  <action>Implement auto-scroll logic that advances viewport on active-line changes using smooth stepped movement toward a center-biased target. Pause automatic scrolling when user manually scrolls away, keep highlight updates active, and surface a clear return-to-live control that immediately re-centers active line and resumes automation. Add tests covering manual override timeout, return-to-live action, and no hard-jump/no-constant-drift constraints.</action>
  <verify>
    <automated>npm test -- src/ui/lyrics/use-auto-scroll-controller.test.ts src/ui/lyrics/lyrics-viewport.test.tsx</automated>
  </verify>
  <done>Viewport behavior respects user manual reading while preserving reliable live-line recentering and smooth progression.</done>
</task>

</tasks>

<verification>
Run presenter and viewport tests to verify status-line behavior, dual emphasis, confidence indicator, smooth auto-scroll, manual override pause, and return-to-live control:
- `npm test -- src/app/live-lyrics-presenter.test.ts src/ui/lyrics/live-lyrics-panel.test.tsx src/ui/lyrics/use-auto-scroll-controller.test.ts src/ui/lyrics/lyrics-viewport.test.tsx`
</verification>

<success_criteria>
- SYNC-02 viewport keeps active line in view with center-biased smooth stepping.
- PLAY-03 visual behavior correctly responds to pause/resume/seek/skip and track resets.
- PLAY-01 UI reflects currently playing track context with explicit idle/unavailable states when no supported playback exists.
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-playback-sync-engine/02-03-SUMMARY.md`
</output>
