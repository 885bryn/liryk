---
phase: 02-live-playback-sync-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/playback/types.ts
  - src/core/playback/playback-transition.ts
  - src/core/playback/playback-transition.test.ts
  - src/infra/spotify/spotify-playback-client.ts
  - src/app/playback-runtime.ts
  - src/app/playback-runtime.test.ts
autonomous: true
requirements:
  - PLAY-01
  - PLAY-03
must_haves:
  truths:
    - User sees app playback state tied to the active Spotify track and active device without requiring Premium playback control features.
    - User sees pause/resume/seek/skip changes recognized quickly and reflected as deterministic runtime transitions.
    - User never sees stale track state after rapid playback changes; newest Spotify snapshot wins.
  artifacts:
    - path: src/core/playback/playback-transition.ts
      provides: Transition classifier for pause/resume/seek/skip/track-change/device-change.
    - path: src/infra/spotify/spotify-playback-client.ts
      provides: Typed adapter for currently-playing snapshot retrieval.
    - path: src/app/playback-runtime.ts
      provides: Polling loop and latest-session guarded playback dispatch.
  key_links:
    - from: src/app/playback-runtime.ts
      to: src/infra/spotify/spotify-playback-client.ts
      via: currently playing poll call
      pattern: fetchCurrentPlayback|getCurrentlyPlaying
    - from: src/app/playback-runtime.ts
      to: src/core/playback/playback-transition.ts
      via: previous/current snapshot transition classification
      pattern: classifyPlaybackTransition
---

<objective>
Build the playback snapshot foundation and transition detection runtime for live sync.

Purpose: Establish a deterministic, testable source of truth for Spotify playback events before lyric timing logic is layered on top.
Output: A typed playback client, transition classifier, and polling runtime that emits stable playback state updates.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-live-playback-sync-engine/02-CONTEXT.md
@.planning/phases/02-live-playback-sync-engine/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed playback snapshot and transition classifier primitives</name>
  <files>src/core/playback/types.ts, src/core/playback/playback-transition.ts, src/core/playback/playback-transition.test.ts</files>
  <action>Define normalized playback snapshot types for track identity, progress, timestamp, play state, and device identity. Implement a classifier that compares consecutive snapshots and returns `no_change`, `paused`, `resumed`, `seeked`, `track_changed`, or `device_changed`, with thresholds tuned to prevent false seek detection from small jitter. Include tests for each transition and rapid update ordering edge cases.</action>
  <verify>
    <automated>npm test -- src/core/playback/playback-transition.test.ts</automated>
  </verify>
  <done>Playback transitions are represented by explicit deterministic states and fully covered by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Spotify playback client adapter and polling runtime loop</name>
  <files>src/infra/spotify/spotify-playback-client.ts, src/app/playback-runtime.ts, src/app/playback-runtime.test.ts</files>
  <action>Create a production playback adapter that requests currently-playing snapshots through existing authenticated runtime boundaries and normalizes the response into playback snapshot types. Build `playback-runtime` polling orchestration with adaptive cadence (faster while playing, slower when paused/idle), transition classification integration, and latest-action-wins guards so stale async responses never overwrite newer state. Include tests proving pause/resume/seek/skip handling and stale-result suppression.</action>
  <verify>
    <automated>npm test -- src/app/playback-runtime.test.ts</automated>
  </verify>
  <done>Runtime consistently emits newest playback snapshots and transition metadata tied to the real active Spotify track/device.</done>
</task>

</tasks>

<verification>
Run transition and runtime tests to confirm playback-state transitions are deterministic and newest snapshot ordering is enforced:
- `npm test -- src/core/playback/playback-transition.test.ts src/app/playback-runtime.test.ts`
</verification>

<success_criteria>
- PLAY-01 playback source is tied to Spotify currently-playing snapshots without Premium-only control dependencies.
- PLAY-03 transition events (pause/resume/seek/skip/device change) are explicitly classified and tested.
- Runtime prevents stale response races during rapid playback changes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-playback-sync-engine/02-01-SUMMARY.md`
</output>
