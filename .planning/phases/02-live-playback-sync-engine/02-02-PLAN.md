---
phase: 02-live-playback-sync-engine
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/core/sync/lyric-timeline.ts
  - src/core/sync/lyric-timeline.test.ts
  - src/core/sync/lyric-sync-engine.ts
  - src/core/sync/lyric-sync-engine.test.ts
  - src/state/playback/live-sync-store.ts
  - src/state/playback/live-sync-store.test.ts
  - src/app/live-sync-runtime.ts
  - src/app/live-sync-runtime.test.ts
autonomous: true
requirements:
  - PLAY-02
  - PLAY-03
  - SYNC-01
must_haves:
  truths:
    - User sees active lyric line update continuously from playback position while audio is playing.
    - User sees seek actions jump highlight immediately to the correct line instead of replaying skipped lines.
    - User sees pause freeze the active line and resume continue from correct predicted position.
    - User sees subtle confidence state when sync is estimated instead of exact.
  artifacts:
    - path: src/core/sync/lyric-sync-engine.ts
      provides: Playback-anchor prediction, drift correction, and active-line index selection.
    - path: src/state/playback/live-sync-store.ts
      provides: Canonical sync UI state including active line, next line, and confidence indicator.
    - path: src/app/live-sync-runtime.ts
      provides: Wiring from playback runtime snapshots into sync engine + store updates.
  key_links:
    - from: src/app/live-sync-runtime.ts
      to: src/app/playback-runtime.ts
      via: snapshot subscription/consumer callback
      pattern: onSnapshot|subscribePlayback
    - from: src/app/live-sync-runtime.ts
      to: src/core/sync/lyric-sync-engine.ts
      via: re-anchor and progress estimation calls
      pattern: reanchor|estimateProgress|getActiveLine
    - from: src/app/live-sync-runtime.ts
      to: src/state/playback/live-sync-store.ts
      via: state dispatch for active/next/confidence values
      pattern: setActiveLine|setConfidence|setPlaybackState
---

<objective>
Implement the live lyric synchronization engine and runtime state projection.

Purpose: Convert playback snapshots into user-visible line-level sync behavior with smooth corrections and deterministic control-response behavior.
Output: Timeline parser, sync engine, live-sync store, and runtime integration covered by unit/integration tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-live-playback-sync-engine/02-CONTEXT.md
@.planning/phases/02-live-playback-sync-engine/02-RESEARCH.md
@.planning/phases/02-live-playback-sync-engine/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build lyric timeline indexing and active-line lookup primitives</name>
  <files>src/core/sync/lyric-timeline.ts, src/core/sync/lyric-timeline.test.ts</files>
  <action>Create timeline helpers that ingest timestamped lyric lines, normalize ordering, and expose efficient active-line lookup by progress ms (binary search, bounds-safe for before-first and after-last positions). Include fixtures that model short gaps, dense transitions, and missing trailing timestamps to keep runtime cursoring predictable.</action>
  <verify>
    <automated>npm test -- src/core/sync/lyric-timeline.test.ts</automated>
  </verify>
  <done>Timeline utilities deterministically return active/next line indices for representative progress inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement anchor-based sync engine with drift policy and control transition handling</name>
  <files>src/core/sync/lyric-sync-engine.ts, src/core/sync/lyric-sync-engine.test.ts</files>
  <action>Implement sync engine APIs to re-anchor on new playback snapshots, estimate current progress via `performance.now` deltas while playing, freeze on pause, snap immediately on seeks/skips, and apply gentle correction on minor drift. Engine output must include active line index, next line index, and confidence (`synced`/`estimated`) so UI can render dual emphasis and confidence indicator. Add tests for pause/resume, seek jump, drift correction thresholds, and rapid snapshot races.</action>
  <verify>
    <automated>npm test -- src/core/sync/lyric-sync-engine.test.ts</automated>
  </verify>
  <done>Sync engine continuously maps playback progress to correct lyric line with stable confidence output and control-aware behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Wire sync runtime to playback stream and live-sync state store</name>
  <files>src/state/playback/live-sync-store.ts, src/state/playback/live-sync-store.test.ts, src/app/live-sync-runtime.ts, src/app/live-sync-runtime.test.ts</files>
  <action>Create a live-sync store slice representing playback availability, active/next line, confidence, and transient status text. Integrate runtime wiring that consumes playback snapshots from Plan 01 runtime, feeds them into sync engine, and updates store at animation cadence while preserving latest-action-wins semantics under rapid playback changes. Add tests that assert correct store transitions for playing, paused, seeked, skipped, and no-playback states.</action>
  <verify>
    <automated>npm test -- src/state/playback/live-sync-store.test.ts src/app/live-sync-runtime.test.ts</automated>
  </verify>
  <done>App runtime publishes stable live-sync state derived from playback and sync engine outputs, satisfying real-time highlight behavior.</done>
</task>

</tasks>

<verification>
Run sync core and runtime tests to validate continuous line updates, seek jumps, pause freeze behavior, and confidence derivation:
- `npm test -- src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts src/state/playback/live-sync-store.test.ts src/app/live-sync-runtime.test.ts`
</verification>

<success_criteria>
- PLAY-02 active line updates continuously from predicted playback progress between polls.
- PLAY-03 control transitions (pause/resume/seek/skip) produce expected line movement and state behavior.
- SYNC-01 active and next line selection is correct, deterministic, and confidence-aware.
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-playback-sync-engine/02-02-SUMMARY.md`
</output>
