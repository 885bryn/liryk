---
phase: 13-frame-synced-lyric-engine-and-drift-reconciliation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/core/sync/lyric-timeline.ts
  - src/core/sync/lyric-timeline.test.ts
  - src/core/sync/lyric-sync-engine.test.ts
autonomous: true
requirements:
  - LYR-02
must_haves:
  truths:
    - Active lyric selection is computed via binary search over monotonic line start times.
    - Progress before first timestamp does not mark an active line and keeps next-line prediction deterministic.
    - Boundary behavior at exact line starts, dense transitions, and end-of-track remains correct.
  artifacts:
    - path: src/core/sync/lyric-timeline.ts
      provides: Binary-search active/next resolver with explicit boundary semantics.
    - path: src/core/sync/lyric-timeline.test.ts
      provides: Coverage for before-first, exact-match, and after-last index selection.
    - path: src/core/sync/lyric-sync-engine.test.ts
      provides: Integration proof that engine frame selection reflects resolver contract.
  key_links:
    - from: src/core/sync/lyric-sync-engine.ts
      to: src/core/sync/lyric-timeline.ts
      via: getLineIndicesAt call in estimateFrame
      pattern: "getLineIndicesAt"
---

<objective>
Harden and verify active-line derivation through a binary-search timeline resolver with explicit edge-case semantics.

Purpose: Deliver LYR-02 with deterministic, scalable line selection that remains accurate across timestamp boundaries.
Output: Binary-search resolver updates plus focused unit/integration tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/sync/lyric-timeline.ts
@src/core/sync/lyric-timeline.test.ts
@src/core/sync/lyric-sync-engine.ts

<interfaces>
From src/core/sync/lyric-timeline.ts:
```typescript
export type LyricLine = { startMs: number; text: string; endMs?: number };
export type LyricTimeline = { lines: IndexedLyricLine[] };

export function createLyricTimeline(input: LyricLine[]): LyricTimeline;
export function getLineIndicesAt(
  timeline: LyricTimeline,
  progressMs: number,
): { activeIndex: number | null; nextIndex: number | null };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for binary-search active-line boundaries</name>
  <files>src/core/sync/lyric-timeline.test.ts, src/core/sync/lyric-sync-engine.test.ts</files>
  <behavior>
    - Test 1: Progress before first line returns `activeIndex: null` and `nextIndex: 0`.
    - Test 2: Exact timestamp boundaries select the matching line as active.
    - Test 3: Engine integration reflects resolver output for early progress and dense transitions.
  </behavior>
  <action>Expand `lyric-timeline` tests to codify explicit before-first and exact-start semantics required for user-visible accuracy. Add one `lyric-sync-engine` test asserting `estimateFrame()` propagates `activeLineIndex`/`nextLineIndex` according to resolver behavior without ad hoc fallback logic.</action>
  <verify>
    <automated>npm run test -- src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts</automated>
  </verify>
  <done>Failing tests precisely define LYR-02 selection behavior across binary-search boundary cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement binary-search resolver behavior for active/next line indices</name>
  <files>src/core/sync/lyric-timeline.ts, src/core/sync/lyric-timeline.test.ts, src/core/sync/lyric-sync-engine.test.ts</files>
  <behavior>
    - Binary search finds the latest line with start <= progress and returns null active before first start.
    - Next index always points to the immediate upcoming line when one exists.
    - Existing dense and trailing range tests remain green.
  </behavior>
  <action>Update `getLineIndicesAt` to keep O(log n) active-index lookup while separating pre-first behavior from post-last fallback. Preserve existing timeline normalization (`createLyricTimeline`) and exported function signatures. Ensure implementation stays pure and allocation-light to support frame-driven calls.</action>
  <verify>
    <automated>npm run test -- src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts && npm run build</automated>
  </verify>
  <done>Binary-search resolver is deterministic, edge-safe, and validated by unit + engine integration tests.</done>
</task>

</tasks>

<verification>
Run `npm run test -- src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts && npm run build` to confirm resolver correctness and compile stability.
</verification>

<success_criteria>
Active lyric selection is derived by binary search with explicit boundary semantics and is verified end-to-end through engine frame output.
</success_criteria>

<output>
After completion, create `.planning/phases/13-frame-synced-lyric-engine-and-drift-reconciliation/13-02-SUMMARY.md`
</output>
