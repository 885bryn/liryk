---
phase: 13-frame-synced-lyric-engine-and-drift-reconciliation
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/core/sync/lyric-sync-engine.ts
  - src/core/sync/lyric-sync-engine.test.ts
autonomous: true
requirements:
  - CLK-03
must_haves:
  truths:
    - Large observed-vs-estimated drift applies immediate hard reset to observed progress.
    - Small drift applies bounded soft correction instead of abrupt snapping.
    - Drift confidence state is deterministic and stable for repeated no-change samples.
  artifacts:
    - path: src/core/sync/lyric-sync-engine.ts
      provides: Explicit hard-reset and bounded soft-correction drift policy.
    - path: src/core/sync/lyric-sync-engine.test.ts
      provides: Deterministic coverage for hard/soft correction thresholds and transitions.
  key_links:
    - from: src/core/sync/lyric-sync-engine.ts
      to: src/core/playback/types.ts
      via: transition-aware reanchor policy using PlaybackTransitionKind
      pattern: "PlaybackTransitionKind|reanchor|applyDriftPolicy"
---

<objective>
Implement deterministic drift reconciliation in the lyric sync engine with explicit hard-reset and bounded soft-correction paths.

Purpose: Deliver CLK-03 so timing drift is corrected predictably without introducing jitter or multi-second desync.
Output: Drift policy implementation plus threshold-focused regression tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/sync/lyric-sync-engine.ts
@src/core/sync/lyric-sync-engine.test.ts
@src/core/playback/types.ts

<interfaces>
From src/core/sync/lyric-sync-engine.ts:
```typescript
export type LyricSyncEngine = {
  setTimeline(lines: LyricLine[]): void;
  reanchor(input: { snapshot: PlaybackSnapshot; transition: PlaybackTransitionKind }): void;
  estimateFrame(): SyncFrame;
};
```

From src/core/playback/types.ts:
```typescript
export type PlaybackTransitionKind =
  | "no_change"
  | "paused"
  | "resumed"
  | "seeked"
  | "track_changed"
  | "device_changed";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for drift hard-reset and bounded soft-correction policy</name>
  <files>src/core/sync/lyric-sync-engine.test.ts</files>
  <behavior>
    - Test 1: Drift magnitude above hard threshold snaps to observed progress with `confidence: synced`.
    - Test 2: Drift within soft range applies bounded correction toward observed value without full snap.
    - Test 3: Transition events (`seeked`, `track_changed`, `paused`, `resumed`) always bypass soft correction and sync directly.
  </behavior>
  <action>Extend engine tests with deterministic `nowPerfMs` control to assert exact progress outcomes for hard-reset and soft-correction scenarios. Include both positive and negative drift directions to prevent asymmetric correction bugs and lock confidence semantics.</action>
  <verify>
    <automated>npm run test -- src/core/sync/lyric-sync-engine.test.ts</automated>
  </verify>
  <done>Failing tests define CLK-03 behavior with explicit threshold and confidence expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic drift reconciliation thresholds and bounded correction</name>
  <files>src/core/sync/lyric-sync-engine.ts, src/core/sync/lyric-sync-engine.test.ts</files>
  <behavior>
    - Hard drift path snaps immediately to observed progress.
    - Soft drift path applies clamped incremental correction and never regresses below zero.
    - Confidence emits `estimated` only for in-band corrected drift and `synced` for hard/transition sync.
  </behavior>
  <action>Refine `applyDriftPolicy` in `lyric-sync-engine` to keep the existing transition-sync behavior and implement explicit bounded soft-correction constants (threshold and max correction per sample). Keep policy pure and deterministic (no timers, randomness, or mutable globals). Preserve stale snapshot rejection in `reanchor` while updating drift outputs to satisfy new tests.</action>
  <verify>
    <automated>npm run test -- src/core/sync/lyric-sync-engine.test.ts && npm run build</automated>
  </verify>
  <done>Engine drift policy is deterministic, bounded, and validated for hard reset and soft correction behavior.</done>
</task>

</tasks>

<verification>
Run `npm run test -- src/core/sync/lyric-sync-engine.test.ts && npm run build` and confirm hard-reset/soft-correction assertions plus build gate pass.
</verification>

<success_criteria>
Drift reconciliation is explicit and deterministic: large drift snaps immediately, small drift corrects gradually within bounds, and transition-driven resync remains immediate.
</success_criteria>

<output>
After completion, create `.planning/phases/13-frame-synced-lyric-engine-and-drift-reconciliation/13-03-SUMMARY.md`
</output>
