---
phase: 08-live-playback-and-lyrics-data-flow-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - src/web/app-shell.tsx
  - src/web/app-shell.test.tsx
  - .planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-SIMPLIFIED-CHINESE-VERIFICATION.md
autonomous: true
requirements:
  - CHN-01
  - CHN-02
must_haves:
  truths:
    - Web shell renders Simplified Chinese lyric text for active and next line display.
    - If upstream line data lacks `displayText`, shell still normalizes Chinese glyphs safely.
    - Phase has a reproducible QA checklist mapping CHN requirements to automated and manual evidence.
  artifacts:
    - path: src/web/app-shell.tsx
      provides: Shell-level fallback normalization before panel rendering.
    - path: src/web/app-shell.test.tsx
      provides: Tests for Simplified rendering and mixed-content preservation in shell output.
    - path: .planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-SIMPLIFIED-CHINESE-VERIFICATION.md
      provides: Repeatable verification runbook for CHN-01 and CHN-02.
  key_links:
    - from: src/web/app-shell.tsx
      to: src/core/lyrics/unicode-normalization.ts
      via: normalizeChineseForDisplay fallback when mapping line text
      pattern: normalizeChineseForDisplay\(
    - from: src/web/app-shell.test.tsx
      to: src/web/app-shell.tsx
      via: rendered line text assertions
      pattern: lyrics-status-rail|lyrics-now-playing
---

<objective>
Finalize Chinese normalization at the web rendering layer and publish the phase verification gate artifact.

Purpose: Guarantee user-visible Simplified Chinese output even under partial upstream contracts.
Output: Shell fallback normalization tests plus a phase QA checklist for CHN requirements.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-RESEARCH.md
@.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-VALIDATION.md
@.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-02-SUMMARY.md
@src/web/app-shell.tsx
@src/web/app-shell.test.tsx

<interfaces>
From src/web/app-shell.tsx:
```typescript
const lyricTexts = (resolvedLyrics?.lines ?? []).map((line) => line.displayText ?? line.text);
```

From src/core/lyrics/unicode-normalization.ts:
```typescript
export function normalizeChineseForDisplay(input: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shell tests that fail when Traditional glyphs are rendered</name>
  <files>src/web/app-shell.test.tsx</files>
  <read_first>src/web/app-shell.test.tsx, src/web/app-shell.tsx, src/core/lyrics/unicode-normalization.ts, src/core/lyrics/types.ts</read_first>
  <behavior>
    - Test 1: active/next lyric lines render Simplified Chinese from Traditional source text.
    - Test 2: mixed-content line keeps `ABC 2026!` unchanged while Chinese glyphs normalize.
    - Test 3: `lyrics-now-playing` and `lyrics-status-rail` markers remain present in the same render path.
  </behavior>
  <action>Extend `src/web/app-shell.test.tsx` with deterministic shell tests that mock resolved lyric lines containing Traditional text such as `愛在臺北` and `歡迎光臨 ABC 2026!`. Assert rendered text includes `爱在台北` and `欢迎光临 ABC 2026!`, and does not include the Traditional originals in visible active/next line output. Keep existing marker assertions for `lyrics-now-playing` and `lyrics-status-rail` in these tests.</action>
  <acceptance_criteria>
    - test file includes assertions for rendered `爱在台北` and `欢迎光临 ABC 2026!`
    - test file includes negative assertions that Traditional source strings are not rendered as active/next text
    - test file asserts `lyrics-now-playing` and `lyrics-status-rail` still exist
    - `npm test -- src/web/app-shell.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/app-shell.test.tsx</automated>
  </verify>
  <done>Shell rendering path has automated guardrails against Traditional-glyph regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Add app-shell fallback normalization and publish Chinese QA checklist</name>
  <files>src/web/app-shell.tsx, .planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-SIMPLIFIED-CHINESE-VERIFICATION.md</files>
  <read_first>src/web/app-shell.tsx, src/core/lyrics/unicode-normalization.ts, .planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-VISUAL-CHECKPOINT.md, .planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-VALIDATION.md</read_first>
  <action>In `src/web/app-shell.tsx`, import `normalizeChineseForDisplay` and update line mapping to `line.displayText ?? normalizeChineseForDisplay(line.text)` before panel build. Keep all existing test IDs and state-rail behavior unchanged. Create `.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-SIMPLIFIED-CHINESE-VERIFICATION.md` with sections: prerequisites, automated commands, manual browser checks, and requirement traceability. Include these exact automated commands: `npm test -- src/core/lyrics/unicode-normalization.test.ts src/core/lyrics/lyrics-resolver.test.ts src/web/app-shell.test.tsx` and `npm run build`. Manual checks must explicitly verify synced and plain lines display Simplified Chinese and mixed-content lines preserve non-Chinese text.</action>
  <acceptance_criteria>
    - `src/web/app-shell.tsx` uses `line.displayText ?? normalizeChineseForDisplay(line.text)`
    - `src/web/app-shell.tsx` still contains `data-testid` markers `lyrics-now-playing`, `lyrics-status-rail`, and `shell-layout`
    - verification file exists at exact path and contains both required commands exactly
    - verification file includes a traceability table with rows for `CHN-01` and `CHN-02`
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Web shell guarantees Simplified display fallback and Phase 8 closes with a reproducible CHN verification gate.</done>
</task>

</tasks>

<verification>
- `npm test -- src/web/app-shell.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- CHN-01 is verifiable at the final user-visible shell surface.
- CHN-02 is verifiable via mixed-content rendering checks and QA artifact traceability.
</success_criteria>

<output>
After completion, create `.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-03-SUMMARY.md`
</output>
