---
phase: 08-live-playback-and-lyrics-data-flow-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/web/app-shell.tsx
  - src/core/lyrics/lyrics-resolver.test.ts
  - src/web/app-shell.test.tsx
autonomous: true
requirements:
  - LYR-WEB-01
must_haves:
  truths:
    - Lyrics resolution runs from real now-playing metadata, not placeholder track labels.
    - Remaster/live title variants still resolve through existing matcher normalization behavior.
    - Not-found outcome remains explicit in shell state instead of silent empty rendering.
  artifacts:
    - path: src/web/app-shell.tsx
      provides: Metadata-to-resolver wiring with explicit not-found fallback projection.
    - path: src/core/lyrics/lyrics-resolver.test.ts
      provides: Regression coverage for remaster-title variant acceptance.
    - path: src/web/app-shell.test.tsx
      provides: Shell assertions for not-found fallback and resolver-driven copy.
  key_links:
    - from: src/web/app-shell.tsx
      to: src/core/lyrics/lyrics-resolver.ts
      via: resolveLyricsForTrack metadata input
      pattern: resolveLyricsForTrack\(\{\s*trackId: nowPlaying\.trackId
    - from: src/core/lyrics/lyrics-resolver.test.ts
      to: src/core/lyrics/unicode-normalization.ts
      via: remaster suffix normalization acceptance
      pattern: remaster|live|normalizeForMatch
---

<objective>
Harden lyrics resolution input mapping from web now-playing metadata and lock explicit not-found fallback behavior.

Purpose: Ensure LYR-WEB-01 succeeds for real track titles, including remaster/live variants.
Output: Deterministic resolver inputs from web playback and coverage for remaster-title matching outcomes.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-RESEARCH.md
@.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-01-SUMMARY.md
@src/core/lyrics/lyrics-resolver.ts
@src/core/lyrics/unicode-normalization.ts
@src/web/app-shell.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add remaster-variant resolver regression coverage</name>
  <files>src/core/lyrics/lyrics-resolver.test.ts</files>
  <read_first>src/core/lyrics/lyrics-resolver.test.ts, src/core/lyrics/lyrics-resolver.ts, src/core/lyrics/lyrics-matcher.ts, src/core/lyrics/unicode-normalization.ts</read_first>
  <behavior>
    - Test 1: track title with `- Remastered` variant still selects a usable candidate with base title.
    - Test 2: unresolved variants still return explicit `{ sourceState: "not-found", renderMode: "plain-static" }`.
  </behavior>
  <action>Extend `src/core/lyrics/lyrics-resolver.test.ts` with cases where metadata title includes remaster/live suffixes and provider candidate title omits the suffix. Assert resolver returns `sourceState` of `synced`, `plain`, or `low-confidence` when candidate exists, and returns explicit not-found fallback object when no candidate passes. Keep tests aligned to existing matcher gate rules (title + artist strict normalized match before tie-breaks).</action>
  <acceptance_criteria>
    - resolver tests include at least one remaster/live title variant case
    - not-found fallback assertion checks both `sourceState: "not-found"` and `renderMode: "plain-static"`
    - `npm test -- src/core/lyrics/lyrics-resolver.test.ts src/core/lyrics/lyrics-matcher.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/core/lyrics/lyrics-resolver.test.ts src/core/lyrics/lyrics-matcher.test.ts</automated>
  </verify>
  <done>Resolver contract explicitly guards remaster-title matching and not-found fallback semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Wire richer now-playing metadata into web lyrics resolution path</name>
  <files>src/web/app-shell.tsx, src/web/app-shell.test.tsx</files>
  <read_first>src/web/app-shell.tsx, src/web/app-shell.test.tsx, src/web/auth/now-playing.ts, src/core/lyrics/types.ts</read_first>
  <action>Update lyrics resolution call in `src/web/app-shell.tsx` so `resolveLyricsForTrack` receives metadata fields from now-playing state with explicit keys: `trackId`, `title`, `artist`, `album`, and `durationMs` when available. Keep fallback on resolver failures as `{ sourceState: "not-found", renderMode: "plain-static", lines: [] }`. Add/adjust shell tests in `src/web/app-shell.test.tsx` to assert not-found state copy and retry affordance remain visible when resolution fails after metadata mapping changes.</action>
  <acceptance_criteria>
    - app-shell resolver call includes explicit metadata object with `trackId`, `title`, `artist`, and optional `album`/`durationMs`
    - not-found fallback object remains explicit and unchanged in shape
    - shell tests assert `lyrics-not-found-state` with `Lyrics not found` remains visible on resolver failure
    - `npm test -- src/web/app-shell.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/app-shell.test.tsx</automated>
  </verify>
  <done>LYR-WEB-01 is enforced end-to-end from web metadata mapping through explicit not-found UX fallback.</done>
</task>

</tasks>

<verification>
- `npm test -- src/core/lyrics/lyrics-resolver.test.ts src/core/lyrics/lyrics-matcher.test.ts`
- `npm test -- src/web/app-shell.test.tsx`
</verification>

<success_criteria>
- Web lyrics resolution receives real track metadata with remaster-variant-safe matching behavior.
- Not-found fallback remains explicit and regression-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-02-SUMMARY.md`
</output>
