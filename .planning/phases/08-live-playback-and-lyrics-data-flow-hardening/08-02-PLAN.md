---
phase: 08-live-playback-and-lyrics-data-flow-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/core/lyrics/lyrics-resolver.test.ts
  - src/core/lyrics/lrc-parser.ts
  - src/core/lyrics/plain-lyrics-timing.ts
  - src/ui/lyrics/lyrics-viewport.test.tsx
autonomous: true
requirements:
  - CHN-01
  - CHN-02
must_haves:
  truths:
    - Synced and plain lyric line contracts provide Simplified-ready display output.
    - Non-Chinese content remains unchanged after normalization in resolver output.
    - Downstream consumers can render Simplified Chinese without implementing duplicate conversion logic.
  artifacts:
    - path: src/core/lyrics/lrc-parser.ts
      provides: Synced line output with populated `displayText`.
    - path: src/core/lyrics/plain-lyrics-timing.ts
      provides: Plain line output with populated `displayText`.
    - path: src/core/lyrics/lyrics-resolver.test.ts
      provides: Resolver-level tests proving synced/plain normalization behavior.
    - path: src/ui/lyrics/lyrics-viewport.test.tsx
      provides: Viewport assertions that converted display text remains stable.
  key_links:
    - from: src/core/lyrics/lrc-parser.ts
      to: src/core/lyrics/types.ts
      via: ResolvedLyricLine displayText field
      pattern: displayText:
    - from: src/core/lyrics/plain-lyrics-timing.ts
      to: src/core/lyrics/types.ts
      via: plain-static line generation
      pattern: renderMode: "plain-static"
---

<objective>
Wire Simplified Chinese normalization into resolver-produced lyric line contracts for both synced and plain modes.

Purpose: Make CHN-01 reliable for every consumer by normalizing at line-creation boundaries.
Output: Resolver pipeline emits `displayText` with Simplified Chinese while preserving source `text`.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-RESEARCH.md
@.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-01-SUMMARY.md
@src/core/lyrics/types.ts
@src/core/lyrics/lrc-parser.ts
@src/core/lyrics/plain-lyrics-timing.ts
@src/core/lyrics/lyrics-resolver.ts

<interfaces>
From src/core/lyrics/types.ts:
```typescript
export type ResolvedLyricLine = {
  startMs: number | null;
  text: string;
  displayText?: string;
  dir?: LyricLineDirection;
  renderMode: LyricRenderMode;
  isTimestamped: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing resolver tests for synced and plain Simplified display output</name>
  <files>src/core/lyrics/lyrics-resolver.test.ts</files>
  <read_first>src/core/lyrics/lyrics-resolver.test.ts, src/core/lyrics/lyrics-resolver.ts, src/core/lyrics/lrc-parser.ts, src/core/lyrics/plain-lyrics-timing.ts</read_first>
  <behavior>
    - Test 1: synced Traditional lyric line exposes `displayText` in Simplified Chinese.
    - Test 2: plain Traditional lyric line exposes `displayText` in Simplified Chinese.
    - Test 3: mixed line preserves `ABC 2026` segments unchanged in `displayText`.
  </behavior>
  <action>Extend `src/core/lyrics/lyrics-resolver.test.ts` with explicit candidates containing Traditional Chinese text in both `syncedLyrics` and `plainLyrics`. Add assertions on resolved lines for exact expected values: source `text` remains Traditional (for traceability), while `displayText` is Simplified (for rendering). Include one mixed-content assertion where output keeps `ABC 2026` unchanged.</action>
  <acceptance_criteria>
    - resolver test file asserts `displayText` for at least one synced and one plain case
    - resolver test file asserts original `text` is preserved for those lines
    - resolver test file includes a mixed-content assertion preserving `ABC 2026`
    - `npm test -- src/core/lyrics/lyrics-resolver.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/core/lyrics/lyrics-resolver.test.ts</automated>
  </verify>
  <done>Resolver tests enforce Simplified display output for both render modes and non-Chinese preservation.</done>
</task>

<task type="auto">
  <name>Task 2: Populate displayText at synced/plain line construction boundaries</name>
  <files>src/core/lyrics/lrc-parser.ts, src/core/lyrics/plain-lyrics-timing.ts, src/ui/lyrics/lyrics-viewport.test.tsx</files>
  <read_first>src/core/lyrics/lrc-parser.ts, src/core/lyrics/plain-lyrics-timing.ts, src/core/lyrics/unicode-normalization.ts, src/ui/lyrics/lyrics-viewport.test.tsx</read_first>
  <action>Import `normalizeChineseForDisplay` into `src/core/lyrics/lrc-parser.ts` and `src/core/lyrics/plain-lyrics-timing.ts`. When constructing each `ResolvedLyricLine`, keep `text` as current NFC-normalized content and set `displayText` to `normalizeChineseForDisplay(text)`. In `src/ui/lyrics/lyrics-viewport.test.tsx`, add one assertion confirming viewport still renders expected Simplified `displayText` when line input already carries normalized display text from upstream.</action>
  <acceptance_criteria>
    - both parser files include `displayText: normalizeChineseForDisplay(...)` in returned line objects
    - parser files still set `renderMode` and `isTimestamped` exactly as before
    - viewport test file includes assertion for upstream-provided Simplified `displayText`
    - `npm test -- src/core/lyrics/lyrics-resolver.test.ts src/ui/lyrics/lyrics-viewport.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/core/lyrics/lyrics-resolver.test.ts src/ui/lyrics/lyrics-viewport.test.tsx</automated>
  </verify>
  <done>Resolver output lines now carry Simplified-ready `displayText` for synced and plain flows, reducing renderer duplication.</done>
</task>

</tasks>

<verification>
- `npm test -- src/core/lyrics/lyrics-resolver.test.ts`
- `npm test -- src/core/lyrics/lyrics-resolver.test.ts src/ui/lyrics/lyrics-viewport.test.tsx`
</verification>

<success_criteria>
- CHN-01 is implemented at resolver data-flow boundaries for both synced and plain lines.
- CHN-02 is preserved by explicit mixed-content contract assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-02-SUMMARY.md`
</output>
