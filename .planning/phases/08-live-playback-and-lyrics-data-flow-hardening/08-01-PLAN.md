---
phase: 08-live-playback-and-lyrics-data-flow-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/lyrics/unicode-normalization.test.ts
  - src/core/lyrics/unicode-normalization.ts
autonomous: true
requirements:
  - CHN-02
must_haves:
  truths:
    - Deterministic Traditional-to-Simplified conversion is applied for common lyric characters.
    - Mixed-script lines keep non-Chinese content unchanged while Chinese script is normalized.
    - Conversion behavior is locked by explicit unit tests, not ad hoc manual checks.
  artifacts:
    - path: src/core/lyrics/unicode-normalization.ts
      provides: Deterministic conversion map and normalization helper for display text.
    - path: src/core/lyrics/unicode-normalization.test.ts
      provides: Contract tests for conversion coverage and non-Chinese preservation.
  key_links:
    - from: src/core/lyrics/unicode-normalization.test.ts
      to: src/core/lyrics/unicode-normalization.ts
      via: normalizeChineseForDisplay assertions
      pattern: normalizeChineseForDisplay\(
---

<objective>
Define and lock the Simplified Chinese conversion contract used by lyric rendering.

Purpose: Establish one deterministic source of truth for script normalization before flow wiring.
Output: Expanded conversion map with tests that prove Chinese normalization and non-Chinese preservation.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-RESEARCH.md
@.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-VALIDATION.md
@src/core/lyrics/unicode-normalization.ts
@src/core/lyrics/unicode-normalization.test.ts

<interfaces>
From src/core/lyrics/unicode-normalization.ts:
```typescript
export function normalizeForMatch(input: string): string;
export function isUnusableLyricText(input: string): boolean;
export function getLineDirection(input: string): "rtl" | "ltr" | "auto";
export function normalizeChineseForDisplay(input: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand failing conversion-contract tests for Traditional Chinese inputs</name>
  <files>src/core/lyrics/unicode-normalization.test.ts</files>
  <read_first>src/core/lyrics/unicode-normalization.test.ts, src/core/lyrics/unicode-normalization.ts, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: `normalizeChineseForDisplay("愛在臺北的風裡")` returns `"爱在台北的风里"`.
    - Test 2: `normalizeChineseForDisplay("歡迎光臨 2026! ABC")` returns `"欢迎光临 2026! ABC"`.
    - Test 3: punctuation and emoji are preserved while Chinese glyphs normalize.
  </behavior>
  <action>Add explicit assertions in `src/core/lyrics/unicode-normalization.test.ts` for these exact conversions: `愛->爱`, `臺->台`, `風->风`, `裡->里`, `歡->欢`, `臨->临`. Include one mixed-content case proving `2026! ABC` is unchanged and one punctuation/emoji case (for example `說吧，🎵` -> `说吧，🎵`). Keep existing direction and mojibake tests intact.</action>
  <acceptance_criteria>
    - test file contains assertions for all mappings `愛`, `臺`, `風`, `裡`, `歡`, `臨`, and `說`
    - test file contains at least one mixed-content assertion retaining `2026! ABC`
    - `npm test -- src/core/lyrics/unicode-normalization.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/core/lyrics/unicode-normalization.test.ts</automated>
  </verify>
  <done>Normalization test contract covers deterministic Chinese conversion and mixed-script preservation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic mapping expansion in unicode normalizer</name>
  <files>src/core/lyrics/unicode-normalization.ts</files>
  <read_first>src/core/lyrics/unicode-normalization.ts, src/core/lyrics/unicode-normalization.test.ts</read_first>
  <action>Update `TRADITIONAL_TO_SIMPLIFIED` in `src/core/lyrics/unicode-normalization.ts` to include mappings required by Task 1 assertions (`愛`, `臺`, `風`, `裡`, `歡`, `臨`, `說`) while preserving existing mappings and helper exports. Keep implementation deterministic character-by-character (`[...text].map(...)`) and do not alter `normalizeForMatch`, `isUnusableLyricText`, or `getLineDirection` logic in this plan.</action>
  <acceptance_criteria>
    - `src/core/lyrics/unicode-normalization.ts` map contains keys `愛`, `臺`, `風`, `裡`, `歡`, `臨`, and `說`
    - `normalizeChineseForDisplay` remains exported and still uses character-level mapping
    - `npm test -- src/core/lyrics/unicode-normalization.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/core/lyrics/unicode-normalization.test.ts</automated>
  </verify>
  <done>Core conversion helper deterministically converts required Traditional glyphs to Simplified without touching non-Chinese text.</done>
</task>

</tasks>

<verification>
- `npm test -- src/core/lyrics/unicode-normalization.test.ts`
</verification>

<success_criteria>
- CHN-02 conversion rules are codified with explicit deterministic mappings.
- A failing-to-passing test contract exists for mixed-language preservation behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-01-SUMMARY.md`
</output>
