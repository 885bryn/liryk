---
phase: 08-live-playback-and-lyrics-data-flow-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/auth/now-playing.ts
  - src/web/auth/now-playing.test.ts
  - src/web/app-shell.test.tsx
autonomous: true
requirements:
  - WEB-04
must_haves:
  truths:
    - Connected playback returns non-empty now-playing metadata in the web shell flow.
    - Polling fallback still resolves current track when `/currently-playing` returns 204.
    - Metadata parsing failures do not crash shell polling and instead return explicit null.
  artifacts:
    - path: src/web/auth/now-playing.ts
      provides: Hardened web now-playing payload contract and normalization.
    - path: src/web/auth/now-playing.test.ts
      provides: Contract tests for fallback, malformed payload, and metadata parsing invariants.
    - path: src/web/app-shell.test.tsx
      provides: Shell-level assertion that now-playing metadata can be rendered from parsed payload.
  key_links:
    - from: src/web/auth/now-playing.ts
      to: src/web/app-shell.tsx
      via: fetchWebNowPlaying poll loop
      pattern: fetchWebNowPlaying\(webAuth\.sessionAccessToken\)
    - from: src/web/auth/now-playing.test.ts
      to: src/web/auth/now-playing.ts
      via: parser/fallback contract assertions
      pattern: currently-playing|/me/player|Spotify playback endpoint error
---

<objective>
Harden the web now-playing contract so active connected playback reliably produces renderable track metadata.

Purpose: Reduce false idle/no-track outcomes caused by incomplete Spotify payload parsing.
Output: Stable now-playing parser behavior with explicit fallback and edge-case coverage.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-RESEARCH.md
@.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-VALIDATION.md
@src/web/auth/now-playing.ts
@src/web/app-shell.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand now-playing parser contract for resilient metadata extraction</name>
  <files>src/web/auth/now-playing.ts, src/web/auth/now-playing.test.ts</files>
  <read_first>src/web/auth/now-playing.ts, src/web/auth/now-playing.test.ts, src/infra/spotify/spotify-playback-client.ts, src/core/lyrics/types.ts</read_first>
  <behavior>
    - Test 1: 204 on `/currently-playing` still falls back to `/me/player` and returns track metadata.
    - Test 2: malformed or missing `item.id`/`item.name` returns null without throwing parse exceptions.
    - Test 3: parser keeps existing required fields and supports additional metadata fields needed by lyrics resolution.
  </behavior>
  <action>Update `fetchWebNowPlaying` contract and parser helpers in `src/web/auth/now-playing.ts` so parsed objects include stable values for `trackId`, `title`, `artist`, `isPlaying`, and `progressMs`, plus optional metadata fields (`artists` as string array, `album`, `durationMs`, `capturedAtMs`) when present. Keep 204 fallback from `https://api.spotify.com/v1/me/player/currently-playing` to `https://api.spotify.com/v1/me/player`. Extend `src/web/auth/now-playing.test.ts` with explicit cases for malformed payloads, missing fields, and additional metadata extraction. Do not remove existing endpoint error behavior for non-2xx responses.</action>
  <acceptance_criteria>
    - `src/web/auth/now-playing.ts` still exports `fetchWebNowPlaying` and `WebNowPlaying`
    - parser returns `null` when required track identifiers are missing
    - tests assert fallback from 204 `/currently-playing` to `/me/player`
    - tests assert optional metadata extraction for album/duration/artists when payload includes them
    - `npm test -- src/web/auth/now-playing.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/auth/now-playing.test.ts</automated>
  </verify>
  <done>Now-playing parser behavior is deterministic across valid, fallback, and malformed payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Add shell contract assertion for metadata visibility from now-playing flow</name>
  <files>src/web/app-shell.test.tsx</files>
  <read_first>src/web/app-shell.test.tsx, src/web/app-shell.tsx, src/web/auth/now-playing.ts</read_first>
  <action>Extend `src/web/app-shell.test.tsx` with one deterministic test that mocks a connected runtime and verifies `lyrics-now-playing` renders a non-placeholder title/artist from now-playing data path. Assert exact `data-testid` markers `lyrics-now-playing` and `lyrics-status-rail` remain present while metadata is shown. Keep existing layout and theme tests unchanged.</action>
  <acceptance_criteria>
    - `src/web/app-shell.test.tsx` contains a test covering now-playing metadata visibility tied to shell markers
    - test asserts `lyrics-now-playing` and `lyrics-status-rail` both exist in the rendered shell
    - `npm test -- src/web/app-shell.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/app-shell.test.tsx</automated>
  </verify>
  <done>WEB-04 has explicit shell evidence that parsed now-playing metadata reaches visible lyrics-pane UI.</done>
</task>

</tasks>

<verification>
- `npm test -- src/web/auth/now-playing.test.ts`
- `npm test -- src/web/app-shell.test.tsx`
</verification>

<success_criteria>
- Connected playback metadata contract is hardened against partial Spotify payloads.
- WEB-04 has automated proof at parser and shell levels.
</success_criteria>

<output>
After completion, create `.planning/phases/08-live-playback-and-lyrics-data-flow-hardening/08-01-SUMMARY.md`
</output>
