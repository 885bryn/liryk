---
phase: 03-lyrics-resolution-and-rendered-experience
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/state/playback/live-sync-store.ts
  - src/state/playback/live-sync-store.test.ts
  - src/app/lyrics-resolution-runtime.ts
  - src/app/lyrics-resolution-runtime.test.ts
  - src/app/live-sync-runtime.ts
  - src/app/live-sync-runtime.test.ts
autonomous: true
requirements:
  - LYR-01
  - LYR-03
  - LYR-04
must_haves:
  truths:
    - User sees lyrics resolution start fresh on each track change instead of reusing old results.
    - User sees explicit `Lyrics not found` state when no usable lyrics resolve.
    - User can retry a failed or not-found lookup from the current panel without a full playback reset.
    - User gets synced lyric timelines wired into the existing live-sync engine while plain fallback stays readable.
  artifacts:
    - path: src/app/lyrics-resolution-runtime.ts
      provides: Track-session orchestration for resolve, retry, stale-result suppression, and state projection.
    - path: src/state/playback/live-sync-store.ts
      provides: Canonical resolution fields for source state, retry status, warning state, and resolved lines.
    - path: src/app/live-sync-runtime.ts
      provides: Integration from resolved lyric result into synced timeline availability and plain fallback behavior.
  key_links:
    - from: src/app/lyrics-resolution-runtime.ts
      to: src/core/lyrics/lyrics-resolver.ts
      via: per-track resolve and retry calls
      pattern: resolveLyricsForTrack
    - from: src/app/lyrics-resolution-runtime.ts
      to: src/state/playback/live-sync-store.ts
      via: source-state and status-line updates
      pattern: setStatusLine|setTrack|setResolved
    - from: src/app/live-sync-runtime.ts
      to: src/app/lyrics-resolution-runtime.ts
      via: timeline lookup or resolved-line consumption for active track
      pattern: getTimelineForTrack|resolvedLyrics
---

<objective>
Wire lyrics resolution into runtime state so track changes, retry, and not-found behaviors are deterministic.

Purpose: Make the provider/matching pipeline user-visible by projecting resolved lyrics into the existing live-sync runtime and store contracts without stale-result races.
Output: Extended live-sync store, lyrics-resolution runtime, and live-sync integration tests for synced, plain, retry, and not-found flows.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-CONTEXT.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-RESEARCH.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-02-PLAN.md
@.planning/phases/02-live-playback-sync-engine/02-03-SUMMARY.md

<interfaces>
From `src/app/live-sync-runtime.ts` today:
```typescript
export type LiveSyncRuntimeDependencies = {
  subscribePlayback: (listener: (event: PlaybackRuntimeEvent) => void) => () => void;
  syncEngine: LyricSyncEngine;
  liveSyncStore: LiveSyncStore;
  getTimelineForTrack: (trackId: string) => LyricLine[] | null;
};
```

From `src/app/live-lyrics-presenter.ts` today:
```typescript
export type LiveLyricsViewModel = {
  status: "idle" | "syncing" | "reconnecting" | "no-track" | "unsupported" | "paused";
  statusLine: string;
  confidenceBadge?: string;
  activeLineText?: string;
  nextLineText?: string;
  showLyrics: boolean;
  showReturnToLive: boolean;
};
```
Plan 03 should extend the store/runtime with explicit resolution source state so Plan 04 can project it without guessing from playback-only fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the live sync store with lyrics resolution state and retry metadata</name>
  <files>src/state/playback/live-sync-store.ts, src/state/playback/live-sync-store.test.ts</files>
  <read_first>src/state/playback/live-sync-store.ts, src/state/playback/live-sync-store.test.ts, src/core/lyrics/types.ts, src/app/live-lyrics-presenter.ts</read_first>
  <behavior>
    - Test 1: store can represent `loading`, `synced`, `plain`, `low-confidence`, and `not-found` source states separately from playback state.
    - Test 2: track change resets previous resolved lines, warning state, and retry flags immediately.
    - Test 3: retry-in-progress updates status line without wiping current playback context.
  </behavior>
  <action>Update `src/state/playback/live-sync-store.ts` so `LiveSyncUiState` adds explicit fields for resolved lyric lines, `lyricsSourceState`, `lyricsRenderMode`, `lyricsWarning`, `retryAvailable`, and `retryInFlight`. Keep existing playback fields intact. Add setter methods such as `setResolvedLyrics(...)`, `setLyricsSourceState(...)`, `setLyricsRetryState(...)`, and `clearResolvedLyrics()` so runtime code can project exact states instead of reconstructing them from partial booleans. Update tests to prove reset-on-track-change and non-blocking retry status behavior.</action>
  <acceptance_criteria>
    - `src/state/playback/live-sync-store.ts` contains `lyricsSourceState`
    - `src/state/playback/live-sync-store.ts` contains `retryInFlight`
    - `src/state/playback/live-sync-store.ts` exports a setter for resolved lyrics state
    - `src/state/playback/live-sync-store.test.ts` covers track-reset and retry-state cases
    - `npm test -- src/state/playback/live-sync-store.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/state/playback/live-sync-store.test.ts</automated>
  </verify>
  <done>The canonical UI store can express all Phase 3 lyrics result states without overloading playback-only fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement track-session lyrics resolution runtime and wire synced timelines into live sync</name>
  <files>src/app/lyrics-resolution-runtime.ts, src/app/lyrics-resolution-runtime.test.ts, src/app/live-sync-runtime.ts, src/app/live-sync-runtime.test.ts</files>
  <read_first>src/app/live-sync-runtime.ts, src/app/live-sync-runtime.test.ts, src/app/playback-runtime.ts, src/core/lyrics/lyrics-resolver.ts, src/state/playback/live-sync-store.ts</read_first>
  <behavior>
    - Test 1: new track IDs start a fresh resolve session, clear stale not-found/old lines, and ignore late results from older sessions.
    - Test 2: synced results feed `getTimelineForTrack`/timeline access for the active track while plain results keep readable lines but no active highlight timeline.
    - Test 3: unresolved tracks project explicit `Lyrics not found` with retry available.
    - Test 4: retry triggers a new resolve session and shows inline transient retry status.
  </behavior>
  <action>Create `src/app/lyrics-resolution-runtime.ts` to subscribe to playback events, detect track changes by `trackId`, call `resolveLyricsForTrack`, and guard all async completions with an incrementing session token so older responses cannot overwrite newer track state. Update `src/app/live-sync-runtime.ts` so synced `ResolvedLyrics` lines become the active timeline source for the sync engine, while `plain-static` results clear active/next line indices and preserve readable lines for the presenter. On not-found or unusable outcomes, set `lyricsSourceState: "not-found"`, `statusLine: "Lyrics not found"`, and `retryAvailable: true`; on retry, set `retryInFlight: true` plus a transient inline status like `Retrying lyrics lookup...` without replacing the rest of the panel state.</action>
  <acceptance_criteria>
    - `src/app/lyrics-resolution-runtime.ts` exports `createLyricsResolutionRuntime`
    - `src/app/lyrics-resolution-runtime.ts` contains a session or sequence guard for stale async results
    - `src/app/live-sync-runtime.ts` contains logic for synced timeline vs `plain-static` fallback handling
    - `src/app/lyrics-resolution-runtime.test.ts` covers stale-result suppression, not-found, and retry flow
    - `npm test -- src/app/lyrics-resolution-runtime.test.ts src/app/live-sync-runtime.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/app/lyrics-resolution-runtime.test.ts src/app/live-sync-runtime.test.ts</automated>
  </verify>
  <done>Runtime state now resolves and projects synced/plain/not-found results deterministically per track, with retry and stale-result protection.</done>
</task>

</tasks>

<verification>
Run store and runtime tests to confirm reset-on-track-change, stale-result suppression, not-found projection, retry behavior, and synced/plain runtime wiring:
- `npm test -- src/state/playback/live-sync-store.test.ts src/app/lyrics-resolution-runtime.test.ts src/app/live-sync-runtime.test.ts`
</verification>

<success_criteria>
- LYR-01 resolution is triggered from active Spotify track changes rather than ad hoc UI actions.
- LYR-03 synced results integrate with existing line-sync runtime while plain fallback stays readable and non-deceptive.
- LYR-04 explicit not-found and retry behavior are first-class runtime states, not inferred UI hacks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-lyrics-resolution-and-rendered-experience/03-03-SUMMARY.md`
</output>
