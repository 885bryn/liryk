---
phase: 03-lyrics-resolution-and-rendered-experience
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/lyrics/types.ts
  - src/core/lyrics/lrc-parser.ts
  - src/core/lyrics/lrc-parser.test.ts
  - src/core/lyrics/plain-lyrics-timing.ts
  - src/core/lyrics/plain-lyrics-timing.test.ts
  - src/core/lyrics/unicode-normalization.ts
  - src/core/lyrics/unicode-normalization.test.ts
autonomous: true
requirements:
  - LYR-03
  - I18N-01
must_haves:
  truths:
    - User gets a canonical lyric data shape whether the provider returns timestamped or plain lyrics.
    - User sees multilingual text preserved and readable instead of garbled or silently corrupted.
    - User gets plain-lyrics fallback prepared in a deterministic form when timestamps are absent.
  artifacts:
    - path: src/core/lyrics/types.ts
      provides: Shared contracts for provider candidates, resolved lyrics, line display metadata, and source states.
    - path: src/core/lyrics/lrc-parser.ts
      provides: Timestamped lyric parsing from provider `syncedLyrics` payloads.
    - path: src/core/lyrics/unicode-normalization.ts
      provides: Unicode normalization, mojibake detection, per-line direction helpers, and Simplified Chinese render transform.
    - path: src/core/lyrics/plain-lyrics-timing.ts
      provides: Plain-lyrics fallback line preparation for static or estimated display.
  key_links:
    - from: src/core/lyrics/lrc-parser.ts
      to: src/core/sync/lyric-timeline.ts
      via: parsed timestamp lines shaped for sync engine consumption
      pattern: startMs|LyricLine
    - from: src/core/lyrics/unicode-normalization.ts
      to: src/ui/lyrics/lyrics-viewport.tsx
      via: direction and display text metadata
      pattern: direction|displayText|simplified
---

<objective>
Create the core lyric domain contracts and text-preparation primitives for Phase 3.

Purpose: Give later plans a deterministic, testable foundation for synced parsing, plain fallback, and multilingual-safe rendering before any provider or runtime wiring is added.
Output: Shared lyric types, Unicode-safe text helpers, LRC parsing, and plain fallback preparation with unit tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-CONTEXT.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-RESEARCH.md

<interfaces>
From `src/core/sync/lyric-timeline.ts` consumers already expect timeline-style line data compatible with line-index lookup.

From `src/state/playback/live-sync-store.ts`:
```typescript
export type LiveSyncUiState = {
  playbackState: "idle" | "playing" | "paused" | "unavailable";
  activeLineIndex: number | null;
  nextLineIndex: number | null;
  confidence: SyncConfidence | "static";
  trackId: string | null;
  statusLine: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define lyric contracts, parse synced LRC, and prepare plain fallback lines</name>
  <files>src/core/lyrics/types.ts, src/core/lyrics/lrc-parser.ts, src/core/lyrics/lrc-parser.test.ts, src/core/lyrics/plain-lyrics-timing.ts, src/core/lyrics/plain-lyrics-timing.test.ts</files>
  <read_first>src/core/sync/lyric-timeline.ts, src/core/sync/lyric-timeline.test.ts, src/app/live-sync-runtime.ts, src/state/playback/live-sync-store.ts</read_first>
  <behavior>
    - Test 1: `parseLrc` converts `[mm:ss.xx]` and `[mm:ss.xxx]` lines into ascending `startMs` values with normalized text.
    - Test 2: blank timestamped lines are preserved, invalid timestamp rows are skipped, and duplicate timestamps stay stable in source order.
    - Test 3: plain fallback builder splits multiline `plainLyrics` into readable lines and marks render mode as `plain-static` with no active-highlight requirement.
  </behavior>
  <action>Create `src/core/lyrics/types.ts` with exported contracts for `LyricTrackMetadata`, `ProviderLyricCandidate`, `ResolvedLyrics`, `ResolvedLyricLine`, `LyricsSourceState`, and `LyricRenderMode`. Implement `parseLrc(input: string): ResolvedLyricLine[]` in `src/core/lyrics/lrc-parser.ts` that accepts LRCLIB-style `[mm:ss.xx]` and `[mm:ss.xxx]` timestamps, preserves empty lyric lines, normalizes text to NFC, and returns ascending `startMs` rows ready for `src/core/sync/lyric-timeline.ts`. Implement `buildPlainLyricsLines(input)` in `src/core/lyrics/plain-lyrics-timing.ts` that splits `plainLyrics` on newlines, trims only trailing carriage returns, preserves visible punctuation/glyphs, and returns line objects with `renderMode: "plain-static"`, `startMs: null`, and `isTimestamped: false`. Do not invent active-line timing in this plan.</action>
  <acceptance_criteria>
    - `src/core/lyrics/types.ts` exports `type LyricsSourceState = "synced" | "plain" | "low-confidence" | "not-found" | "loading"`
    - `src/core/lyrics/lrc-parser.ts` contains `export function parseLrc(`
    - `src/core/lyrics/plain-lyrics-timing.ts` contains `renderMode: "plain-static"`
    - `src/core/lyrics/lrc-parser.test.ts` exits 0
    - `src/core/lyrics/plain-lyrics-timing.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/core/lyrics/lrc-parser.test.ts src/core/lyrics/plain-lyrics-timing.test.ts</automated>
  </verify>
  <done>Phase 3 has shared lyric data contracts and deterministic synced/plain line preparation covered by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Unicode-safe normalization, mojibake detection, and direction helpers</name>
  <files>src/core/lyrics/unicode-normalization.ts, src/core/lyrics/unicode-normalization.test.ts</files>
  <read_first>src/ui/lyrics/lyrics-viewport.tsx, .planning/phases/03-lyrics-resolution-and-rendered-experience/03-CONTEXT.md, .planning/research/PITFALLS.md</read_first>
  <behavior>
    - Test 1: matching inputs normalize visually identical composed/decomposed strings to the same comparison value.
    - Test 2: Arabic and mixed-direction lines receive deterministic `dir` metadata suitable for bidi-safe rendering.
    - Test 3: obviously garbled payloads (for example replacement-character-heavy or mojibake patterns) are marked unusable.
    - Test 4: Chinese render normalization returns Simplified display output while keeping script-insensitive comparison helpers available.
  </behavior>
  <action>Implement `src/core/lyrics/unicode-normalization.ts` with exported helpers `normalizeForMatch`, `isUnusableLyricText`, `getLineDirection`, and `normalizeChineseForDisplay`. `normalizeForMatch` must NFC-normalize, lowercase, collapse repeated whitespace, and strip only minor version suffix wrappers used by the matcher later (`live`, `remaster`, `clean`, `explicit`, bracketed or hyphen suffix forms). `isUnusableLyricText` must flag empty-after-trim text, replacement-character-heavy text, and common mojibake markers without rejecting valid Arabic, CJK, or Korean glyphs. `getLineDirection` must return `"rtl"`, `"ltr"`, or `"auto"` based on script presence. `normalizeChineseForDisplay` must convert Traditional variants to Simplified display output for this milestone while keeping original non-Chinese glyphs intact.</action>
  <acceptance_criteria>
    - `src/core/lyrics/unicode-normalization.ts` exports `normalizeForMatch`
    - `src/core/lyrics/unicode-normalization.ts` exports `getLineDirection`
    - `src/core/lyrics/unicode-normalization.ts` contains `normalizeChineseForDisplay`
    - `src/core/lyrics/unicode-normalization.test.ts` covers Arabic, Korean, and Chinese fixture cases
    - `npm test -- src/core/lyrics/unicode-normalization.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/core/lyrics/unicode-normalization.test.ts</automated>
  </verify>
  <done>Multilingual lyric text can be normalized for matching and safely projected for rendering without silently accepting garbled payloads.</done>
</task>

</tasks>

<verification>
Run the new Phase 3 domain tests to confirm synced parsing, plain fallback shaping, normalization, and multilingual direction logic:
- `npm test -- src/core/lyrics/lrc-parser.test.ts src/core/lyrics/plain-lyrics-timing.test.ts src/core/lyrics/unicode-normalization.test.ts`
</verification>

<success_criteria>
- LYR-03 has canonical synced and plain lyric line shapes available for later runtime wiring.
- I18N-01 groundwork exists for NFC-safe matching, direction-aware rendering, and garbled-text rejection.
- Later plans can consume explicit contracts instead of rediscovering lyric text semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/03-lyrics-resolution-and-rendered-experience/03-01-SUMMARY.md`
</output>
