---
phase: 03-lyrics-resolution-and-rendered-experience
plan: "04"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - src/app/live-lyrics-presenter.ts
  - src/app/live-lyrics-presenter.test.ts
  - src/ui/lyrics/live-lyrics-panel.tsx
  - src/ui/lyrics/live-lyrics-panel.test.tsx
  - src/ui/lyrics/lyrics-viewport.tsx
  - src/ui/lyrics/lyrics-viewport.test.tsx
autonomous: true
requirements:
  - LYR-04
  - I18N-01
  - UI-01
must_haves:
  truths:
    - User sees explicit source state in the lyrics panel: synced, plain fallback, low-confidence warning, or not found.
    - User sees one clear retry action when lookup fails.
    - User can read Arabic, CJK, and Korean lyrics without forced horizontal scrolling or broken direction handling.
    - User sees plain fallback as static readable text with no misleading active-line highlight.
    - User sees the milestone surface stay within existing panel/viewport patterns and shadcn/ui-style affordances.
  artifacts:
    - path: src/app/live-lyrics-presenter.ts
      provides: Presenter mapping from runtime resolution state to user-facing copy, badges, and actions.
    - path: src/ui/lyrics/live-lyrics-panel.tsx
      provides: Panel model for retry action, warning badge, and source-state-aware status output.
    - path: src/ui/lyrics/lyrics-viewport.tsx
      provides: Viewport model for direction-aware multilingual line rendering and no-highlight plain mode.
  key_links:
    - from: src/app/live-lyrics-presenter.ts
      to: src/state/playback/live-sync-store.ts
      via: source-state-aware presenter mapping
      pattern: lyricsSourceState|retryInFlight|resolvedLyrics
    - from: src/ui/lyrics/live-lyrics-panel.tsx
      to: src/app/live-lyrics-presenter.ts
      via: panel model and retry action projection
      pattern: buildLiveLyricsViewModel
    - from: src/ui/lyrics/lyrics-viewport.tsx
      to: src/core/lyrics/unicode-normalization.ts
      via: per-line direction/display text metadata
      pattern: getLineDirection|normalizeChineseForDisplay
---

<objective>
Deliver the user-visible fallback and multilingual rendering experience for Phase 3.

Purpose: Turn runtime resolution state into readable panel and viewport behavior that makes source quality explicit, keeps retry simple, and honors the project's existing shadcn/ui-oriented surface patterns.
Output: Updated presenter, panel model, and viewport model with tests for synced/plain/not-found/low-confidence and multilingual rendering states.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-CONTEXT.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-RESEARCH.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-03-PLAN.md
@.planning/phases/02-live-playback-sync-engine/02-03-SUMMARY.md

<interfaces>
From `src/app/live-lyrics-presenter.ts` today:
```typescript
export type LiveLyricsViewModel = {
  status: "idle" | "syncing" | "reconnecting" | "no-track" | "unsupported" | "paused";
  statusLine: string;
  confidenceBadge?: string;
  activeLineText?: string;
  nextLineText?: string;
  showLyrics: boolean;
  showReturnToLive: boolean;
};
```

From `src/ui/lyrics/live-lyrics-panel.tsx` today:
```typescript
export type LiveLyricsPanelModel = LiveLyricsViewModel & {
  title: string;
  trackLabel: string;
};
```
Plan 04 should extend these contracts to carry explicit source-state, retry-action, warning badge, and direction-aware line metadata rather than inferring them from strings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update presenter and panel model for explicit fallback, warning, and retry states</name>
  <files>src/app/live-lyrics-presenter.ts, src/app/live-lyrics-presenter.test.ts, src/ui/lyrics/live-lyrics-panel.tsx, src/ui/lyrics/live-lyrics-panel.test.tsx</files>
  <read_first>src/app/live-lyrics-presenter.ts, src/app/live-lyrics-presenter.test.ts, src/ui/lyrics/live-lyrics-panel.tsx, src/state/playback/live-sync-store.ts, .planning/phases/03-lyrics-resolution-and-rendered-experience/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `not-found` projects explicit "Lyrics not found" copy plus one primary retry action.
    - Test 2: `low-confidence` keeps lyrics visible and adds a subtle warning badge instead of hiding lyrics.
    - Test 3: `plain` fallback shows readable lines with no synced confidence badge and no misleading active-line assumptions.
    - Test 4: retry-in-flight updates only the inline status line, not the entire panel state.
  </behavior>
  <action>Update `src/app/live-lyrics-presenter.ts` so the view-model carries exact fields for `sourceState`, `warningBadge`, `primaryActionLabel`, `showPrimaryAction`, `renderMode`, and explicit status copy for `loading`, `synced`, `plain`, `low-confidence`, and `not-found`. Keep existing playback statuses intact for idle/no-track/reconnecting/paused flows. Then update `src/ui/lyrics/live-lyrics-panel.tsx` to project those presenter fields into the panel model using existing panel semantics and shadcn/ui-oriented affordances: title, track label, status badge, primary retry button label, and warning copy. Do not add new navigation, settings, or multiple action buttons.</action>
  <acceptance_criteria>
    - `src/app/live-lyrics-presenter.ts` contains `sourceState`
    - `src/app/live-lyrics-presenter.ts` contains `Lyrics not found`
    - `src/ui/lyrics/live-lyrics-panel.tsx` contains a primary retry action field or handler
    - `src/ui/lyrics/live-lyrics-panel.test.tsx` covers not-found, low-confidence, and plain-fallback cases
    - `npm test -- src/app/live-lyrics-presenter.test.ts src/ui/lyrics/live-lyrics-panel.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/app/live-lyrics-presenter.test.ts src/ui/lyrics/live-lyrics-panel.test.tsx</automated>
  </verify>
  <done>Panel-facing view models explicitly represent all Phase 3 source and recovery states with one clear retry action.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make viewport rendering direction-aware and plain-fallback-safe</name>
  <files>src/ui/lyrics/lyrics-viewport.tsx, src/ui/lyrics/lyrics-viewport.test.tsx</files>
  <read_first>src/ui/lyrics/lyrics-viewport.tsx, src/ui/lyrics/use-auto-scroll-controller.ts, src/core/lyrics/unicode-normalization.ts, src/state/playback/live-sync-store.ts</read_first>
  <behavior>
    - Test 1: synced mode preserves active/next line behavior and auto-scroll contracts from Phase 2.
    - Test 2: plain fallback mode renders all lines as static readable text with no active-line highlight and no return-to-live affordance.
    - Test 3: Arabic, Korean, and CJK fixtures carry direction/display metadata that avoids forced horizontal scroll assumptions.
    - Test 4: Chinese display text is normalized to Simplified for render output in this milestone.
  </behavior>
  <action>Update `src/ui/lyrics/lyrics-viewport.tsx` so it consumes resolved lyric lines with display text and direction metadata instead of raw strings only. In synced mode, keep existing center-biased viewport and return-to-live behavior. In `plain-static` mode, bypass active/next highlight logic, return all readable lines as static entries, suppress `showReturnToLive`, and preserve natural wrapping semantics. Each visible line model must include `text`, `dir`, and an isolation-friendly display field so the eventual renderer can map directly to shadcn/ui `ScrollArea` content without adding another transformation layer.</action>
  <acceptance_criteria>
    - `src/ui/lyrics/lyrics-viewport.tsx` contains `plain-static`
    - `src/ui/lyrics/lyrics-viewport.tsx` contains line metadata including `dir`
    - `src/ui/lyrics/lyrics-viewport.test.tsx` covers Arabic, Korean, and Chinese fixture lines
    - `src/ui/lyrics/lyrics-viewport.test.tsx` proves plain mode disables active highlight and return-to-live
    - `npm test -- src/ui/lyrics/lyrics-viewport.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/ui/lyrics/lyrics-viewport.test.tsx</automated>
  </verify>
  <done>Viewport output now supports multilingual readable rendering and an honest plain fallback mode without violating Phase 2 auto-scroll behavior.</done>
</task>

</tasks>

<verification>
Run presenter and viewport tests to confirm explicit source states, retry behavior, low-confidence warnings, plain-fallback display, and multilingual line metadata:
- `npm test -- src/app/live-lyrics-presenter.test.ts src/ui/lyrics/live-lyrics-panel.test.tsx src/ui/lyrics/lyrics-viewport.test.tsx`
</verification>

<success_criteria>
- LYR-04 user-visible not-found and retry states are explicit and deterministic.
- I18N-01 multilingual line output includes direction-aware metadata and preserves readable scripts without fake highlighting in plain mode.
- UI-01 remains within the repo's current panel/viewport/shadcn-ui-oriented surface conventions and adds no alternate UI system.
</success_criteria>

<output>
After completion, create `.planning/phases/03-lyrics-resolution-and-rendered-experience/03-04-SUMMARY.md`
</output>
