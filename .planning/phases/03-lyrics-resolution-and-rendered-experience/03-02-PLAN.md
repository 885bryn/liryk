---
phase: 03-lyrics-resolution-and-rendered-experience
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/infra/providers/lrclib-client.ts
  - src/infra/providers/lrclib-client.test.ts
  - src/core/lyrics/lyrics-matcher.ts
  - src/core/lyrics/lyrics-matcher.test.ts
  - src/core/lyrics/lyrics-resolver.ts
  - src/core/lyrics/lyrics-resolver.test.ts
autonomous: true
requirements:
  - LYR-01
  - LYR-02
  - LYR-03
must_haves:
  truths:
    - User gets lyrics fetched from a real provider for the active Spotify track metadata.
    - User gets one best-match lyric result chosen deterministically instead of an arbitrary provider hit.
    - User gets timestamped lyrics when a close synced candidate exists and plain lyrics otherwise.
    - User sees low-confidence status only when the best available candidate is acceptable but risky.
  artifacts:
    - path: src/infra/providers/lrclib-client.ts
      provides: LRCLIB HTTP adapter returning normalized provider candidates.
    - path: src/core/lyrics/lyrics-matcher.ts
      provides: Candidate scoring and confidence classification.
    - path: src/core/lyrics/lyrics-resolver.ts
      provides: End-to-end provider-candidate selection into one canonical resolved result.
  key_links:
    - from: src/core/lyrics/lyrics-resolver.ts
      to: src/infra/providers/lrclib-client.ts
      via: metadata lookup request and candidate normalization
      pattern: searchLyrics|getLyrics|fetch
    - from: src/core/lyrics/lyrics-resolver.ts
      to: src/core/lyrics/lyrics-matcher.ts
      via: candidate scoring and best-result selection
      pattern: score|selectBest|confidence
---

<objective>
Build the provider-backed lyrics resolution pipeline and deterministic candidate selection rules.

Purpose: Turn raw LRCLIB responses into one canonical lyrics result that preserves the phase's strict-match, synced-first, and low-confidence behaviors.
Output: LRCLIB client, candidate matcher, and resolver modules with tests covering remaster/live/clean variants and timestamp/plain fallback decisions.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-CONTEXT.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-RESEARCH.md
@.planning/phases/03-lyrics-resolution-and-rendered-experience/03-01-PLAN.md

<interfaces>
From `src/core/lyrics/types.ts` created in Plan 01, the resolver layer must consume shared contracts instead of introducing a second lyric shape.

From Phase 2 runtime expectations in `src/app/live-sync-runtime.ts`:
```typescript
getTimelineForTrack: (trackId: string) => LyricLine[] | null;
```
Plan 02 should produce resolved outputs that can later be converted into this timeline input for synced lyrics while still preserving `plain-static` fallback information.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement LRCLIB adapter with normalized candidate output</name>
  <files>src/infra/providers/lrclib-client.ts, src/infra/providers/lrclib-client.test.ts</files>
  <read_first>package.json, src/infra/spotify/spotify-playback-client.ts, .planning/phases/03-lyrics-resolution-and-rendered-experience/03-RESEARCH.md, src/core/lyrics/types.ts, src/core/lyrics/unicode-normalization.ts</read_first>
  <behavior>
    - Test 1: exact metadata lookup hits LRCLIB `api/get` and maps `plainLyrics` and `syncedLyrics` into one normalized candidate shape.
    - Test 2: empty or unusable lyric payloads are filtered out before resolver scoring.
    - Test 3: adapter falls back to search results when direct get lookup misses and returns an array of candidates, not just one raw payload.
  </behavior>
  <action>Create `src/infra/providers/lrclib-client.ts` with an exported factory `createLrclibClient({ fetchFn, baseUrl? })` and methods `getByMetadata(metadata)` plus `searchByMetadata(metadata)`. Query `https://lrclib.net/api/get` with `track_name`, `artist_name`, `album_name`, and rounded `duration`; if that misses, query `https://lrclib.net/api/search` with the same metadata. Normalize each payload into the shared `ProviderLyricCandidate` contract with provider name `lrclib`, provider lyric id, raw title/artist/album/duration fields, `plainLyrics`, optional `syncedLyrics`, and a boolean for whether the payload is usable after `isUnusableLyricText` checks. Keep this module fetch-only; do not score candidates here.</action>
  <acceptance_criteria>
    - `src/infra/providers/lrclib-client.ts` exports `createLrclibClient`
    - `src/infra/providers/lrclib-client.ts` contains `/api/get`
    - `src/infra/providers/lrclib-client.ts` contains `/api/search`
    - `src/infra/providers/lrclib-client.test.ts` covers direct-hit, search-fallback, and unusable-payload cases
    - `npm test -- src/infra/providers/lrclib-client.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/infra/providers/lrclib-client.test.ts</automated>
  </verify>
  <done>Phase 3 has a real provider adapter that returns normalized lyric candidates suitable for deterministic scoring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Score candidates and resolve one canonical lyrics result</name>
  <files>src/core/lyrics/lyrics-matcher.ts, src/core/lyrics/lyrics-matcher.test.ts, src/core/lyrics/lyrics-resolver.ts, src/core/lyrics/lyrics-resolver.test.ts</files>
  <read_first>src/core/lyrics/types.ts, src/core/lyrics/lrc-parser.ts, src/core/lyrics/plain-lyrics-timing.ts, src/core/lyrics/unicode-normalization.ts, src/infra/providers/lrclib-client.ts, .planning/research/PITFALLS.md</read_first>
  <behavior>
    - Test 1: strict title + artist alignment rejects clearly wrong synced candidates even if timestamps exist.
    - Test 2: close candidates prefer synced lyrics first, then metadata quality, producing `low-confidence` only when within the acceptance threshold.
    - Test 3: plain-only results resolve to `sourceState: "plain"` with `renderMode: "plain-static"`.
    - Test 4: no acceptable candidates resolves to an explicit not-found result rather than `null`.
  </behavior>
  <action>Create `src/core/lyrics/lyrics-matcher.ts` with exported functions to score normalized candidates using strict `normalizeForMatch(title)` + `normalizeForMatch(artist)` baseline, minor suffix tolerance for title variants, duration penalty bands, and synced-first tie-breaks only among acceptable matches. Then create `src/core/lyrics/lyrics-resolver.ts` with `resolveLyricsForTrack(metadata, client)` that requests candidates from the LRCLIB client, filters unusable rows, parses `syncedLyrics` through `parseLrc`, falls back to `buildPlainLyricsLines` when timestamps are absent, and returns one `ResolvedLyrics` object with `sourceState` set to `synced`, `plain`, `low-confidence`, or `not-found`. Use exact copy-safe state names so Plan 03 can project them directly; do not emit ambiguous booleans like `isMaybeWrong`.</action>
  <acceptance_criteria>
    - `src/core/lyrics/lyrics-matcher.ts` exports `scoreCandidate`
    - `src/core/lyrics/lyrics-resolver.ts` exports `resolveLyricsForTrack`
    - `src/core/lyrics/lyrics-resolver.ts` contains `sourceState: "not-found"` or equivalent explicit not-found state assignment
    - `src/core/lyrics/lyrics-matcher.test.ts` covers remaster, live, and clean/explicit variant cases
    - `npm test -- src/core/lyrics/lyrics-matcher.test.ts src/core/lyrics/lyrics-resolver.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/core/lyrics/lyrics-matcher.test.ts src/core/lyrics/lyrics-resolver.test.ts</automated>
  </verify>
  <done>Lyrics resolution produces one deterministic result with the phase's strict-match, synced-first, plain-fallback, and explicit not-found behaviors.</done>
</task>

</tasks>

<verification>
Run provider and resolution tests to validate candidate normalization, strict metadata scoring, low-confidence handling, and synced/plain selection:
- `npm test -- src/infra/providers/lrclib-client.test.ts src/core/lyrics/lyrics-matcher.test.ts src/core/lyrics/lyrics-resolver.test.ts`
</verification>

<success_criteria>
- LYR-01 lyrics are fetched through a dedicated provider adapter instead of ad hoc UI fetches.
- LYR-02 best-match selection is explicit, deterministic, and covered by variant-heavy tests.
- LYR-03 synced lyrics win when acceptable; plain lyrics remain available as a first-class fallback mode.
</success_criteria>

<output>
After completion, create `.planning/phases/03-lyrics-resolution-and-rendered-experience/03-02-SUMMARY.md`
</output>
