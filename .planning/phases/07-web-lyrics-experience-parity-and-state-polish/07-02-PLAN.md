---
phase: 07-web-lyrics-experience-parity-and-state-polish
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/web/app-shell.tsx
  - src/web/app-shell.test.tsx
autonomous: true
requirements:
  - WEB-03
  - UI-04
must_haves:
  truths:
    - User can see now-playing metadata in the lyrics pane within the web shell.
    - User sees distinct loading, empty, and lyrics-not-found states with cohesive visual treatment.
    - User sees state transitions in one stable inline status-rail location without shell layout regressions.
  artifacts:
    - path: src/web/app-shell.tsx
      provides: Web lyrics pane rendering now-playing metadata and parity state visuals.
    - path: src/web/app-shell.test.tsx
      provides: Component contract tests for parity states and transition-safe rail placement.
  key_links:
    - from: src/web/app-shell.tsx
      to: src/ui/lyrics/live-lyrics-panel.tsx
      via: createLiveLyricsPanelBuilder consumption
      pattern: createLiveLyricsPanelBuilder
    - from: src/web/app-shell.test.tsx
      to: src/web/app-shell.tsx
      via: state-specific assertions
      pattern: lyrics-status-rail
---

<objective>
Integrate the phase-07 lyrics parity contract into the web shell with explicit state rendering behavior.

Purpose: Deliver user-visible WEB-03 and UI-04 outcomes in the browser shell while preserving Phase 6 layout and visual-system contracts.
Output: App shell lyrics pane with now-playing metadata, stable inline status rail, and explicit loading/empty/not-found views backed by tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-CONTEXT.md
@.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-RESEARCH.md
@.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-01-PLAN.md
@.planning/phases/06-responsive-layout-and-visual-system/06-03-SUMMARY.md

<interfaces>
From `src/ui/lyrics/live-lyrics-panel.tsx` (after Plan 01):
```typescript
export type LiveLyricsPanelModel = LiveLyricsViewModel & {
  title: string;
  trackLabel: string;
  nowPlayingTitle: string;
  nowPlayingArtist: string;
  isNowPlayingKnown: boolean;
  stateRailMessage: string;
  stateRailVariant: "idle" | "info" | "warning";
};
```

From `src/web/app-shell.tsx`:
```typescript
export function AppShell(input?: AppShellProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing shell tests for metadata and explicit parity states</name>
  <files>src/web/app-shell.test.tsx</files>
  <read_first>src/web/app-shell.test.tsx, src/web/app-shell.tsx, src/ui/lyrics/live-lyrics-panel.tsx, .planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-CONTEXT.md</read_first>
  <behavior>
    - Test 1: app shell renders now-playing title and artist text in lyrics pane metadata region.
    - Test 2: app shell renders distinct explicit copy for loading/syncing, empty/no-track, and not-found states.
    - Test 3: app shell uses one stable `data-testid="lyrics-status-rail"` container regardless of state variant.
  </behavior>
  <action>Extend `src/web/app-shell.test.tsx` with new expectations that target concrete markers: `data-testid="lyrics-now-playing"`, `data-testid="lyrics-status-rail"`, `data-testid="lyrics-empty-state"`, and `data-testid="lyrics-not-found-state"`. Add cases that render `AppShell` with deterministic props/state fixtures so each variant can be asserted by exact text and test id. Keep existing responsive/layout and theme-toggle assertions unchanged. Run tests to confirm RED before implementing shell updates.</action>
  <acceptance_criteria>
    - `src/web/app-shell.test.tsx` contains tests asserting `lyrics-now-playing` and `lyrics-status-rail`
    - `src/web/app-shell.test.tsx` contains distinct assertions for loading/syncing, empty/no-track, and not-found state copy
    - `src/web/app-shell.test.tsx` keeps existing split-layout assertions (`lg:grid-cols-5`, `lg:col-span-3`, `lg:col-span-2`)
    - `npm test -- src/web/app-shell.test.tsx` exits non-zero before shell implementation updates
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/app-shell.test.tsx</automated>
  </verify>
  <done>Failing shell tests precisely define WEB-03/UI-04 parity rendering expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement state-driven lyrics pane rendering with stable inline status rail</name>
  <files>src/web/app-shell.tsx, src/web/app-shell.test.tsx</files>
  <read_first>src/web/app-shell.tsx, src/web/app-shell.test.tsx, src/ui/lyrics/live-lyrics-panel.tsx, src/state/playback/live-sync-store.ts</read_first>
  <behavior>
    - Test 1: now-playing metadata is rendered from panel model contract and defaults to explicit fallback labels.
    - Test 2: loading/empty/not-found each render dedicated visible containers with deterministic copy.
    - Test 3: status rail remains in one inline location and changes variant classes without relocating.
  </behavior>
  <action>Refactor `src/web/app-shell.tsx` lyrics pane to consume a `LiveLyricsPanelModel` instance produced by `createLiveLyricsPanelBuilder` with deterministic local fixture inputs (idle baseline for default render, plus optional `lyricsPanelOverride` prop for test-driven state injection). Render now-playing metadata inside one container `data-testid="lyrics-now-playing"` with exact fallback text `No active track` and `Spotify`. Render one inline rail container `data-testid="lyrics-status-rail"` directly below metadata and apply variant classes by `stateRailVariant`: `idle -> text-muted-foreground`, `info -> text-foreground`, `warning -> text-amber-600 dark:text-amber-400`. Add explicit body containers: `data-testid="lyrics-empty-state"` for `status` in `idle|no-track`, `data-testid="lyrics-not-found-state"` for `sourceState === "not-found"`, and a default lyrics content block when `showLyrics` is true. Preserve existing header/theme/account behavior and existing Phase 6 layout classes (`lg:grid-cols-5`, lyrics pane `lg:col-span-3`, connection pane `lg:col-span-2`).</action>
  <acceptance_criteria>
    - `src/web/app-shell.tsx` contains `data-testid="lyrics-now-playing"` and `data-testid="lyrics-status-rail"`
    - `src/web/app-shell.tsx` contains variant class mapping for `idle`, `info`, and `warning`
    - `src/web/app-shell.tsx` contains `data-testid="lyrics-empty-state"` and `data-testid="lyrics-not-found-state"`
    - `src/web/app-shell.tsx` retains `lg:grid-cols-5`, `lg:col-span-3`, and `lg:col-span-2`
    - `npm test -- src/web/app-shell.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/app-shell.test.tsx</automated>
  </verify>
  <done>Web shell lyrics pane presents parity metadata and explicit state treatments with stable transition-safe rail placement.</done>
</task>

</tasks>

<verification>
- `npm test -- src/web/app-shell.test.tsx`
</verification>

<success_criteria>
- WEB-03: now-playing metadata and lyrics-pane state behavior are rendered in web shell from panel contract.
- UI-04: loading, empty, and not-found states are explicit, visually cohesive, and covered by shell tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-02-SUMMARY.md`
</output>
