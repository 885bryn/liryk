---
phase: 07-web-lyrics-experience-parity-and-state-polish
plan: "03"
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - src/web/visual-system.test.tsx
  - .planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-VISUAL-CHECKPOINT.md
autonomous: true
requirements:
  - WEB-03
  - UI-04
must_haves:
  truths:
    - User can move through key lyrics states without jarring visual regressions across themes.
    - User sees parity metadata and status rail within the same visual hierarchy as existing shell cards.
    - Team can reproduce automated and manual verification evidence for Phase 7 outcomes.
  artifacts:
    - path: src/web/visual-system.test.tsx
      provides: Regression checks for phase-07 lyrics metadata and rail styling hierarchy.
    - path: .planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-VISUAL-CHECKPOINT.md
      provides: Requirement-to-evidence checkpoint instructions.
  key_links:
    - from: src/web/visual-system.test.tsx
      to: src/web/app-shell.tsx
      via: rendered class/testid assertions
      pattern: lyrics-status-rail|lyrics-now-playing
    - from: .planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-VISUAL-CHECKPOINT.md
      to: WEB-03, UI-04
      via: traceability checklist
      pattern: WEB-03|UI-04
---

<objective>
Lock visual regression coverage and deliver reproducible verification artifacts for Phase 7 parity behavior.

Purpose: Ensure state-transition polish remains durable after implementation and provide explicit proof mapping for WEB-03 and UI-04.
Output: Visual-system regression tests and phase-local checkpoint guide with automated/manual validation steps.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-CONTEXT.md
@.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-VALIDATION.md
@.planning/phases/06-responsive-layout-and-visual-system/06-03-SUMMARY.md

<interfaces>
From `src/web/app-shell.tsx` (after Plan 02):
```typescript
// Key rendered contract markers expected by tests
data-testid="lyrics-now-playing"
data-testid="lyrics-status-rail"
data-testid="lyrics-empty-state"
data-testid="lyrics-not-found-state"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend visual-system regression tests for phase-07 lyrics state markers</name>
  <files>src/web/visual-system.test.tsx</files>
  <read_first>src/web/visual-system.test.tsx, src/web/app-shell.tsx, src/web/app-shell.test.tsx</read_first>
  <behavior>
    - Test 1: now-playing metadata container uses stable hierarchy classes and remains inside lyrics pane card.
    - Test 2: inline status rail marker exists once and preserves expected variant class outputs.
    - Test 3: state-specific containers for empty and not-found scenarios retain readable token-based styling.
  </behavior>
  <action>Update `src/web/visual-system.test.tsx` with assertions for `data-testid="lyrics-now-playing"` and `data-testid="lyrics-status-rail"` rendered inside the lyrics pane card. Add checks that status rail variant classes resolve to token-safe styles (`text-muted-foreground`, `text-foreground`, `text-amber-600`/`dark:text-amber-400`) and that empty/not-found containers include readable support-text classes (`text-sm`, `leading-relaxed`). Keep existing card ring and heading hierarchy assertions untouched.</action>
  <acceptance_criteria>
    - `src/web/visual-system.test.tsx` asserts `lyrics-now-playing` and `lyrics-status-rail` presence inside lyrics pane
    - `src/web/visual-system.test.tsx` checks status rail variant class tokens for idle/info/warning states
    - `src/web/visual-system.test.tsx` retains prior card token assertions (`bg-card`, `text-card-foreground`, `ring-border/60`)
    - `npm test -- src/web/visual-system.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/web/visual-system.test.tsx</automated>
  </verify>
  <done>Visual-system test suite guards phase-07 parity markers and transition-rail styling against regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Create phase-07 visual checkpoint and requirement traceability artifact</name>
  <files>.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-VISUAL-CHECKPOINT.md</files>
  <read_first>.planning/phases/06-responsive-layout-and-visual-system/06-VISUAL-CHECKPOINT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, src/web/app-shell.tsx</read_first>
  <action>Create `.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-VISUAL-CHECKPOINT.md` with sections: (1) Automated commands, (2) Manual visual checks, (3) Requirement traceability. Include exact automated commands `npm test -- src/app/live-lyrics-presenter.test.ts src/ui/lyrics/live-lyrics-panel.test.tsx`, `npm test -- src/web/app-shell.test.tsx`, `npm test -- src/web/visual-system.test.tsx`, and `npm run build`. In manual checks, include explicit steps for idle, reconnecting, paused, and not-found transitions in both themes and confirm status rail remains in one location while lyrics pane layout stays stable.</action>
  <acceptance_criteria>
    - `.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-VISUAL-CHECKPOINT.md` exists
    - Checkpoint file contains commands for presenter/panel tests, app-shell tests, visual-system tests, and build
    - Checkpoint file includes a traceability table containing both `WEB-03` and `UI-04`
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Phase 7 has a reproducible verification artifact that ties parity behavior to requirement evidence.</done>
</task>

</tasks>

<verification>
- `npm test -- src/web/visual-system.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- WEB-03 and UI-04 have explicit regression guards and a reproducible checkpoint document.
- Transition-polish verification is executable through both automated commands and manual state walkthrough steps.
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-03-SUMMARY.md`
</output>
