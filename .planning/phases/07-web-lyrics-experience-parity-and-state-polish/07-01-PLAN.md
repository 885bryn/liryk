---
phase: 07-web-lyrics-experience-parity-and-state-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/live-lyrics-presenter.ts
  - src/app/live-lyrics-presenter.test.ts
  - src/ui/lyrics/live-lyrics-panel.tsx
  - src/ui/lyrics/live-lyrics-panel.test.tsx
autonomous: true
requirements:
  - WEB-03
  - UI-04
must_haves:
  truths:
    - User gets deterministic lyrics panel state outputs that cover syncing, reconnecting, idle, no-track, unsupported, paused, and not-found outcomes.
    - User gets now-playing metadata fields from the panel model instead of shell-local hardcoded placeholders.
    - User gets one consistent inline status rail message contract across state variants.
  artifacts:
    - path: src/ui/lyrics/live-lyrics-panel.tsx
      provides: Panel model contract with now-playing metadata and status rail fields.
    - path: src/app/live-lyrics-presenter.ts
      provides: Canonical state-to-message mapping used by panel builder.
    - path: src/ui/lyrics/live-lyrics-panel.test.tsx
      provides: Contract tests for metadata and state-rail semantics.
  key_links:
    - from: src/ui/lyrics/live-lyrics-panel.tsx
      to: src/app/live-lyrics-presenter.ts
      via: buildLiveLyricsViewModel
      pattern: buildLiveLyricsViewModel\(
    - from: src/ui/lyrics/live-lyrics-panel.test.tsx
      to: src/ui/lyrics/live-lyrics-panel.tsx
      via: builder.build assertions
      pattern: createLiveLyricsPanelBuilder
---

<objective>
Define and lock the lyrics-panel state contract needed for web parity before shell rendering changes.

Purpose: Ensure WEB-03/UI-04 behavior is model-driven and testable so shell integration can consume one deterministic contract.
Output: Updated presenter/panel contracts and tests covering now-playing metadata and unified status-rail semantics.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-CONTEXT.md
@.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-RESEARCH.md
@.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-VALIDATION.md
@.planning/phases/06-responsive-layout-and-visual-system/06-02-SUMMARY.md

<interfaces>
From `src/app/live-lyrics-presenter.ts`:
```typescript
export type LiveLyricsViewModel = {
  status: LiveLyricsStatus;
  statusLine: string;
  sourceState: LyricsSourceState;
  renderMode: LyricRenderMode | null;
  activeLineText?: string;
  nextLineText?: string;
  showLyrics: boolean;
};
```

From `src/ui/lyrics/live-lyrics-panel.tsx`:
```typescript
export type LiveLyricsPanelModel = LiveLyricsViewModel & {
  title: string;
  trackLabel: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for now-playing metadata and unified status-rail contract</name>
  <files>src/app/live-lyrics-presenter.test.ts, src/ui/lyrics/live-lyrics-panel.test.tsx</files>
  <read_first>src/app/live-lyrics-presenter.test.ts, src/ui/lyrics/live-lyrics-panel.test.tsx, src/app/live-lyrics-presenter.ts, src/ui/lyrics/live-lyrics-panel.tsx, .planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-CONTEXT.md</read_first>
  <behavior>
    - Test 1: panel model includes `nowPlayingTitle`, `nowPlayingArtist`, and `isNowPlayingKnown` fields for both known-track and no-track scenarios.
    - Test 2: panel model includes `stateRailMessage` and `stateRailVariant` where `not-found`/`unsupported` map to `warning`, `idle`/`no-track` map to `idle`, and syncing/reconnecting/paused map to `info`.
    - Test 3: presenter and panel tests still assert explicit not-found retry semantics (`showPrimaryAction` true only when `retryAvailable` and not `retryInFlight`).
  </behavior>
  <action>Add or extend tests in `src/app/live-lyrics-presenter.test.ts` and `src/ui/lyrics/live-lyrics-panel.test.tsx` to assert exact new contract keys: `nowPlayingTitle`, `nowPlayingArtist`, `isNowPlayingKnown`, `stateRailMessage`, and `stateRailVariant` (`"idle" | "info" | "warning"`). Include explicit expected values for at least idle, syncing, reconnecting, paused, unsupported, and not-found cases. Keep existing retry/not-found assertions intact. Run target tests to confirm RED before implementation.</action>
  <acceptance_criteria>
    - `src/ui/lyrics/live-lyrics-panel.test.tsx` contains assertions for `nowPlayingTitle`, `nowPlayingArtist`, and `isNowPlayingKnown`
    - `src/ui/lyrics/live-lyrics-panel.test.tsx` contains assertions for `stateRailMessage` and `stateRailVariant`
    - `src/app/live-lyrics-presenter.test.ts` still contains explicit not-found retry assertions (`showPrimaryAction`, `primaryActionLabel`)
    - `npm test -- src/app/live-lyrics-presenter.test.ts src/ui/lyrics/live-lyrics-panel.test.tsx` exits non-zero before implementation updates
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/app/live-lyrics-presenter.test.ts src/ui/lyrics/live-lyrics-panel.test.tsx</automated>
  </verify>
  <done>Failing tests define the exact panel metadata and status-rail contract needed for shell parity rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement panel metadata and state-rail contract in presenter and builder</name>
  <files>src/app/live-lyrics-presenter.ts, src/ui/lyrics/live-lyrics-panel.tsx, src/app/live-lyrics-presenter.test.ts, src/ui/lyrics/live-lyrics-panel.test.tsx</files>
  <read_first>src/app/live-lyrics-presenter.ts, src/ui/lyrics/live-lyrics-panel.tsx, src/app/live-lyrics-presenter.test.ts, src/ui/lyrics/live-lyrics-panel.test.tsx</read_first>
  <behavior>
    - Test 1: builder returns stable metadata fields (`nowPlayingTitle`, `nowPlayingArtist`, `isNowPlayingKnown`) from input.
    - Test 2: builder returns deterministic status rail mapping via `stateRailMessage` and `stateRailVariant`.
    - Test 3: existing status/retry behavior remains backward-compatible for not-found and retry-in-flight.
  </behavior>
  <action>Update `LiveLyricsPanelModel` in `src/ui/lyrics/live-lyrics-panel.tsx` to add `nowPlayingTitle`, `nowPlayingArtist`, `isNowPlayingKnown`, `stateRailMessage`, and `stateRailVariant: "idle" | "info" | "warning"`. Extend builder input with optional `trackArtist`. Use `buildLiveLyricsViewModel` output as the source of `stateRailMessage` (`statusLine`) and map variants with concrete rules: `warning` for `sourceState === "not-found"` or `status === "unsupported"`; `idle` for `status === "idle"` or `status === "no-track"`; otherwise `info`. Set `nowPlayingTitle` from `trackTitle ?? "No active track"`, `nowPlayingArtist` from `trackArtist ?? "Spotify"`, and `isNowPlayingKnown` true only when `trackTitle` exists and `syncState.trackId` is non-null. Keep all existing fields in returned model to avoid shell regressions.</action>
  <acceptance_criteria>
    - `src/ui/lyrics/live-lyrics-panel.tsx` exports `stateRailVariant` union containing `idle`, `info`, and `warning`
    - `src/ui/lyrics/live-lyrics-panel.tsx` maps `stateRailVariant` using source-state/status rules described in action
    - `src/ui/lyrics/live-lyrics-panel.tsx` returns `nowPlayingTitle`, `nowPlayingArtist`, and `isNowPlayingKnown`
    - `npm test -- src/app/live-lyrics-presenter.test.ts src/ui/lyrics/live-lyrics-panel.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/app/live-lyrics-presenter.test.ts src/ui/lyrics/live-lyrics-panel.test.tsx</automated>
  </verify>
  <done>Panel builder emits a complete parity-ready contract for shell rendering with deterministic state-rail semantics.</done>
</task>

</tasks>

<verification>
- `npm test -- src/app/live-lyrics-presenter.test.ts src/ui/lyrics/live-lyrics-panel.test.tsx`
</verification>

<success_criteria>
- WEB-03 contract readiness: panel model includes now-playing metadata fields consumed by web shell.
- UI-04 state readiness: loading/empty/not-found related state messages are represented through one status-rail contract.
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-lyrics-experience-parity-and-state-polish/07-01-SUMMARY.md`
</output>
