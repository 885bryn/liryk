---
phase: 20-viewport-regression-and-timing-safety-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/fullscreen-lyrics-page.test.tsx
autonomous: true
requirements:
  - SAFE-01
  - QA-01
must_haves:
  truths:
    - "Automated fullscreen regressions prove track start, track transition, song end, final handoff, manual browse-away, and Back to Live recovery without React act warnings."
    - "Fullscreen viewport-lock tests include independent observable assertions for active row bounds, live-lock state, and scroll origin instead of only echoing getBoundaryLockedScrollTop output."
    - "Fullscreen timing authority remains sourced from estimatedProgressMs, applyEarlyCue, and getLineIndicesAt rather than DOM position or scroll state."
    - "The targeted timing and motion safety suite still passes after viewport regression hardening."
  artifacts:
    - path: "src/web/fullscreen-lyrics-page.test.tsx"
      provides: "Warning-clean fullscreen regression coverage for boundary visibility, browse-away, Back to Live recovery, and timing-authority guardrails."
    - path: "src/web/fullscreen-lyrics-page.tsx"
      provides: "Existing viewport-owned fullscreen implementation consumed by tests; production edits allowed only if a new regression exposes a concrete failure."
  key_links:
    - from: "src/web/fullscreen-lyrics-page.test.tsx"
      to: "src/web/fullscreen-lyrics-page.tsx"
      via: "Tests assert the viewport-owned scroll origin, active row geometry, Back to Live marker, and timing source strings."
      pattern: "getBoundaryLockedScrollTop|fullscreen-return-live|estimatedProgressMs|getLineIndicesAt"
    - from: "src/web/fullscreen-lyrics-page.tsx"
      to: "src/core/sync/lyric-timeline.ts"
      via: "Fullscreen active line selection continues through getLineIndicesAt with cue-adjusted progress."
      pattern: "getLineIndicesAt\\(syncedTimeline, cueAdjustedProgressMs\\)"
    - from: "src/web/fullscreen-lyrics-page.tsx"
      to: "src/core/sync/lyric-motion-window.ts"
      via: "Viewport locking remains tied to row-layout anchors and motion-window settle helpers."
      pattern: "buildRowLayout|getFloatingRowAnchorPx|getTransitionProgress|getRenderedFloatingIndex"
---

<objective>
Harden automated fullscreen viewport regressions and the targeted timing/motion safety gate for Phase 20.

Purpose: Prove the Phase 18/19 viewport fix across boundary and live-lock recovery scenarios without reopening playback timing, drift policy, active-line selection, or settle semantics.
Output: Warning-clean fullscreen tests with independent boundary assertions and source guardrails for timing authority.
</objective>

<execution_context>
@C:/Users/bryan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-viewport-regression-and-timing-safety-closure/20-RESEARCH.md
@.planning/phases/20-viewport-regression-and-timing-safety-closure/20-VALIDATION.md
@.planning/phases/18-viewport-anchor-ownership-and-scroll-surface/18-VERIFICATION.md
@.planning/phases/19-song-boundary-visibility-and-live-lock-recovery/19-VERIFICATION.md
@.planning/phases/19-song-boundary-visibility-and-live-lock-recovery/19-02-SUMMARY.md
@src/web/fullscreen-lyrics-page.tsx
@src/web/fullscreen-lyrics-page.test.tsx
@src/core/playback/playback-clock.test.ts
@src/core/sync/lyric-timeline.test.ts
@src/core/sync/lyric-sync-engine.test.ts
@src/core/sync/lyric-motion-window.test.ts
@src/app/live-sync-runtime.test.ts

<interfaces>
From src/web/fullscreen-lyrics-page.tsx:
```typescript
export function getBoundaryLockedScrollTop(input: {
  viewportHeight: number;
  rowLayout: RowLayout;
  floatingIndex: number;
}): number;

export function FullscreenLyricsPage(): JSX.Element;
```

Existing fullscreen markers and test helpers:
```typescript
data-testid="fullscreen-lyrics-viewport"
data-testid="fullscreen-lyrics-center-stage"
data-testid="fullscreen-lyrics-track"
data-testid="fullscreen-return-live"

function stubViewportGeometry(input: {
  viewportHeight: number;
  activeIndex: number;
  rowHeights: number[];
  rowHeight?: number;
});

function waitForProgrammaticScrollWindowToSettle(): Promise<void>;
```

Timing and motion contracts that must stay unchanged:
```typescript
HARD_DRIFT_SNAP_MS === 1_200
MAX_SOFT_CORRECTION_MS === 100
DEFAULT_CUE_LEAD_MS is imported by fullscreen and passed to applyEarlyCue(...)
getLineIndicesAt(timeline, progressMs) owns activeIndex/nextIndex selection
getTransitionPhase(...) and getTargetScrollOffset(...) own settle semantics
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make programmatic recentering and Back to Live tests act-warning clean</name>
  <read_first>
    - src/web/fullscreen-lyrics-page.test.tsx
    - src/web/fullscreen-lyrics-page.tsx
    - .planning/phases/20-viewport-regression-and-timing-safety-closure/20-RESEARCH.md
    - .planning/phases/20-viewport-regression-and-timing-safety-closure/20-VALIDATION.md
    - .planning/phases/19-song-boundary-visibility-and-live-lock-recovery/19-VERIFICATION.md
  </read_first>
  <files>src/web/fullscreen-lyrics-page.test.tsx</files>
  <behavior>
    - Test 1: `keeps live lock enabled during programmatic recentering and hides fullscreen-return-live` still proves programmatic track-change recentering does not show Back to Live.
    - Test 2: `Back to Live restores the boundary-aware live anchor` still proves button recovery restores live lock and row bounds.
    - Test 3: running the fullscreen test file with raw output does not print `not wrapped in act`.
  </behavior>
  <action>Update only the fullscreen test harness unless a failing regression proves production code is wrong. Import `act` from `@testing-library/react`. Wrap asynchronous state-producing test actions in `act(...)`: the `waitForProgrammaticScrollWindowToSettle` timer, the track-change `rerender(<FullscreenLyricsPage />)` in the programmatic recentering test, and the Back to Live click path that triggers `scrollToLiveAnchor("smooth")`. Keep the existing test names `keeps live lock enabled during programmatic recentering and hides fullscreen-return-live` and `Back to Live restores the boundary-aware live anchor`. Do not hide warnings by spying on `console.error`; the raw test run must be warning-clean.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.test.tsx` contains the exact import token `act`.
    - `src/web/fullscreen-lyrics-page.test.tsx` contains the exact helper name `waitForProgrammaticScrollWindowToSettle`.
    - `src/web/fullscreen-lyrics-page.test.tsx` contains at least one exact string `await act(async () =>`.
    - `src/web/fullscreen-lyrics-page.test.tsx` does not contain the exact string `spyOn(console, "error")`.
    - `rtk proxy npm run test -- src/web/fullscreen-lyrics-page.test.tsx` exits 0 and its raw output does not contain the exact string `not wrapped in act`.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy npm run test -- src/web/fullscreen-lyrics-page.test.tsx</automated>
  </verify>
  <done>The fullscreen interaction suite passes and the raw Vitest output is free of React `act(...)` warnings for programmatic recentering and Back to Live recovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add independent boundary and live-lock assertions beyond helper output</name>
  <read_first>
    - src/web/fullscreen-lyrics-page.test.tsx
    - src/web/fullscreen-lyrics-page.tsx
    - .planning/phases/20-viewport-regression-and-timing-safety-closure/20-RESEARCH.md
    - .planning/phases/18-viewport-anchor-ownership-and-scroll-surface/18-VERIFICATION.md
    - .planning/phases/19-song-boundary-visibility-and-live-lock-recovery/19-VERIFICATION.md
  </read_first>
  <files>src/web/fullscreen-lyrics-page.test.tsx</files>
  <behavior>
    - Test 1: track start and track transition tests assert active lyric bounds inside the viewport and assert Back to Live is absent.
    - Test 2: song end and final handoff tests assert active lyric bounds inside the viewport and assert live scroll remains at the viewport-owned origin.
    - Test 3: manual browse-away and Back to Live tests assert both button state and active row bounds, not just a computed scroll target.
  </behavior>
  <action>Add focused assertions to the existing boundary and recovery tests in `src/web/fullscreen-lyrics-page.test.tsx`. For the existing tests named `keeps the first synced lyric in viewport after track start`, `keeps the first synced lyric in viewport after track transition`, `keeps the last synced lyric in viewport near song end`, and `keeps the last synced lyric in viewport during final handoff`, assert all of these concrete facts: `viewport.scrollTop` is `0`, the active row `top` is `>= 0`, the active row `bottom` is `<= viewportHeight`, and `screen.queryByTestId("fullscreen-return-live")` is `null`. Add a small local helper named `expectActiveRowInsideViewport` if it removes duplication. For the Back to Live recovery test, keep the existing `getBoundaryLockedScrollTop(...)` expected target but add an independent active-row bounds assertion after the click. Do not change `src/web/fullscreen-lyrics-page.tsx` unless one of these new assertions exposes a concrete production bug.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.test.tsx` contains the exact test names `keeps the first synced lyric in viewport after track start`, `keeps the first synced lyric in viewport after track transition`, `keeps the last synced lyric in viewport near song end`, and `keeps the last synced lyric in viewport during final handoff`.
    - `src/web/fullscreen-lyrics-page.test.tsx` contains the exact assertion text `expect(viewport.scrollTop).toBe(0)`.
    - `src/web/fullscreen-lyrics-page.test.tsx` contains the exact marker `screen.queryByTestId("fullscreen-return-live")`.
    - `src/web/fullscreen-lyrics-page.test.tsx` contains both bound assertions `toBeGreaterThanOrEqual(0)` and `toBeLessThanOrEqual(`.
    - `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx</automated>
  </verify>
  <done>Boundary and recovery regressions prove observable row visibility, live-lock state, and live scroll origin independently of helper echo assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard fullscreen timing authority and run the targeted safety suite</name>
  <read_first>
    - src/web/fullscreen-lyrics-page.test.tsx
    - src/web/fullscreen-lyrics-page.tsx
    - src/core/playback/playback-clock.test.ts
    - src/core/sync/lyric-timeline.test.ts
    - src/core/sync/lyric-sync-engine.test.ts
    - src/core/sync/lyric-motion-window.test.ts
    - src/app/live-sync-runtime.test.ts
    - .planning/phases/20-viewport-regression-and-timing-safety-closure/20-RESEARCH.md
  </read_first>
  <files>src/web/fullscreen-lyrics-page.test.tsx</files>
  <behavior>
    - Test 1: fullscreen source still computes `progressSourceMs`, `cueAdjustedProgressMs`, and `lineIndices` from `estimatedProgressMs`, `applyEarlyCue`, and `getLineIndicesAt`.
    - Test 2: fullscreen source does not introduce timing policy constants such as `HARD_DRIFT_SNAP_MS` or `MAX_SOFT_CORRECTION_MS`.
    - Test 3: the targeted SAFE-01 suite passes after fullscreen test hardening.
  </behavior>
  <action>Add or tighten one source-guard test in `src/web/fullscreen-lyrics-page.test.tsx` named `preserves playback timing authority while viewport locking`. The test must read `src/web/fullscreen-lyrics-page.tsx` with the existing `readFileSync` pattern and assert the source includes the exact strings `estimatedProgressMs`, `applyEarlyCue(progressSourceMs, DEFAULT_CUE_LEAD_MS)`, `getLineIndicesAt(syncedTimeline, cueAdjustedProgressMs)`, `getTransitionProgress({`, and `getRenderedFloatingIndex({`. Assert the source does not include `HARD_DRIFT_SNAP_MS =` or `MAX_SOFT_CORRECTION_MS =` so fullscreen viewport work does not define drift policy. Do not edit `src/core/playback/*`, `src/core/sync/*`, or `src/app/live-sync-runtime.test.ts` unless the targeted safety suite fails and identifies an existing test contract violation.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.test.tsx` contains the exact test name `preserves playback timing authority while viewport locking`.
    - `src/web/fullscreen-lyrics-page.test.tsx` contains the exact strings `applyEarlyCue(progressSourceMs, DEFAULT_CUE_LEAD_MS)`, `getLineIndicesAt(syncedTimeline, cueAdjustedProgressMs)`, `getTransitionProgress({`, and `getRenderedFloatingIndex({`.
    - `src/web/fullscreen-lyrics-page.test.tsx` contains negative assertions for the exact strings `HARD_DRIFT_SNAP_MS =` and `MAX_SOFT_CORRECTION_MS =`.
    - `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts src/core/sync/lyric-motion-window.test.ts src/core/playback/playback-clock.test.ts src/app/live-sync-runtime.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts src/core/sync/lyric-motion-window.test.ts src/core/playback/playback-clock.test.ts src/app/live-sync-runtime.test.ts</automated>
  </verify>
  <done>SAFE-01 has targeted automated proof that viewport-lock regression work did not move timing authority, drift policy, active-line selection, or settle semantics.</done>
</task>

</tasks>

<verification>
Run both commands before completing this plan:

1. `rtk proxy npm run test -- src/web/fullscreen-lyrics-page.test.tsx`
2. `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts src/core/sync/lyric-motion-window.test.ts src/core/playback/playback-clock.test.ts src/app/live-sync-runtime.test.ts`

The raw fullscreen command must pass without `not wrapped in act`. The targeted safety suite must pass without production timing, drift, timeline, or motion helper edits.
</verification>

<success_criteria>
Phase 20 automated coverage proves the viewport fix across track start, track end, track transitions, manual browse-away, and Back to Live recovery, while the targeted timing/motion suite preserves playback clock, drift policy, active-line selection, and settle contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/20-viewport-regression-and-timing-safety-closure/20-01-SUMMARY.md`
</output>
