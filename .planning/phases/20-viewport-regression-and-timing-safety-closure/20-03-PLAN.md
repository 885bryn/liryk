---
phase: 20-viewport-regression-and-timing-safety-closure
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/fullscreen-lyrics-page.tsx
  - src/web/fullscreen-lyrics-page.test.tsx
autonomous: true
requirements:
  - SAFE-01
  - QA-01
gap_closure: true
must_haves:
  truths:
    - "During sustained fullscreen playback, the active lyric no longer drifts upward over repeated line transitions."
    - "The viewport regression suite fails before the fix and passes after the fix for cumulative mid-song progression."
    - "Timing authority and motion-settle contracts stay green after the viewport drift fix."
  artifacts:
    - path: "src/web/fullscreen-lyrics-page.tsx"
      provides: "Production drift fix so row-height measurement stays invariant to visual tier transforms during live playback."
    - path: "src/web/fullscreen-lyrics-page.test.tsx"
      provides: "Regression proving no cumulative active-line viewport drift across sequential mid-song transitions."
  key_links:
    - from: "src/web/fullscreen-lyrics-page.tsx"
      to: "src/core/sync/lyric-motion-window.ts"
      via: "Measured row heights feed buildRowLayout/getFloatingRowAnchorPx and therefore live translateY anchoring."
      pattern: "buildRowLayout\(|getFloatingRowAnchorPx\(|setRowHeights\("
    - from: "src/web/fullscreen-lyrics-page.test.tsx"
      to: "src/web/fullscreen-lyrics-page.tsx"
      via: "Regression advances progress through multiple transitions and asserts active-row center remains stable inside viewport."
      pattern: "cumulative|drift|viewport|Line"
---

<objective>
Close the Phase 20 blocker by fixing cumulative upward active-line drift during real fullscreen playback.

Purpose: Restore trustworthy viewport lock in production while preserving previously validated timing, active-line, and motion contracts.
Output: Drift-proof fullscreen row-measurement behavior plus a regression that fails before the fix and passes after it.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-viewport-regression-and-timing-safety-closure/20-RESEARCH.md
@.planning/phases/20-viewport-regression-and-timing-safety-closure/20-VERIFICATION.md
@.planning/phases/20-viewport-regression-and-timing-safety-closure/20-01-SUMMARY.md
@.planning/phases/20-viewport-regression-and-timing-safety-closure/20-02-SUMMARY.md
@src/web/fullscreen-lyrics-page.tsx
@src/web/fullscreen-lyrics-page.test.tsx
@src/core/sync/lyric-motion-window.ts

<interfaces>
From src/web/fullscreen-lyrics-page.tsx:
```typescript
const measuredRowHeights = useMemo(() => ...);
const rowLayout = useMemo(() => buildRowLayout(measuredRowHeights, BASE_ROW_GAP_PX), ...);
const syncedTrackTranslateY = `${renderedTrackOffsetPx + trackBrowseOffsetPx}px`;
```

Critical helper contract already in production:
```typescript
export function getBoundaryLockedScrollTop(input: {
  viewportHeight: number;
  rowLayout: RowLayout;
  floatingIndex: number;
}): number;
```

Gap evidence from verification:
```text
Active lyric drifts upward by ~half a line per progression until off-screen mid-song.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a failing cumulative-drift regression for sustained mid-song progression</name>
  <read_first>
    - src/web/fullscreen-lyrics-page.test.tsx
    - src/web/fullscreen-lyrics-page.tsx
    - .planning/phases/20-viewport-regression-and-timing-safety-closure/20-VERIFICATION.md
    - .planning/phases/20-viewport-regression-and-timing-safety-closure/20-01-SUMMARY.md
  </read_first>
  <files>src/web/fullscreen-lyrics-page.test.tsx</files>
  <behavior>
    - Test 1: a sustained mid-song sequence (at least 8 sequential progress jumps) keeps the active row center within viewport bounds for every step.
    - Test 2: center deviation from viewport midpoint stays bounded (no cumulative ratchet upward between steps).
    - Test 3: Back to Live remains hidden while still live-locked during scripted progression.
  </behavior>
  <action>Add a new test in `src/web/fullscreen-lyrics-page.test.tsx` named `keeps active row position stable across sustained mid-song progression`. Use a synced fixture with at least 10 timestamped lines (`Line 1` ... `Line 10`) and progress values that advance through at least 8 transitions (for example, `4_100`, `6_100`, `8_100`, ...). At each step rerender `FullscreenLyricsPage`, call the existing geometry helper, and assert: active row `top >= 0`, active row `bottom <= viewportHeight`, and active row center remains within +/-36px of viewport midpoint. Also assert `screen.queryByTestId("fullscreen-return-live")` is `null` at each step. Write this test first so it reproduces the current drift gap before production fix.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.test.tsx` contains the exact test name `keeps active row position stable across sustained mid-song progression`.
    - The new test includes at least 8 sequential progression checks.
    - The test asserts `toBeGreaterThanOrEqual(0)` and `toBeLessThanOrEqual(` against active row bounds.
    - The test asserts center deviation is bounded by an explicit numeric threshold (`36`).
    - `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx -t "keeps active row position stable across sustained mid-song progression"` exits 0 after the fix.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx -t "keeps active row position stable across sustained mid-song progression"</automated>
  </verify>
  <done>A deterministic regression test exists that proves sustained progression no longer drifts the active lyric out of viewport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix fullscreen row-height measurement so transforms cannot ratchet the live anchor</name>
  <read_first>
    - src/web/fullscreen-lyrics-page.tsx
    - src/web/fullscreen-lyrics-page.test.tsx
    - src/core/sync/lyric-motion-window.ts
    - .planning/phases/19-song-boundary-visibility-and-live-lock-recovery/19-VERIFICATION.md
  </read_first>
  <files>src/web/fullscreen-lyrics-page.tsx, src/web/fullscreen-lyrics-page.test.tsx</files>
  <behavior>
    - Test 1: row measurement is based on layout height unaffected by visual transform scaling.
    - Test 2: sequential live motion uses stable row layout inputs and does not accumulate offset drift.
    - Test 3: existing boundary start/end/transition/recovery tests remain green.
  </behavior>
  <action>In `src/web/fullscreen-lyrics-page.tsx`, update the row-measurement path inside the `measureRows` effect to stop using transformed `getBoundingClientRect().height` as the primary value. Measure with invariant layout metrics first (`offsetHeight`, then `clientHeight`/`scrollHeight` fallback), round to integer px, and keep `FALLBACK_ROW_TEXT_HEIGHT_PX` only for zero/invalid values. Keep the existing <=1px stability guard in `setRowHeights`. Do not change timing source selection (`estimatedProgressMs`, `applyEarlyCue`, `getLineIndicesAt`) or drift policy constants. In the new regression test, verify the active row remains centered/bounded after each progression step with the production fix applied.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.tsx` no longer relies on `getBoundingClientRect().height` as the primary row-height source in `measureRows`.
    - `src/web/fullscreen-lyrics-page.tsx` contains `offsetHeight` in the row measurement path.
    - `src/web/fullscreen-lyrics-page.tsx` still contains `FALLBACK_ROW_TEXT_HEIGHT_PX` fallback behavior.
    - `src/web/fullscreen-lyrics-page.tsx` still contains `setRowHeights((current) =>` with the <=1px stability check.
    - `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx</automated>
  </verify>
  <done>The production viewport anchor no longer drifts due to transform-coupled row measurement, and fullscreen regression tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-run the targeted timing/motion safety gate after drift fix</name>
  <read_first>
    - src/web/fullscreen-lyrics-page.test.tsx
    - src/core/playback/playback-clock.test.ts
    - src/core/sync/lyric-timeline.test.ts
    - src/core/sync/lyric-sync-engine.test.ts
    - src/core/sync/lyric-motion-window.test.ts
    - src/app/live-sync-runtime.test.ts
    - package.json
  </read_first>
  <files>src/web/fullscreen-lyrics-page.test.tsx</files>
  <behavior>
    - Test 1: fullscreen regression suite remains green with the new drift test included.
    - Test 2: SAFE-01 timing/drift/timeline/motion/runtime contracts remain green.
    - Test 3: production build remains successful after the fix.
  </behavior>
  <action>Run the established Phase 20 safety commands without weakening assertions: `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts src/core/sync/lyric-motion-window.test.ts src/core/playback/playback-clock.test.ts src/app/live-sync-runtime.test.ts` then `rtk npm run build`. If any command fails, fix the underlying issue instead of relaxing drift bounds or removing the new regression test.</action>
  <acceptance_criteria>
    - The six-file targeted safety command exits 0.
    - `rtk npm run build` exits 0.
    - `src/web/fullscreen-lyrics-page.test.tsx` still contains `preserves playback timing authority while viewport locking`.
    - `src/web/fullscreen-lyrics-page.test.tsx` still contains the new sustained-progression drift regression test.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts src/core/sync/lyric-motion-window.test.ts src/core/playback/playback-clock.test.ts src/app/live-sync-runtime.test.ts</automated>
    <automated>rtk npm run build</automated>
  </verify>
  <done>The drift fix is validated against existing SAFE-01 guardrails and does not regress targeted timing/motion behavior.</done>
</task>

</tasks>

<verification>
Plan verification requires all of the following:

1. `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx -t "keeps active row position stable across sustained mid-song progression"`
2. `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts src/core/sync/lyric-motion-window.test.ts src/core/playback/playback-clock.test.ts src/app/live-sync-runtime.test.ts`
3. `rtk npm run build`

The new sustained-progression regression must pass and existing SAFE-01 command coverage must stay green.
</verification>

<success_criteria>
Fullscreen active-line placement no longer drifts upward during sustained playback, and SAFE-01 timing/motion contracts remain unchanged and green.
</success_criteria>

<output>
After completion, create `.planning/phases/20-viewport-regression-and-timing-safety-closure/20-03-SUMMARY.md`
</output>
