---
phase: 20-viewport-regression-and-timing-safety-closure
plan: "05"
type: execute
wave: 2
depends_on:
  - 20-03
files_modified:
  - src/web/fullscreen-lyrics-page.tsx
  - src/web/fullscreen-lyrics-page.test.tsx
autonomous: true
requirements:
  - SAFE-01
  - QA-01
gap_closure: true
must_haves:
  truths:
    - "Fullscreen active-line screen position stays stable during sustained real-browser playback without cumulative upward drift."
    - "A regression reproduces the drift bug when transform-inflated heights are used and passes with layout-invariant measured heights."
    - "Playback timing, drift policy, active-line selection, and motion settle contracts remain unchanged and green."
  artifacts:
    - path: "src/web/fullscreen-lyrics-page.tsx"
      provides: "Viewport anchoring uses stable row metrics for motion interpolation and live recentering across sustained progression."
    - path: "src/web/fullscreen-lyrics-page.test.tsx"
      provides: "Browser-parity drift regression with explicit step-by-step center delta assertions."
  key_links:
    - from: "src/web/fullscreen-lyrics-page.tsx"
      to: "src/core/sync/lyric-motion-window.ts"
      via: "rowLayout from measuredRowHeights feeds getFloatingRowAnchorPx/getRenderedFloatingIndex during live playback"
      pattern: "buildRowLayout\(|getFloatingRowAnchorPx\(|getRenderedFloatingIndex\("
    - from: "src/web/fullscreen-lyrics-page.test.tsx"
      to: "src/web/fullscreen-lyrics-page.tsx"
      via: "sustained progression assertions validate translateY and active-row center stability over repeated transitions"
      pattern: "sustained|drift|center|translateY"
---

<objective>
Close the remaining drift blocker by making fullscreen row anchoring deterministic under sustained transitions and proving it with browser-parity regressions.

Purpose: Resolve the QA-01 sustained-playback failure while preserving SAFE-01 timing and motion contracts.
Output: Updated fullscreen anchoring behavior plus stronger sustained-drift regression coverage.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-viewport-regression-and-timing-safety-closure/20-VERIFICATION.md
@.planning/phases/20-viewport-regression-and-timing-safety-closure/20-03-SUMMARY.md
@.planning/phases/20-viewport-regression-and-timing-safety-closure/20-04-SUMMARY.md
@src/web/fullscreen-lyrics-page.tsx
@src/web/fullscreen-lyrics-page.test.tsx

<interfaces>
From src/web/fullscreen-lyrics-page.tsx:
```typescript
const measuredRowHeights = useMemo(() => ...);
const rowLayout = useMemo(() => buildRowLayout(measuredRowHeights, BASE_ROW_GAP_PX), [measuredRowHeights]);
const renderedTrackOffsetPx = canRenderSyncedMotion ? -getFloatingRowAnchorPx(rowLayout, renderedFloatingIndex) : 0;
```

Current blocker evidence from verification:
```text
Sustained mid-song progression (drift check) fails in real browser with active lyric drifting above physical center.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a browser-parity failing regression for cumulative center drift</name>
  <read_first>
    - src/web/fullscreen-lyrics-page.test.tsx
    - src/web/fullscreen-lyrics-page.tsx
    - .planning/phases/20-viewport-regression-and-timing-safety-closure/20-VERIFICATION.md
    - .planning/phases/20-viewport-regression-and-timing-safety-closure/20-04-SUMMARY.md
  </read_first>
  <files>src/web/fullscreen-lyrics-page.test.tsx</files>
  <behavior>
    - Test 1: sustained progression across at least 12 transitions tracks the active-row center delta per step.
    - Test 2: cumulative center drift must remain bounded by an explicit threshold after each transition.
    - Test 3: live lock remains active (no Back to Live button) throughout scripted progression.
  </behavior>
  <action>Add a new test named `prevents cumulative upward drift across 12 sustained transitions` in `src/web/fullscreen-lyrics-page.test.tsx`. Use a 14-line synced fixture (`Line 1` to `Line 14`) and run progress points that produce 12 active-line transitions. For each step, compute active-row center and compare against viewport midpoint and the previous-step center. Assert (1) active bounds stay inside viewport, (2) absolute center delta to midpoint stays <= `36`, and (3) inter-step center change stays <= `24` to catch ratchet drift. Keep `screen.queryByTestId("fullscreen-return-live")` equal to `null` each step. Write this test first so it fails before the production adjustment.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.test.tsx` contains `prevents cumulative upward drift across 12 sustained transitions`.
    - The new regression uses at least 12 transition steps.
    - The test includes explicit numeric center thresholds `36` and `24`.
    - The test checks active bounds with `toBeGreaterThanOrEqual(0)` and `toBeLessThanOrEqual(`.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx -t "prevents cumulative upward drift across 12 sustained transitions"</automated>
  </verify>
  <done>A deterministic sustained-progression regression exists and fails before the fix, then passes after implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize row measurement and rendering cadence for sustained progression</name>
  <read_first>
    - src/web/fullscreen-lyrics-page.tsx
    - src/web/fullscreen-lyrics-page.test.tsx
    - src/core/sync/lyric-motion-window.ts
    - .planning/phases/20-viewport-regression-and-timing-safety-closure/20-03-SUMMARY.md
  </read_first>
  <files>src/web/fullscreen-lyrics-page.tsx, src/web/fullscreen-lyrics-page.test.tsx</files>
  <behavior>
    - Test 1: measured row heights stay stable across tier-style changes and repeated transitions.
    - Test 2: rendered translateY tracks floating index without monotonic upward bias over sustained progression.
    - Test 3: timing authority inputs remain `estimatedProgressMs` + early cue + timeline helpers.
  </behavior>
  <action>In `src/web/fullscreen-lyrics-page.tsx`, keep timing and line-selection contracts unchanged, and harden row measurement for sustained playback by computing a single stable row metric per element from layout-first values (`offsetHeight`, `clientHeight`, `scrollHeight`) with integer normalization and rect fallback only when all layout values are non-positive. Ensure `setRowHeights` still uses the existing <=1px no-op guard so transition style changes do not churn rowLayout each frame. If needed, scope measurement refreshes to actual content/viewport changes (not every visual transition) so floating-index interpolation reads a stable layout baseline across long runs. Update tests to assert these invariants directly from source contracts and sustained progression behavior.</action>
  <acceptance_criteria>
    - `src/web/fullscreen-lyrics-page.tsx` still contains `estimatedProgressMs`, `applyEarlyCue(`, and `getLineIndicesAt(`.
    - `src/web/fullscreen-lyrics-page.tsx` contains layout-first measurement keys: `offsetHeight`, `clientHeight`, `scrollHeight`.
    - `src/web/fullscreen-lyrics-page.tsx` retains the <=1px `setRowHeights((current) =>` stability guard.
    - `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx</automated>
  </verify>
  <done>Fullscreen sustained progression no longer exhibits cumulative upward drift and all fullscreen regressions stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run SAFE-01 targeted safety gate after sustained-drift fix</name>
  <read_first>
    - src/web/fullscreen-lyrics-page.test.tsx
    - src/core/playback/playback-clock.test.ts
    - src/core/sync/lyric-timeline.test.ts
    - src/core/sync/lyric-sync-engine.test.ts
    - src/core/sync/lyric-motion-window.test.ts
    - src/app/live-sync-runtime.test.ts
    - package.json
  </read_first>
  <files>src/web/fullscreen-lyrics-page.test.tsx</files>
  <action>Run the established Phase 20 targeted safety gate exactly: `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts src/core/sync/lyric-motion-window.test.ts src/core/playback/playback-clock.test.ts src/app/live-sync-runtime.test.ts` followed by `rtk npm run build`. If failures occur, fix root causes without weakening drift assertions or removing the sustained regression.</action>
  <acceptance_criteria>
    - The six-file targeted safety command exits 0.
    - `rtk npm run build` exits 0.
    - `src/web/fullscreen-lyrics-page.test.tsx` still contains `preserves playback timing authority while viewport locking`.
    - `src/web/fullscreen-lyrics-page.test.tsx` contains `prevents cumulative upward drift across 12 sustained transitions`.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts src/core/sync/lyric-motion-window.test.ts src/core/playback/playback-clock.test.ts src/app/live-sync-runtime.test.ts</automated>
    <automated>rtk npm run build</automated>
  </verify>
  <done>SAFE-01 remains satisfied after the sustained-drift closure changes.</done>
</task>

</tasks>

<verification>
Overall checks:

1. `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx -t "prevents cumulative upward drift across 12 sustained transitions"`
2. `rtk npm run test -- src/web/fullscreen-lyrics-page.test.tsx src/core/sync/lyric-timeline.test.ts src/core/sync/lyric-sync-engine.test.ts src/core/sync/lyric-motion-window.test.ts src/core/playback/playback-clock.test.ts src/app/live-sync-runtime.test.ts`
3. `rtk npm run build`
</verification>

<success_criteria>
Real-browser drift behavior is represented by a sustained regression and the fullscreen anchor remains stable without regressing SAFE-01 timing/motion contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/20-viewport-regression-and-timing-safety-closure/20-05-SUMMARY.md`
</output>
