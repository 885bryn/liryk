---
phase: 01-spotify-connection-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infra/config/env.ts
  - src/infra/config/env.test.ts
  - src/core/auth/types.ts
  - src/core/auth/spotify-auth-service.ts
  - src/core/auth/spotify-auth-service.test.ts
autonomous: true
requirements:
  - AUTH-01
  - SECU-01
must_haves:
  truths:
    - User can start Spotify OAuth PKCE flow from app-owned auth service.
    - App refuses to run auth flow when required `.env` secrets are missing or invalid.
  artifacts:
    - path: src/infra/config/env.ts
      provides: Validated runtime env contract for Spotify auth settings.
    - path: src/core/auth/spotify-auth-service.ts
      provides: PKCE auth session start and callback exchange orchestration.
  key_links:
    - from: src/core/auth/spotify-auth-service.ts
      to: src/infra/config/env.ts
      via: validated env import
      pattern: SPOTIFY_CLIENT_ID|SPOTIFY_REDIRECT_URI
---

<objective>
Create a secure, typed Spotify auth foundation and environment boundary.

Purpose: Prevent insecure config drift and establish a reusable PKCE auth orchestration layer.
Output: Validated env module and initial Spotify auth service contract used by later UI/session plans.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-spotify-connection-foundation/01-CONTEXT.md
@.planning/phases/01-spotify-connection-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validated Spotify auth env module</name>
  <files>src/infra/config/env.ts, src/infra/config/env.test.ts</files>
  <action>Implement a single env loader with runtime validation for Spotify auth variables and any auth-related app base URL/redirect values. Fail fast with human-readable errors when required values are missing; remove any fallback constants for auth secrets. Include tests for valid config and missing-variable failure behavior.</action>
  <verify>
    <automated>npm test -- src/infra/config/env.test.ts</automated>
  </verify>
  <done>Auth configuration is loaded only from environment, and invalid config is rejected before auth flow starts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Spotify PKCE auth orchestration service</name>
  <files>src/core/auth/types.ts, src/core/auth/spotify-auth-service.ts, src/core/auth/spotify-auth-service.test.ts</files>
  <action>Implement a typed auth service that starts PKCE auth, tracks pending auth state, validates callback state, and exchanges auth codes through the Spotify SDK/client abstraction. Service must expose explicit state transitions needed by UI (`disconnected`, `authorizing`, `connected`, `recoverable_error`) and must never expose raw secrets to renderer-level state.</action>
  <verify>
    <automated>npm test -- src/core/auth/spotify-auth-service.test.ts</automated>
  </verify>
  <done>Auth service can initiate and complete PKCE flow with validated inputs and explicit error states.</done>
</task>

</tasks>

<verification>
Run auth unit tests and config tests. Confirm no hardcoded Spotify secret/client fallback values remain in auth service/config modules.
</verification>

<success_criteria>
- PKCE auth orchestration has deterministic state transitions and tests.
- `.env` is the single source of Spotify auth configuration.
- AUTH-01 and SECU-01 have concrete implementation entry points.
</success_criteria>

<output>
After completion, create `.planning/phases/01-spotify-connection-foundation/01-01-SUMMARY.md`
</output>
