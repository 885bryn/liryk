---
phase: 01-spotify-connection-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/infra/auth/token-store.ts
  - src/infra/auth/token-store.test.ts
  - src/core/auth/session-bootstrap.ts
  - src/core/auth/session-bootstrap.test.ts
  - src/ui/connection/account-menu.tsx
  - src/app/bootstrap-auth.ts
autonomous: true
requirements:
  - AUTH-02
  - SECU-01
must_haves:
  truths:
    - User remains connected after app restart without reauthorizing when refresh token is valid.
    - User can disconnect/logout from a clearly accessible connected state control.
    - Account switch/disconnect resets session cleanly and returns to first-connect state.
  artifacts:
    - path: src/infra/auth/token-store.ts
      provides: Secure token persistence interface for auth session recovery.
    - path: src/core/auth/session-bootstrap.ts
      provides: Startup session rehydrate and refresh logic.
    - path: src/ui/connection/account-menu.tsx
      provides: Connected account identity and disconnect action.
  key_links:
    - from: src/core/auth/session-bootstrap.ts
      to: src/infra/auth/token-store.ts
      via: persisted session load/save
      pattern: loadTokens|saveTokens|clearTokens
    - from: src/ui/connection/account-menu.tsx
      to: src/state/auth/auth-store.ts
      via: disconnect action
      pattern: logout|disconnect
---

<objective>
Deliver durable Spotify session behavior across app restarts with secure token handling.

Purpose: Complete the phase by making auth persistence reliable and user-controllable.
Output: Secure token store adapter, startup session bootstrap, and connected account/logout surface.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spotify-connection-foundation/01-CONTEXT.md
@.planning/phases/01-spotify-connection-foundation/01-RESEARCH.md
@.planning/phases/01-spotify-connection-foundation/01-01-SUMMARY.md
@.planning/phases/01-spotify-connection-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement secure token persistence adapter</name>
  <files>src/infra/auth/token-store.ts, src/infra/auth/token-store.test.ts</files>
  <action>Implement token store abstraction using existing secure desktop storage mechanism. Persist refresh/access token metadata needed for restart recovery, support clear/reset for logout/account switch, and ensure no plaintext token writes are introduced in project files. Add tests for load/save/clear and corrupted-entry handling.</action>
  <verify>
    <automated>npm test -- src/infra/auth/token-store.test.ts</automated>
  </verify>
  <done>Tokens are persisted and cleared through one secure adapter with automated test coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add startup session bootstrap and refresh recovery</name>
  <files>src/core/auth/session-bootstrap.ts, src/core/auth/session-bootstrap.test.ts, src/app/bootstrap-auth.ts</files>
  <action>Implement startup auth bootstrap that reads persisted tokens, validates expiry, refreshes when necessary, and updates auth store/service state to connected automatically on successful recovery. Include bounded retry + visible status hooks for transient refresh errors and fallback to reconnect when session is unrecoverable.</action>
  <verify>
    <automated>npm test -- src/core/auth/session-bootstrap.test.ts</automated>
  </verify>
  <done>App restart path reconnects automatically with valid persisted session and handles unrecoverable sessions gracefully.</done>
</task>

<task type="auto">
  <name>Task 3: Add connected account identity and disconnect control</name>
  <files>src/ui/connection/account-menu.tsx</files>
  <action>Expose connected account display name and an easy-to-find disconnect/logout control in the connection area. Wire disconnect to token-store clear + auth-store reset so account switch begins from clean disconnected state.</action>
  <verify>
    <automated>npm test -- src/ui/connection</automated>
  </verify>
  <done>Connected UI shows account identity and disconnect reliably resets session to first-connect state.</done>
</task>

</tasks>

<verification>
Run auth persistence and bootstrap tests. Execute simulated restart test path proving recovered session enters connected state without user reauth.
</verification>

<success_criteria>
- AUTH-02 behavior is implemented through secure persistence + startup rehydrate.
- SECU-01 remains enforced in token handling and config boundaries.
- Logout/account-switch is explicit and resets session cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-spotify-connection-foundation/01-03-SUMMARY.md`
</output>
