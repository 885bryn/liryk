---
phase: 01-spotify-connection-foundation
plan: "04"
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - src/infra/spotify/spotify-auth-client.ts
  - src/app/auth-runtime.ts
  - src/app/auth-runtime.test.ts
  - src/app/bootstrap-auth.ts
  - src/ui/connection/connect-spotify-card.tsx
  - src/ui/connection/connect-flow.test.tsx
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-02
  - SECU-01
must_haves:
  truths:
    - User can start Spotify OAuth PKCE from the in-app Connect Spotify action without entering credentials in app UI.
    - User can return from Spotify callback and land in a connected state with explicit waiting/success messaging.
    - User restart path rehydrates or refreshes persisted tokens automatically and only falls back to reconnect when recovery fails.
    - User auth config and secrets remain sourced from env and secure token storage boundaries.
  artifacts:
    - path: src/infra/spotify/spotify-auth-client.ts
      provides: Concrete SpotifyAuthClient implementation used by production auth runtime.
    - path: src/app/auth-runtime.ts
      provides: Runtime wiring between auth service, token bootstrap, and UI auth store actions.
    - path: src/app/bootstrap-auth.ts
      provides: Startup bootstrap entrypoint consumed by runtime initialization path.
    - path: src/ui/connection/connect-spotify-card.tsx
      provides: Connect action wiring helper that dispatches auth-store and auth-service flow.
  key_links:
    - from: src/ui/connection/connect-spotify-card.tsx
      to: src/app/auth-runtime.ts
      via: onConnect action calling runtime connect flow
      pattern: onConnect|connectSpotify|startAuthorization
    - from: src/app/auth-runtime.ts
      to: src/core/auth/spotify-auth-service.ts
      via: runtime service orchestration and callback completion
      pattern: new SpotifyAuthService|completeAuthorization
    - from: src/app/auth-runtime.ts
      to: src/app/bootstrap-auth.ts
      via: startup initialize path
      pattern: bootstrapAuth|bootstrapAuthSession
---

<objective>
Close Phase 1 verification gaps by wiring existing auth/session primitives into a production runtime flow.

Purpose: Convert currently orphaned auth modules into an executable connect + restart lifecycle so AUTH-01 and AUTH-02 are truly achieved while preserving SECU-01 boundaries.
Output: Concrete Spotify auth client adapter, runtime auth coordinator, connect-action wiring, and startup bootstrap integration tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spotify-connection-foundation/01-CONTEXT.md
@.planning/phases/01-spotify-connection-foundation/01-RESEARCH.md
@.planning/phases/01-spotify-connection-foundation/01-VERIFICATION.md
@.planning/phases/01-spotify-connection-foundation/01-01-SUMMARY.md
@.planning/phases/01-spotify-connection-foundation/01-02-SUMMARY.md
@.planning/phases/01-spotify-connection-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add concrete Spotify auth client adapter and runtime service assembly</name>
  <files>src/infra/spotify/spotify-auth-client.ts, src/app/auth-runtime.ts, src/app/auth-runtime.test.ts</files>
  <action>Create a production Spotify auth client adapter that satisfies `SpotifyAuthClient` and consumes validated env configuration (no hardcoded client values). In `auth-runtime`, instantiate `SpotifyAuthService` with that adapter, expose connect/callback entry functions, and map lifecycle transitions into `AuthStore` state updates so the runtime path is no longer test-only. Add tests proving runtime assembly starts auth and returns an authorization URL from the real coordinator surface.</action>
  <verify>
    <automated>npm test -- src/app/auth-runtime.test.ts</automated>
    <manual>Confirm test assertions cover service instantiation and connect-start behavior through runtime APIs rather than direct unit-only service calls.</manual>
  </verify>
  <done>Production auth runtime can start Spotify authorization through a concrete adapter with no placeholder credentials and with store-visible state transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire connect action and callback completion through UI-model action helpers</name>
  <files>src/ui/connection/connect-spotify-card.tsx, src/app/auth-runtime.ts, src/ui/connection/connect-flow.test.tsx</files>
  <action>Add a dedicated connect action helper (matching existing model-builder patterns) that dispatches auth-store authorization state, invokes runtime connect flow, and routes callback completion into runtime/service + store state updates. Keep messaging decisions from CONTEXT.md unchanged (prominent Connect Spotify CTA, trust copy, explicit waiting text). Extend connect flow tests to assert that invoking card actions triggers runtime wiring and transitions through authorizing to connected/waiting states.</action>
  <verify>
    <automated>npm test -- src/ui/connection/connect-flow.test.tsx</automated>
    <manual>Confirm no connect path leaves the user in a blank state; failures still surface retry card behavior.</manual>
  </verify>
  <done>Connect UI action is wired to real auth runtime behavior and callback completion, satisfying the missing AUTH-01 production integration link.</done>
</task>

<task type="auto">
  <name>Task 3: Invoke startup bootstrap from runtime initialization and secure token store path</name>
  <files>src/app/auth-runtime.ts, src/app/bootstrap-auth.ts, src/app/auth-runtime.test.ts</files>
  <action>Integrate `bootstrapAuth` into runtime startup initialization so app launch always attempts token rehydrate/refresh before presenting disconnected state. Use `createTokenStore` as the secure persistence boundary and apply bootstrap results back into runtime session + auth store (`connected_waiting_playback` or reconnect fallback). Add startup tests covering: valid persisted session, expired session refreshed successfully, and unrecoverable refresh clearing tokens then returning disconnected/reconnect state.</action>
  <verify>
    <automated>npm test -- src/app/auth-runtime.test.ts src/core/auth/session-bootstrap.test.ts</automated>
    <manual>Review that startup wiring uses secure store/env modules only and does not add plaintext token persistence.</manual>
  </verify>
  <done>Startup path executes bootstrap in production wiring, restoring sessions across restarts when possible and preserving SECU-01 handling constraints.</done>
</task>

</tasks>

<verification>
Run runtime integration and connection model tests proving both previously failed truths now pass in production wiring:
- `npm test -- src/app/auth-runtime.test.ts src/ui/connection/connect-flow.test.tsx src/core/auth/session-bootstrap.test.ts`
</verification>

<success_criteria>
- AUTH-01: Connect Spotify action invokes runtime OAuth start and callback completion path (not test-only service usage).
- AUTH-02: Startup initialization invokes bootstrap recovery and restores connected state from secure persisted tokens when valid.
- SECU-01: Env and secure token-store boundaries remain the only credential/token sources in runtime wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/01-spotify-connection-foundation/01-04-SUMMARY.md`
</output>
