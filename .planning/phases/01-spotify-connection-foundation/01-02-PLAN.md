---
phase: 01-spotify-connection-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/state/auth/auth-store.ts
  - src/ui/connection/connect-spotify-card.tsx
  - src/ui/connection/permission-summary.tsx
  - src/ui/connection/retry-card.tsx
  - src/ui/connection/connected-status.tsx
  - src/ui/connection/connect-flow.test.tsx
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - User sees a clear first-run Connect Spotify entry with onboarding explanation.
    - User sees plain-language permissions/trust messaging before OAuth redirect.
    - User sees explicit success, waiting-for-track, and retry/troubleshooting states instead of blank screens.
  artifacts:
    - path: src/ui/connection/connect-spotify-card.tsx
      provides: Primary connect entry and onboarding copy.
    - path: src/ui/connection/retry-card.tsx
      provides: Persistent error recovery UI with retry/troubleshooting actions.
    - path: src/ui/connection/connected-status.tsx
      provides: Connected state with waiting message and account identity placeholder.
  key_links:
    - from: src/ui/connection/connect-spotify-card.tsx
      to: src/state/auth/auth-store.ts
      via: connect action dispatch
      pattern: startAuth|authorize
    - from: src/ui/connection/retry-card.tsx
      to: src/state/auth/auth-store.ts
      via: retry action dispatch
      pattern: retryAuth|resetAuthError
---

<objective>
Implement the user-facing Spotify connection states defined in phase context.

Purpose: Ensure connection flow is understandable, trustworthy, and recoverable for first-time and failure scenarios.
Output: Connection UI components and auth-state wiring that reflect all required messaging/states.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spotify-connection-foundation/01-CONTEXT.md
@.planning/phases/01-spotify-connection-foundation/01-RESEARCH.md
@.planning/phases/01-spotify-connection-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build auth UI state store contract</name>
  <files>src/state/auth/auth-store.ts</files>
  <action>Implement or extend auth store selectors/actions that map service states to UI states (`disconnected`, `authorizing`, `success`, `connected_waiting_playback`, `recoverable_error`). Include explicit fields for user-facing error reason and retry eligibility, and account-display information for connected state.</action>
  <verify>
    <automated>npm test -- src/state/auth</automated>
  </verify>
  <done>UI can render all required connection states from a stable auth store contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement first-run connect and trust messaging components</name>
  <files>src/ui/connection/connect-spotify-card.tsx, src/ui/connection/permission-summary.tsx, src/ui/connection/connected-status.tsx</files>
  <action>Build shadcn-compatible UI components for first connect entry, one-line onboarding explainer, plain-language permission summary, and connected state messaging including exact waiting text `Connected - play a track on Spotify`. Include post-auth success confirmation micro-state before normal connected display.</action>
  <verify>
    <automated>npm test -- src/ui/connection/connect-flow.test.tsx</automated>
  </verify>
  <done>Connect flow surfaces required onboarding, trust copy, success confirmation, and waiting-for-track state.</done>
</task>

<task type="auto">
  <name>Task 3: Implement persistent retry and troubleshooting card</name>
  <files>src/ui/connection/retry-card.tsx, src/ui/connection/connect-flow.test.tsx</files>
  <action>Create persistent retry UI for auth failures with reason-specific human-readable copy (cancel, timeout, network, rate-limit). Add visible auto-retry status for temporary failures and a guided troubleshooting branch after repeated failures. Ensure retry card calls store actions rather than embedding auth logic.</action>
  <verify>
    <automated>npm test -- src/ui/connection/connect-flow.test.tsx</automated>
  </verify>
  <done>Failure states have clear retry and troubleshooting paths, matching context constraints.</done>
</task>

</tasks>

<verification>
Run UI/auth-store tests. Confirm required waiting-state string is rendered exactly and no blank-state UI path exists.
</verification>

<success_criteria>
- AUTH-01 user-facing flow is explicit, trustworthy, and recoverable.
- Locked context decisions for connect, permissions, success, and failures are represented in tasks/components.
- Connection state UX is deterministic and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/01-spotify-connection-foundation/01-02-SUMMARY.md`
</output>
